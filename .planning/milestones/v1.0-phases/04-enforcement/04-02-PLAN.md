---
phase: 04-enforcement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - commitlint.config.mjs
  - buf.yaml
  - buf.gen.yaml
autonomous: false
requirements: [ENF-03, ENF-04]
user_setup:
  - service: npm
    why: "commitlint is the single Node dev dependency (Conventional Commits validation)"
    bootstrap:
      - task: "Run `npm install` once to install exact-pinned @commitlint/cli + @commitlint/config-conventional"
        location: "repo root (after package.json exists)"
must_haves:
  truths:
    - "`echo 'bad message' | npx --no-install commitlint` exits non-zero (rejects non-Conventional commit) (ENF-03)"
    - "`echo 'docs(04): x' | npx --no-install commitlint` exits 0 (accepts the repo's own GSD commit style)"
    - "`buf build` exits 0 on the empty skeleton module (no `.proto` -> empty, not error) (ENF-04)"
    - "commitlint is the ONLY Node dependency; `package.json` is `private: true` with exact pins (D-04)"
  artifacts:
    - path: "package.json"
      provides: "private commitlint devDeps (exact-pinned), the single Node dependency"
      contains: "\"private\": true"
    - path: "commitlint.config.mjs"
      provides: "extends @commitlint/config-conventional"
      contains: "config-conventional"
    - path: "buf.yaml"
      provides: "v2 lint+breaking skeleton (NOT hooked, no .proto yet)"
      contains: "version: v2"
    - path: "buf.gen.yaml"
      provides: "v2 codegen skeleton with pinned plugins (inert until .proto exist)"
      contains: "version: v2"
  key_links:
    - from: "commitlint.config.mjs"
      to: "@commitlint/config-conventional"
      via: "extends"
      pattern: "config-conventional"
    - from: "package.json"
      to: "@commitlint/cli"
      via: "exact-pinned devDependency"
      pattern: "@commitlint/cli"
---

<objective>
Ship the commitlint config group (ENF-03) — a private root `package.json` exact-pinning `@commitlint/cli` + `@commitlint/config-conventional`, and a `commitlint.config.*` extending config-conventional — plus the buf **skeleton** (ENF-04): `buf.yaml` (v2 lint+breaking) and `buf.gen.yaml` (v2, pinned codegen plugins). The commitlint config is wired into the `commit-msg` hook by plan 04-03; the buf skeleton is wired into NO hook (D-10, no-phantom).

Purpose: Conventional Commits become mechanically enforceable (the single isolated Node dep, D-04), and proto codegen is **seeded** with pinned plugins ready to activate when `.proto` schemas land — without claiming working codegen today.
Output: `package.json`, `commitlint.config.mjs`, `buf.yaml`, `buf.gen.yaml`.

This plan is `autonomous: false`: it contains a blocking legitimacy checkpoint for the two SUS-flagged commitlint npm packages before `npm install`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enforcement/04-CONTEXT.md
@.planning/phases/04-enforcement/04-RESEARCH.md
@.planning/phases/04-enforcement/04-PATTERNS.md
@knowledge/git.md
@knowledge/boundaries.md
</context>

<artifacts_this_phase_produces>
This plan creates (of the full phase set):
- `package.json` — ENF-03, private, commitlint devDeps only (exact-pinned)
- `commitlint.config.mjs` — ENF-03, extends config-conventional (`.mjs` forces ESM under Node 22)
- `buf.yaml` — ENF-04, v2 lint+breaking skeleton
- `buf.gen.yaml` — ENF-04, v2 codegen skeleton (pinned plugins, inert)

(`.golangci.yml`/`Makefile` are produced by 04-01; `lefthook.yml` by 04-03; the `knowledge/*.md` ENF-05 flips by 04-04.)
</artifacts_this_phase_produces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Legitimacy gate — verify the two SUS-flagged commitlint npm packages before install</name>
  <what-built>
    Nothing yet — this gate runs BEFORE `npm install`. RESEARCH Package Legitimacy Audit (~L128-138) flagged both `@commitlint/cli` and `@commitlint/config-conventional` as SUS via a `too-new` verdict. The audit concludes this is a FALSE POSITIVE (the `too-new` flag fires on the latest release date, not package age): both are mature packages — 8.5M+ / 8.1M+ weekly downloads, real source repo `github.com/conventional-changelog/commitlint`, `postinstall: null` (no install script), not deprecated. Recommendation: exact-pin a stable (`21.0.2`, or `21.0.1` as a settling buffer).
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@commitlint/cli — confirm weekly downloads in the millions, repository points to `conventional-changelog/commitlint`, latest version is the one to be pinned, no deprecation banner.
    2. Open https://www.npmjs.com/package/@commitlint/config-conventional — same checks; confirm major matches `@commitlint/cli`.
    3. Confirm the exact version to pin (RESEARCH suggests `21.0.2`; verify it is still current/stable at execution time — `21.0.1` is an acceptable settling buffer).
    4. Confirm both packages have no `postinstall` script.
  </how-to-verify>
  <resume-signal>Type "approved" (and the exact version to pin, e.g. "21.0.2"), or describe the discrepancy to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create `package.json` + `commitlint.config.mjs` (ENF-03) and run npm install bootstrap</name>
  <files>package.json, commitlint.config.mjs</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §Code Examples `package.json` (~L437-447) and `commitlint.config.js` (~L451-458)
    - .planning/phases/04-enforcement/04-RESEARCH.md §Open Question 2 (~L569-572) — module format under Node 22; §Pitfall 6 (~L312-316) — `--no-install`
    - .planning/phases/04-enforcement/04-PATTERNS.md §`package.json` / §`commitlint.config.*` (~L76-94)
    - knowledge/git.md §Conventional Commits (~L18-58) — the `docs(NN):` GSD commit style the config MUST accept
  </read_first>
  <action>
    Create `package.json` at repo root from RESEARCH §Code Examples: `{ "name": "gwall-e-tooling", "private": true, ... "devDependencies": { "@commitlint/cli": "<pinned>", "@commitlint/config-conventional": "<pinned>" } }` using the version approved in Task 1 (exact pin, NO caret/tilde — supply-chain hardening, D-04). `private: true` prevents accidental publish; commitlint MUST stay the ONLY Node dependency — do NOT add any other devDep. Create the commitlint config as `commitlint.config.mjs` (use `.mjs` to force ESM, since `package.json` has no `"type": "module"` and a bare `.js` may resolve as CJS and break `export default` — RESEARCH Open Question 2). Content: `export default { extends: ['@commitlint/config-conventional'] };`. config-conventional's type-enum already accepts the repo's own GSD commit style (`docs(04): ...`, `feat(04): ...`, `chore(04): ...` — git.md), so the config MUST NOT reject those. Then run `npm install` (one-time bootstrap, D-04) to install the exact-pinned packages. Verify the round-trip: a bad message is rejected and a valid GSD-style message is accepted.
  </action>
  <verify>
    <automated>test -f package.json && grep -q '"private": true' package.json && grep -q '@commitlint/cli' package.json && test -f commitlint.config.mjs && grep -q 'config-conventional' commitlint.config.mjs && npm install >/dev/null 2>&1 && (printf 'bad message' | npx --no-install commitlint && echo UNEXPECTED_PASS || echo REJECTED_OK) && printf 'docs(04): wire commitlint' | npx --no-install commitlint && echo ACCEPTED_OK</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` exists, `private: true`, exact pins (no caret/tilde), commitlint the only dependency
    - `commitlint.config.mjs` extends `@commitlint/config-conventional`
    - `npm install` succeeds (one-time bootstrap)
    - `printf 'bad message' | npx --no-install commitlint` exits non-zero
    - `printf 'docs(04): x' | npx --no-install commitlint` exits 0
  </acceptance_criteria>
  <done>commitlint installed and isolated as the single Node devDep; rejects non-Conventional, accepts GSD `docs(04):` style.</done>
</task>

<task type="auto">
  <name>Task 3: Create buf skeleton `buf.yaml` + `buf.gen.yaml` (ENF-04, no-phantom)</name>
  <files>buf.yaml, buf.gen.yaml</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §Code Examples `buf.yaml` (~L462-475) and `buf.gen.yaml` (~L478-492)
    - .planning/phases/04-enforcement/04-RESEARCH.md §Pitfall 7 (~L318-322) — phantom-codegen phrasing; §Assumption A4 (~L558) — plugin version pins
    - .planning/phases/04-enforcement/04-PATTERNS.md §`buf.yaml` / §`buf.gen.yaml` (~L96-114)
  </read_first>
  <action>
    Create `buf.yaml` at repo root from RESEARCH §Code Examples: `version: v2`; `modules: [{ path: proto }]` (future proto-root, no files yet); `lint.use: [STANDARD]`; `breaking.use: [FILE]`. Create `buf.gen.yaml`: `version: v2`; pinned remote plugins `buf.build/protocolbuffers/go:v1.36.5` (protoc-gen-go) and `buf.build/grpc/go:v1.5.1` (protoc-gen-go-grpc), each `out: gen/go`, `opt: [paths=source_relative]` (confirm exact plugin versions at execution per Assumption A4; the root go.mod shows protobuf runtime v1.36.5). Both files MUST carry a header comment stating they are a **SKELETON**: no `.proto` exist yet, buf is NOT wired into any lefthook hook, and codegen ACTIVATES WHEN `.proto` ARE ADDED — never phrase as "working codegen" (Pitfall 7, D-10, no-phantom). Do NOT add protovalidate (optional, only when validation schemas exist). Verify `buf build` exits 0 (empty module → empty output, not an error).
  </action>
  <verify>
    <automated>test -f buf.yaml && grep -q 'version: v2' buf.yaml && grep -qi 'skeleton' buf.yaml && test -f buf.gen.yaml && grep -q 'version: v2' buf.gen.yaml && grep -q 'protocolbuffers/go' buf.gen.yaml && (command -v buf >/dev/null 2>&1 && buf build && echo BUF_BUILD_OK || echo "MISSING — run 'make tools' first, then: buf build (exits 0 on empty module)")</automated>
  </verify>
  <acceptance_criteria>
    - `buf.yaml` v2 with `lint.use: [STANDARD]` + `breaking.use: [FILE]`, module path `proto`
    - `buf.gen.yaml` v2 with exact-pinned remote plugins
    - Both files' header comments mark them SKELETON / not-hooked / codegen-activates-later (no-phantom)
    - `buf build` exits 0 on the empty module (after `make tools`)
  </acceptance_criteria>
  <done>buf config seeded as an honest skeleton; `buf build` green on empty module; not wired into any hook.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → npm registry | `npm install` fetches commitlint packages (supply chain — only Node deps in the repo) |
| developer machine → buf remote plugins | `buf.gen.yaml` references remote codegen plugins (inert until `.proto` exist) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SC | Tampering | npm install of `@commitlint/cli` + `@commitlint/config-conventional` (both SUS via `too-new` false-positive) | mitigate | Blocking legitimacy checkpoint (Task 1) verifies real repo / 8M+ downloads / `postinstall: null` before install; exact-pin in `package.json` (`private: true`, no caret/tilde); legitimacy checkpoint is never auto-approvable |
| T-4-04 | Tampering | unpinned buf codegen plugins drifting | mitigate | Exact-pin remote plugin versions in `buf.gen.yaml`; inert until `.proto` exist (not executed this phase) |
| T-4-05 | Repudiation/Info | buf skeleton presented as working codegen (phantom) | mitigate | Header comments mark SKELETON + not-hooked + activates-later (Pitfall 7, D-10) |
| T-4-06 | Tampering | bare `npx` pulling an unpinned commitlint from the network at hook time | mitigate | `commit-msg` hook (plan 04-03) uses `npx --no-install` so it fails loudly rather than fetching a drifted version (Pitfall 6) |
</threat_model>

<verification>
- `npm install` → exit 0; `npx --no-install commitlint` rejects `bad message`, accepts `docs(04): ...`
- `buf build` → exit 0 on the empty skeleton module (after `make tools`)
- `grep -c skeleton buf.yaml buf.gen.yaml` → both contain the honesty marker
</verification>

<success_criteria>
- commitlint installed as the single private, exact-pinned Node devDep; round-trip passes
- buf skeleton parses (`buf build` exit 0), is marked skeleton, and is wired into NO hook
- Legitimacy checkpoint cleared before any npm install
</success_criteria>

<output>
Create `.planning/phases/04-enforcement/04-02-SUMMARY.md` when done
</output>
