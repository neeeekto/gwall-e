---
phase: 04-enforcement
plan: 04
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - knowledge/authoring.md
  - knowledge/style.md
  - knowledge/architecture.md
  - knowledge/testing.md
  - knowledge/patterns.md
  - knowledge/build.md
  - knowledge/boundaries.md
  - knowledge/README.md
autonomous: true
requirements: [ENF-05]
must_haves:
  truths:
    - "`grep -rn 'planned:' knowledge/` returns nothing — every Phase-3 forward mark is flipped (ENF-05)"
    - "No `CI-gated` STATUS is applied to any rule; `CI-gated` appears only in authoring.md's legend definition + 'becomes CI-gated when CI lands' notes (D-07/D-08, Pitfall 8)"
    - "`knowledge/authoring.md` §Статус enforcement is the single canon defining 3 statuses + the 'no CI-gated without CI' rule (D-09)"
    - "`knowledge/build.md` documents the one-time bootstrap: `make tools` + `npm install` + `lefthook install` (D-11)"
    - "depguard layer-direction marks read `hook (..., dormant)`; resurrection bans read `hook (..., biting)`; gofumpt reads `hook (format)`; errorlint reads `hook (lint: errorlint)`; typed-IDs and mockery read `convention-only` (D-05/D-06/D-08, A1/A2)"
  artifacts:
    - path: "knowledge/authoring.md"
      provides: "canon 3-status enforcement legend (hook/convention-only/CI-gated-reserved) + no-CI-gated-without-CI rule"
      contains: "Статус enforcement"
    - path: "knowledge/build.md"
      provides: "tooling bootstrap recipe (make tools / npm install / lefthook install)"
      contains: "lefthook install"
    - path: "knowledge/boundaries.md"
      provides: "ownership-map rows for enforcement taxonomy + tool bootstrap + inventory pre-push exclusion"
      contains: "Карта владения"
  key_links:
    - from: "knowledge/style.md"
      to: ".golangci.yml errorlint"
      via: "ENF-05 mark flip %w -> hook (lint: errorlint)"
      pattern: "errorlint"
    - from: "knowledge/architecture.md"
      to: ".golangci.yml depguard no-cqrs-bus"
      via: "ENF-05 mark flip -> hook (lint: depguard, biting)"
      pattern: "biting"
    - from: "knowledge/build.md"
      to: "Makefile make tools"
      via: "bootstrap recipe cross-ref"
      pattern: "make tools"
---

<objective>
Complete ENF-05 — flip every Phase-3 forward enforcement mark in `knowledge/*.md` to its **truthful** status now that the configs (04-01/04-02/04-03) exist, refine the canonical status legend in `authoring.md` (D-09), document the tooling bootstrap in `build.md` (D-11), and register the new enforcement facts honestly in `boundaries.md`'s ownership map. Statuses are honest: `hook` for what a committed git hook checks today (lint/format/commit-msg/test), `convention-only` for review-enforced, and NOTHING is labeled `CI-gated` (no CI exists this phase — D-07/D-08, Pitfall 8).

Purpose: the knowledge base stops being purely declarative — each mechanizable rule now carries its real enforcement mechanism, with no phantom claims (dormant depguard layer rules say "dormant"; buf/codegen stays skeleton; mockery stays convention-only).
Output: edits to `authoring.md`, `style.md`, `architecture.md`, `testing.md`, `patterns.md` (verify-only), `build.md`, `boundaries.md`, `README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enforcement/04-CONTEXT.md
@.planning/phases/04-enforcement/04-RESEARCH.md
@.planning/phases/04-enforcement/04-PATTERNS.md
@.planning/phases/04-enforcement/04-01-SUMMARY.md
@.planning/phases/04-enforcement/04-02-SUMMARY.md
@.planning/phases/04-enforcement/04-03-SUMMARY.md
@knowledge/authoring.md
@knowledge/style.md
@knowledge/architecture.md
@knowledge/testing.md
@knowledge/patterns.md
@knowledge/build.md
@knowledge/boundaries.md
@knowledge/README.md
</context>

<artifacts_this_phase_produces>
This plan edits (of the full phase set) the `knowledge/*.md` enforcement-status changes:
- `knowledge/authoring.md` — canon 3-status legend rewrite (D-09)
- `knowledge/style.md` — 3 mark flips (gofumpt/errorlint/DTO-depguard) + 1 to convention-only (typed-IDs, A1)
- `knowledge/architecture.md` — 4 mark flips (2 dormant layer, 2 biting bans) + 1-line "CI-gated when CI lands" note
- `knowledge/testing.md` — 1 mark flip (mockery -> convention-only, A2) + phrasing honesty
- `knowledge/patterns.md` — verify-only (already all convention-only; grep gate, likely no-op)
- `knowledge/build.md` — tooling bootstrap recipe (D-11)
- `knowledge/boundaries.md` — ownership-map rows for the new enforcement facts
- `knowledge/README.md` — index/status consistency

(Configs `.golangci.yml`/`Makefile`/`package.json`/`commitlint.config.mjs`/`lefthook.yml`/`buf.yaml`/`buf.gen.yaml` are produced by 04-01/04-02/04-03.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Refine authoring.md legend (canon) + flip marks in style/architecture/testing; verify patterns</name>
  <files>knowledge/authoring.md, knowledge/style.md, knowledge/architecture.md, knowledge/testing.md, knowledge/patterns.md</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §ENF-05 Mapping Table (~L512-533) — authoritative per-mark instruction (verbatim current mark -> flipped status)
    - .planning/phases/04-enforcement/04-RESEARCH.md §Pitfall 8 (~L324-328) — no CI-gated without CI; §Assumptions A1/A2 (~L555-556)
    - .planning/phases/04-enforcement/04-PATTERNS.md §authoring.md / §style.md / §architecture.md / §testing.md / §patterns.md (~L138-207) — exact line numbers + verbatim marks + flip targets
    - knowledge/authoring.md L64-68 (existing §Статус enforcement); the authoring standard (force tags + "запрет -> do" + pointer-over-copy) which every edit MUST preserve
  </read_first>
  <action>
    Apply the ENF-05 Mapping Table per-doc, flipping STATUS TEXT ONLY (never rewriting rule bodies); preserve the `⟶ ` mark prefix, MUST/SHOULD/WON'T force tags, "запрет -> do" pairing, and pointer-over-copy (one fact = one canon).

    **authoring.md (CANON LEGEND, D-09):** Rewrite §"Статус enforcement" (L64-68). Define the 3 statuses precisely: `hook` = rule checked by a committed git hook today (lint/format/commit-msg/test); `convention-only` = review-enforced; `CI-gated` = RESERVED until a CI pipeline exists (same `.golangci.yml`/`buf` reused unchanged). Add the rule: **MUST NOT** mark anything `CI-gated` without a CI pipeline (do: use `hook` while only local hooks exist; CI-gated -> WON'T until CI lands). Change "В Phase 1 фиксируется только сам стандарт пометок ... (Phase 4, задел под ENF-05)" -> "статусы проставлены (Phase 4)". This is the single canon; topic docs reference it, do not redefine.

    **style.md (4 marks):** L14 gofumpt `(planned: hook Phase 4)` -> `hook (format: gofumpt)`. L53 typed IDs `⟶ planned: CI-gated Phase 4 (linter)` -> `⟶ convention-only (review-enforced)` (no off-the-shelf linter enforces "use a named type for IDs" — A1; a custom linter is out of scope). L72 `%w` `⟶ planned: CI-gated Phase 4 (linter, напр. errorlint)` -> `⟶ hook (lint: errorlint)`. L90 DTO->домен `⟶ planned: CI-gated Phase 4 (depguard)` -> `⟶ hook (lint: depguard, dormant)` (note dormancy). Update the prose preamble (~L10-11, "Phase 4 переключит её на фактический") to read as done. The existing `convention-only (review-enforced)` marks stay unchanged.

    **architecture.md (4 marks):** L26-27 domain inward-only `⟶ planned: CI-gated Phase 4 (depguard)` -> `⟶ hook (lint: depguard, dormant)` + ONE line "становится CI-gated при появлении CI" (D-08). L30 domain-holds-only-ports `⟶ planned: CI-gated Phase 4 (depguard)` -> `⟶ hook (lint: depguard, dormant)`. L151 CQRS bus ban -> `⟶ hook (lint: depguard, biting)` (fires today). L154 TxManager ban -> `⟶ hook (lint: depguard, biting)` (fires on reintroduction). The `convention-only` marks stay unchanged.

    **testing.md (1 mark + honesty):** L111 mockery `⟶ planned: Phase 4 (go:generate)` -> `⟶ convention-only (review-enforced)` + note "mockery harness deferred" (A2 — full mockery wiring is out of scope/Deferred; keep the convention, do NOT claim `go:generate` works). Update the L114 / L126 "planned: Phase 4" phrasing so it does not imply this phase wired mockery (keep no-phantom). The other `convention-only` marks stay (pre-push runs the suite via `go test`, but structure rules stay review-enforced).

    **patterns.md (VERIFY-ONLY, likely no-op):** all marks are already `⟶ convention-only (review-enforced)` and stay unchanged (recipes reference architecture/style). Verify via grep that no `planned:` remains; otherwise leave untouched — do NOT edit blindly.

    After edits, the global grep gate MUST pass: no `planned:` anywhere in `knowledge/`, and `CI-gated` only in authoring.md's legend definition + any "becomes CI-gated when CI lands" notes.
  </action>
  <verify>
    <automated>! grep -rn 'planned:' knowledge/ && grep -rln 'CI-gated' knowledge/ | sort -u | grep -qx 'knowledge/architecture.md' ; grep -rln 'CI-gated' knowledge/ | grep -vqE '(authoring|architecture)\.md' && echo "FAIL: CI-gated leaked outside legend/notes" || echo "CI-gated confined OK" ; grep -q 'hook (lint: errorlint)' knowledge/style.md && grep -q 'biting' knowledge/architecture.md && grep -q 'dormant' knowledge/architecture.md && grep -qi 'convention-only' knowledge/testing.md && echo FLIPS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'planned:' knowledge/` returns nothing (all forward marks flipped)
    - `CI-gated` appears ONLY in authoring.md's legend definition and architecture.md's "becomes CI-gated when CI lands" note (no rule STATUS is CI-gated)
    - authoring.md §Статус enforcement defines 3 statuses + "no CI-gated without CI" rule (canon)
    - style.md: gofumpt->`hook (format)`, `%w`->`hook (lint: errorlint)`, DTO->`hook (lint: depguard, dormant)`, typed-IDs->`convention-only`
    - architecture.md: 2 layer marks->`hook (..., dormant)`, 2 bans->`hook (..., biting)`; testing.md mockery->`convention-only`
    - patterns.md unchanged (verified no `planned:`)
  </acceptance_criteria>
  <done>All mechanizable rules flipped to truthful statuses; authoring.md legend is the single canon; no phantom `CI-gated`; grep gate green.</done>
</task>

<task type="auto">
  <name>Task 2: Document tooling bootstrap in build.md (D-11); register enforcement facts in boundaries.md + README.md</name>
  <files>knowledge/build.md, knowledge/boundaries.md, knowledge/README.md</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §Installation (~L105-119) — exact bootstrap commands; §ENF-05 Mapping (net-new live enforcement, ~L533)
    - .planning/phases/04-enforcement/04-PATTERNS.md §build.md / §boundaries.md ownership / §README.md (~L209-275)
    - knowledge/build.md L16-45 (existing recipe sections — match the style); knowledge/boundaries.md §"Карта владения фактами" (L55-70)
    - .planning/phases/04-enforcement/04-01-SUMMARY.md / 04-02-SUMMARY.md / 04-03-SUMMARY.md — confirm the actual file names/locations created (e.g. `commitlint.config.mjs`)
  </read_first>
  <action>
    **build.md (D-11 bootstrap):** Add a bootstrap section in the existing recipe style (MUST/SHOULD/WON'T, no fragile listings, pointer-over-copy) documenting the one-time tooling setup: `make tools` (Go tools — golangci-lint v2.12.2, lefthook v2.1.9, buf v1.71.0; cross-ref the `Makefile`, do not duplicate versions — pointer-over-copy), `npm install` (commitlint — the single Node dep), `lefthook install` (activate per-clone git hooks). Keep build.md the canon for commands (boundaries.md ownership). Document the `inventory` pre-push exclusion as intentional (D-03), referencing the WIP rationale already in structure.md/boundaries.md (link, do not copy). Do NOT document buf codegen as working (skeleton — no-phantom).

    **boundaries.md (ownership map):** In §"Карта владения фактами" add honest rows: enforcement-status taxonomy -> канон `authoring.md` (существует); tooling bootstrap commands -> канон `build.md` (существует); the `inventory` pre-push exclusion fact -> канон `build.md`/`boundaries.md` (существует as a documented intentional exclusion). Keep "один факт = один канон-док"; reference, do not copy. The config files (`.golangci.yml`/`lefthook.yml`/`package.json`/`commitlint.config.mjs`/`buf.yaml`/`buf.gen.yaml`/`Makefile`) DO exist after this phase (status "существует"); buf CODEGEN does NOT (phrase the buf row as skeleton/seeded).

    **README.md (index):** Update status/notes consistent with the existing index table only if it adds agent value. The new root config files live in repo root (NOT `knowledge/`) — reference them only honestly; index links MUST point only to existing files (README rule). Do not invent links to non-existent files.
  </action>
  <verify>
    <automated>grep -q 'make tools' knowledge/build.md && grep -q 'lefthook install' knowledge/build.md && grep -q 'npm install' knowledge/build.md && grep -qi 'inventory' knowledge/build.md && grep -q 'authoring.md' knowledge/boundaries.md && grep -qi 'enforcement\|статус' knowledge/boundaries.md && ! grep -rn 'planned:' knowledge/ && echo BOOTSTRAP_AND_OWNERSHIP_OK</automated>
  </verify>
  <acceptance_criteria>
    - build.md documents `make tools` + `npm install` + `lefthook install` bootstrap (D-11), pointer-over-copy to the Makefile version table
    - build.md documents the inventory pre-push exclusion as intentional (D-03), no phantom buf codegen
    - boundaries.md ownership map gains honest rows (enforcement taxonomy -> authoring.md; bootstrap -> build.md; inventory exclusion fact)
    - README.md index remains link-honest (only existing files)
    - `grep -rn 'planned:' knowledge/` still returns nothing
  </acceptance_criteria>
  <done>Bootstrap documented in the build.md canon; new enforcement facts registered in the ownership map honestly; index link-integrity intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| knowledge docs → AI/dev consumers | docs assert what is enforced; a phantom status misleads agents into unsafe assumptions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-09 | Repudiation/Info | a rule marked `CI-gated` when no CI exists (phantom enforcement misleading agents) | mitigate | Grep gate asserts no `CI-gated` STATUS remains; authoring.md legend forbids `CI-gated` without CI (D-07/Pitfall 8); confined to legend definition + "when CI lands" note |
| T-4-10 | Repudiation/Info | dormant depguard layer rule documented as actively enforcing | mitigate | Layer marks flip to `hook (..., dormant)` explicitly (D-05); mockery stays `convention-only` (A2); buf stays skeleton — no-phantom |
| T-4-11 | Info | stale `planned:` mark left behind, leaving a rule statusless | mitigate | Global `grep -rn 'planned:' knowledge/` gate must return nothing before phase verification |
| T-4-SC | Tampering | (cross-phase) tool/npm supply chain | accept | This plan edits docs only — no installs; the supply-chain mitigations live in 04-01/04-02. `T-4-SC` tracked at phase level. |
</threat_model>

<verification>
- `grep -rn 'planned:' knowledge/` → nothing
- `CI-gated` only in authoring.md legend + architecture.md "when CI lands" note
- build.md: `make tools` / `npm install` / `lefthook install` present; inventory exclusion documented
- Link integrity across `knowledge/*.md` intact (README points only to existing files)
</verification>

<success_criteria>
- Every Phase-3 forward mark flipped to a truthful status; no phantom `CI-gated`
- authoring.md legend is the single 3-status canon; topic docs reference it
- Tooling bootstrap documented (D-11); ownership map registers the new enforcement facts honestly
</success_criteria>

<output>
Create `.planning/phases/04-enforcement/04-04-SUMMARY.md` when done
</output>
