---
phase: 02-foundation-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - knowledge/structure.md
  - knowledge/build.md
  - knowledge/README.md
autonomous: true
requirements: [DOC-01, DOC-02]
must_haves:
  truths:
    - "Агент видит, какие модули в go.work (pkg, analytics, audit) и какие вне (inventory)"
    - "Агент знает, что inventory собирается с GOWORK=off и помечен WIP (не гарантированно компилируется)"
    - "Агент находит реально работающие команды теста pkg и сборки audit, не выдуманные"
    - "knowledge/README.md больше не помечает structure.md и build.md как «запланировано» и ссылается на них"
  artifacts:
    - path: "knowledge/structure.md"
      provides: "Раскладка go.work на уровне модулей/workspace"
      contains: "go.work"
      min_lines: 25
    - path: "knowledge/build.md"
      provides: "Команды сборки/запуска/тестов, включая GOWORK=off для inventory"
      contains: "GOWORK=off"
      min_lines: 25
    - path: "knowledge/README.md"
      provides: "Индекс со ссылками на structure.md и build.md (без статуса «запланировано»)"
      contains: "structure.md"
  key_links:
    - from: "knowledge/structure.md"
      to: "knowledge/build.md"
      via: "относительная ссылка (команды — канон в build.md)"
      pattern: "build\\.md"
    - from: "knowledge/README.md"
      to: "knowledge/structure.md"
      via: "markdown-ссылка в таблице индекса"
      pattern: "\\[.*\\]\\(structure\\.md\\)"
---

<objective>
Наполнить два инфра-дока навигации/сборки: `knowledge/structure.md` (DOC-01) — раскладка
репозитория на уровне модулей/workspace; `knowledge/build.md` (DOC-02) — реально работающие
команды сборки/запуска/тестов. Снять статус «запланировано» и добавить ссылки в индекс
`knowledge/README.md` для обоих доков (ссылка появляется ВМЕСТЕ с файлом — без битых ссылок).

Purpose: команда/ИИ получают стабильную карту «что где лежит» и «как собрать/протестировать»
на уровне возможностей, без хрупких path-карт и phantom-команд (Pitfall 2/3/8).
Output: `knowledge/structure.md`, `knowledge/build.md`, обновлённые строки индекса в `knowledge/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-docs/02-CONTEXT.md
@knowledge/authoring.md
@knowledge/README.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
Этот план создаёт/изменяет:
- `knowledge/structure.md` (новый файл)
- `knowledge/build.md` (новый файл)
- `knowledge/README.md` (изменение: индекс-строки structure.md и build.md — снять «запланировано», проставить ссылку и статус «существует»; обновить блок «Порядок чтения», убрав маркер *(Phase 2)* у этих двух доков)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Создать knowledge/structure.md — раскладка go.work на уровне модулей</name>
  <files>knowledge/structure.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт MUST/SHOULD/WON'T, парность «запрет → do», pointer-over-copy, no-phantom, размер ~150–200 строк
    - knowledge/README.md — блок «Что где живёт», формат индекса (язык: русское содержимое, тех-термины английские)
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — D-02 (уровень модулей/workspace, НЕ целевая слоистая раскладка), D-03 (inventory/internal/* stale — НЕ авторитет), D-04 (gateway/outgate не упоминать)
    - .planning/PROJECT.md — Key Decisions (inventory вне go.work, снос internal/), Context (workspace)
    - Реальный репо: файл `go.work` в корне (членство: ./pkg, ./services/analytics, ./services/audit; go 1.24.6); `services/inventory/go.mod` (вне workspace)
  </read_first>
  <action>
    Создай `knowledge/structure.md` на русском (тех-термины/имена — английские), по стандарту
    authoring.md (каждое нормативное правило с тегом MUST/SHOULD/WON'T; каждый запрет — с
    предписанной альтернативой «do»). Документируй на уровне МОДУЛЕЙ/workspace (per D-02), НЕ
    описывай целевую слоистую внутреннюю раскладку сервиса (domain/usecases/query/...) — это
    architecture.md (Phase 3), здесь — WON'T с указанием «слои живут в architecture.md».
    Содержимое:
    - Мульти-модульный Go workspace: корневой `go.work` (Go 1.24.6) с членами `./pkg`,
      `./services/analytics`, `./services/audit`. Назови эти три как активные модули workspace.
    - `pkg` — общие пакеты (на момент написания реально есть `pkg/http`); описывать как «общая
      библиотека», БЕЗ перечисления конкретных под-пакетов как карты путей (per D-02 / authoring
      pointer-over-copy / Pitfall 3).
    - `inventory` — модуль ВНЕ `go.work` (собирается отдельно, `GOWORK=off`), помечен WIP:
      пересобирается, его текущая внутренняя раскладка `internal/*` (пустые stale-каталоги)
      НЕ авторитетна и НЕ эталон (per D-03). Не фиксировать `internal/{api,app,cron,domain,repository,usecase}`
      как структуру — WON'T, потому что это снесённые/пустые леса; вместо этого — пометка WIP и
      ссылка на `boundaries.md` (правило «не чинить леса»). Команды для inventory — см. `build.md`.
    - Фронтенд `web/` — отдельная часть: Nx-workspace (TypeScript/React), не входит в Go workspace.
      Описать как отдельный инфра-блок; команды — см. `build.md`.
    - Gateway/outgate НЕ упоминать (per D-04 — только README+go.mod, кода нет).
    - Pointer-over-copy: WIP-статус `inventory` — канон ЗДЕСЬ (structure.md); команды сборки/тестов
      — канон в `build.md` (относительная ссылка `build.md`, не копировать команды). Добавь
      относительную ссылку на `boundaries.md` для правила «не трогать WIP-леса» (файл появится
      в Wave 2; на момент Task 1 ссылка ещё битая — поэтому ссылку на boundaries.md ВСТАВЬ как
      упоминание «см. boundaries.md (правила границ)» — допустимо, т.к. boundaries.md создаётся
      в этой же фазе; финальная проверка битых ссылок — в плане 02-03 после создания boundaries.md).
    Размер: держать ~< 120 строк; если превышает 150 — вынести подтопик и связать ссылкой (authoring).
    Не вставлять фенсед-блоки с длинными листингами путей.
  </action>
  <verify>
    <automated>test -f knowledge/structure.md && grep -q 'go.work' knowledge/structure.md && grep -q 'GOWORK=off\|build.md' knowledge/structure.md && grep -q 'WIP' knowledge/structure.md && ! grep -Eq 'gateway|outgate' knowledge/structure.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/structure.md` существует
    - содержит строку `go.work`
    - называет членов workspace: `pkg`, `analytics`, `audit` (grep каждого находит)
    - помечает `inventory` как WIP/вне workspace (содержит `WIP` и `inventory`)
    - НЕ содержит подстрок `gateway` и `outgate` (per D-04)
    - НЕ перечисляет `internal/api`, `internal/usecase` и т.п. как авторитетную структуру (grep `internal/usecase` не находит)
    - ссылается на `build.md` для команд (содержит `build.md`)
    - каждое нормативное правило несёт тег `**MUST**`/`**SHOULD**`/`**WON'T**` (grep `\*\*MUST\*\*\|\*\*SHOULD\*\*\|\*\*WON'T\*\*` находит ≥1)
  </acceptance_criteria>
  <done>Файл создан, отражает реальное членство go.work, inventory помечен WIP вне workspace, gateway/outgate отсутствуют, internal/* не зафиксирован как структура, команды делегированы в build.md.</done>
</task>

<task type="auto">
  <name>Task 2: Создать knowledge/build.md — реально работающие команды сборки/тестов</name>
  <files>knowledge/build.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт; команды на уровне рецептов, не хрупких длинных листингов; no-phantom (не документировать то, что не запускается)
    - knowledge/README.md — формат/язык
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — раздел «Claude's Discretion → build.md» (реально работающие команды; GOWORK=off для inventory с WIP-пометкой; npx nx как инфра-справка; без phantom-команд)
    - .planning/research/PITFALLS.md — Pitfall 8 (phantom rules), Pitfall 2 (контекст-бюджет)
    - knowledge/structure.md (создаётся в Task 1) — раскладка модулей, на которую ссылается build.md
    - Реальный репо: `go.work` (члены pkg/analytics/audit); `pkg/http/*_test.go` (тесты есть, `go test` зелёный); `services/audit/cmd/main.go` (собирается); `services/analytics` — только go.mod (Go-пакетов ещё нет); `services/inventory` (GOWORK=off, WIP); `web/` (Nx: nx.json, package.json)
  </read_first>
  <action>
    Создай `knowledge/build.md` на русском по стандарту authoring.md. Документируй ТОЛЬКО реально
    запускаемые команды (no-phantom). На уровне рецептов, не хрупких длинных листингов.
    Содержимое:
    - Предусловие: Go 1.24.6, активный `go.work` в корне (членов перечисляет `structure.md` —
      относительная ссылка, не копировать список).
    - Тесты общих пакетов: `cd pkg && go test ./...` — реально проходит (есть `pkg/http`).
      Это рабочий рецепт «как прогнать тесты pkg».
    - Сборка/проверка модулей workspace: для `audit` — сборка из его модуля (есть `cmd/main.go`).
      Для `analytics` явно отметить **SHOULD**: модуль пока без Go-пакетов (`./...` ничего не
      находит) — это не ошибка; команда `go build ./...` в нём матчит ноль пакетов. Не выдавать
      analytics за «собранный сервис с кодом» — WON'T (phantom), вместо этого пометка «модуль-заготовка».
    - inventory: команды помечены **WIP** и выполняются с `GOWORK=off` (модуль вне workspace).
      Явно: inventory сейчас НЕ гарантированно компилируется — команды для него документируются как
      WIP, НЕ как «проверенно рабочие» (per Discretion + Pitfall 8). Дать рецепт `cd services/inventory && GOWORK=off go build ./...`
      с пометкой WIP и ссылкой на `boundaries.md`/`structure.md` (почему inventory вне workspace).
    - Фронтенд `web/`: инфра-справка — команды Nx через `npx nx` (например запуск задач Nx внутри
      `web/`), помечено как отдельная фронтенд-часть. Не углубляться в конкретные таргеты, если они
      не проверены — рецепт-уровень.
    - Pointer-over-copy: раскладка модулей — канон в `structure.md` (ссылка), здесь канон только
      по командам.
    Размер: ~< 120 строк. Команды давать инлайн-кодом в прозе/списках; НЕ городить длинные
    многострочные фенсед-листинги «на все случаи».
  </action>
  <verify>
    <automated>test -f knowledge/build.md && grep -q 'GOWORK=off' knowledge/build.md && grep -q 'go test' knowledge/build.md && grep -q 'cd pkg' knowledge/build.md && grep -q 'nx' knowledge/build.md && grep -q 'WIP' knowledge/build.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/build.md` существует
    - содержит `cd pkg` и `go test` (рецепт тестов pkg)
    - содержит `GOWORK=off` (рецепт inventory)
    - помечает inventory как `WIP`
    - содержит `nx` (фронтенд-справка)
    - ссылается на `structure.md` (содержит `structure.md`)
    - каждое нормативное правило несёт тег `**MUST**`/`**SHOULD**`/`**WON'T**` (grep находит ≥1)
    - НЕ документирует команды для gateway/outgate (grep `gateway` / `outgate` не находит)
  </acceptance_criteria>
  <done>Файл создан, все задокументированные команды соответствуют реальному репо (pkg-тест зелёный, audit собирается, analytics — заготовка, inventory — WIP под GOWORK=off), фронтенд-команды через npx nx, без phantom-команд.</done>
</task>

<task type="auto">
  <name>Task 3: Обновить knowledge/README.md — снять «запланировано» и проставить ссылки на structure.md и build.md</name>
  <files>knowledge/README.md</files>
  <read_first>
    - knowledge/README.md — текущий индекс (строки таблицы для structure.md, build.md помечены «запланировано (Phase 2)»; строка glossary.md помечена «запланировано (Phase 2)»); блок «Порядок чтения»; правило «ссылка появляется вместе с файлом»
    - knowledge/authoring.md — правило: ссылки только на реально существующие файлы; будущие доки — без ссылок, со статусом «запланировано»
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — D-01 (glossary DOC-07 отложен в v2/domain-milestone — НЕ Phase 2)
  </read_first>
  <action>
    Отредактируй `knowledge/README.md`:
    1. В таблице «Индекс» для строк `structure.md` и `build.md`: преврати имя файла в рабочую
       markdown-ссылку (`[structure.md](structure.md)`, `[build.md](build.md)`) и смени статус с
       «запланировано (Phase 2)» на «существует». Содержимое колонок «Назначение»/«Когда читать»
       оставить (или уточнить под фактический контент).
    2. Исправь строку `glossary.md`: сейчас она помечена «запланировано (Phase 2)», но per D-01
       glossary (DOC-07) ОТЛОЖЕН в v2/domain-milestone — поменяй статус на «отложено (v2 / domain-milestone)»
       и убери её из шага про Phase 2 в «Порядок чтения». Это исправление расхождения индекса с
       решением сессии (glossary НЕ создаётся в Phase 2).
    3. В блоке «Порядок чтения»: у пунктов про `structure.md`/`build.md` убрать маркер «появится
       в Phase 2 / *(Phase 2)*», т.к. они теперь существуют. Пункт про glossary убрать/переписать
       как «отложен в domain-milestone».
    Не трогать строки git.md/boundaries.md (их обновят планы 02-02 и 02-03) — они остаются
    «запланировано (Phase 2)» без ссылок. Сохранить правило «ссылки только на существующие файлы».
  </action>
  <verify>
    <automated>grep -Eq '\[structure\.md\]\(structure\.md\)' knowledge/README.md && grep -Eq '\[build\.md\]\(build\.md\)' knowledge/README.md && ! grep -q 'structure.md.*запланировано' knowledge/README.md && grep -Eq 'glossary\.md.*(отложено|v2|domain)' knowledge/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/README.md` содержит рабочую ссылку `[structure.md](structure.md)`
    - содержит рабочую ссылку `[build.md](build.md)`
    - строка structure.md больше НЕ содержит «запланировано»
    - строка build.md больше НЕ содержит «запланировано»
    - строка glossary.md помечена как отложенная (содержит один из: `отложено`, `v2`, `domain`)
    - строки git.md и boundaries.md остаются со статусом «запланировано» без ссылок (grep `git.md.*запланировано` и `boundaries.md.*запланировано` находят)
    - все relative-ссылки в README ведут на существующие файлы: `for f in $(grep -oE '\]\([a-z-]+\.md\)' knowledge/README.md | sed -E 's/\]\(|\)//g'); do test -f knowledge/$f || exit 1; done`
  </acceptance_criteria>
  <done>Индекс обновлён: structure.md и build.md — со ссылками и статусом «существует»; glossary исправлен на «отложено»; git.md/boundaries.md остаются запланированными; битых ссылок нет.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Фаза производит только markdown-документацию в `knowledge/`. Исполняемого кода, обработки
ввода и сетевой поверхности не вводится — новых trust boundaries нет.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | knowledge/*.md контент | mitigate | НЕ документировать секреты, внутренние хостнеймы, креды в базе знаний; команды/раскладка — только публичные факты репо |
| T-02-02 | — (no executable surface) | вся фаза | accept | Фаза не вводит исполняемой поверхности/ввода → high-severity угроз нет (phantom-угрозы не выдумывать) |
</threat_model>

<verification>
- `test -f knowledge/structure.md && test -f knowledge/build.md`
- structure.md отражает реальное членство `go.work` (pkg/analytics/audit) и WIP-статус inventory; gateway/outgate отсутствуют
- build.md документирует только реально запускаемые команды (pkg-тест, audit-сборка, inventory под GOWORK=off с WIP, npx nx)
- README.md ссылается на оба новых дока, статус «запланировано» снят, glossary помечен отложенным
- Все relative-ссылки в изменённых доках ведут на существующие файлы (кроме упоминания boundaries.md, которое финально проверяется в плане 02-03)
</verification>

<success_criteria>
- DOC-01 закрыт: `knowledge/structure.md` описывает раскладку go.work и модули в/вне workspace на уровне возможностей, без хрупкой path-карты
- DOC-02 закрыт: `knowledge/build.md` даёт реальные команды сборки/запуска/тестов, включая GOWORK=off для inventory, cd pkg && go test, фронтенд npx nx
- Индекс `knowledge/README.md` синхронизирован (ссылки + статусы), glossary исправлен на отложенный
- Все нормативные правила тегированы MUST/SHOULD/WON'T; запреты парны с «do»
</success_criteria>

<output>
Create `.planning/phases/02-foundation-docs/02-01-SUMMARY.md` when done
</output>
