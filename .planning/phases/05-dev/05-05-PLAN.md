---
phase: 05-dev
plan: 05
type: execute
wave: 2
depends_on: ["05-01", "05-04"]
files_modified:
  - lefthook.yml
  - knowledge/build.md
  - knowledge/structure.md
  - knowledge/boundaries.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [DOC-02, SVC-05]
must_haves:
  truths:
    - "lefthook pre-push гоняет inventory unit-тесты (go test ./...), без -tags=integration (D-02/D-15)"
    - "Из lefthook/каноны убран GOWORK=off и намеренное исключение inventory (D-01/D-02)"
    - "build.md audit-рецепт заменён на команду с реальным exit 0 (DOC-02 — go vet ./...)"
    - "Каноны build.md/structure.md/boundaries.md описывают inventory как члена go.work, не WIP-вне-workspace (D-04)"
    - "ROADMAP Phase 5 SC1 не содержит формулировку «с GOWORK=off» (D-04)"
  artifacts:
    - path: "lefthook.yml"
      provides: "pre-push с test-inventory (unit only); pre-commit без GOWORK=off для inventory"
      contains: "test-inventory"
    - path: "knowledge/build.md"
      provides: "audit-рецепт go vet ./... (exit 0); inventory как член workspace; pre-push включает inventory"
      contains: "go vet ./..."
    - path: "knowledge/structure.md"
      provides: "inventory — полноправный член go.work (четыре модуля)"
      contains: "inventory"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 5 SC1 без «с GOWORK=off»"
  key_links:
    - from: "lefthook.yml pre-push"
      to: "cd services/inventory && go test ./..."
      via: "test-inventory команда (unit only, D-02)"
      pattern: "services/inventory && go test"
    - from: "knowledge/build.md"
      to: "go vet ./..."
      via: "audit-рецепт DOC-02"
      pattern: "go vet"
---

<objective>
Привести enforcement-конфиг и каноны к решениям D-01/D-02/D-04 и закрыть DOC-02: снять `GOWORK=off`/исключение inventory из `lefthook.yml` (pre-push теперь гоняет inventory **unit-only**, D-15), переписать каноны `build.md`/`structure.md`/`boundaries.md` (inventory — член workspace) + ROADMAP SC1, и заменить падающий audit-рецепт на `go vet ./...` (эмпирически exit 0).

Purpose: SC5/DOC-02 (build.md audit-рецепт реально проходит, exit 0) + D-02 (pre-push включает inventory) + D-04 (каноны и ROADMAP под новое решение). Зависит от Plan 01 (inventory обязан компилироваться до включения в pre-push, инвариант D-03) и Plan 04 (`cmd/` теперь `package main` → рецепты не должны использовать `go build ./cmd`, Pitfall 2).
Output: обновлённые lefthook.yml, build.md, structure.md, boundaries.md, ROADMAP.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev/05-CONTEXT.md
@.planning/phases/05-dev/05-RESEARCH.md
@.planning/phases/05-dev/05-PATTERNS.md
@knowledge/authoring.md
@knowledge/build.md
@knowledge/structure.md
@knowledge/boundaries.md
@lefthook.yml
</context>

<artifacts_produced>
Этот план модифицирует (правка существующих файлов как код):
- `lefthook.yml` — pre-push `test-inventory` (unit only); pre-commit `lint-inventory` без `GOWORK=off` (слит в workspace-цикл); удалён комментарий-блок «inventory INTENTIONALLY NOT tested».
- `knowledge/build.md` — audit-рецепт `go vet ./...` (DOC-02); раздел inventory переписан под workspace-build; pre-push включает inventory.
- `knowledge/structure.md` — inventory = четвёртый член go.work; снят WIP-«вне workspace».
- `knowledge/boundaries.md` — убран факт «исключение inventory из pre-push»; inventory как пример WIP-лесов пересмотрен.
- `.planning/ROADMAP.md` — Phase 5 SC1 без «с GOWORK=off».
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: lefthook.yml — снять GOWORK=off + добавить inventory unit в pre-push</name>
  <files>lefthook.yml</files>
  <read_first>
    - lefthook.yml (целевой файл: pre-commit `lint-inventory` с `GOWORK=off` строки 33-39; pre-push без inventory + комментарий «INTENTIONALLY NOT tested» строки 44-57)
    - .planning/phases/05-dev/05-PATTERNS.md § lefthook.yml (строки 88-118: что менять — слить inventory lint в workspace-цикл, добавить test-inventory БЕЗ -tags=integration, удалить комментарий-блок)
    - .planning/phases/05-dev/05-RESEARCH.md § Runtime State Inventory (lefthook install переустановит хуки локально)
  </read_first>
  <action>В `lefthook.yml`: (1) pre-commit — убрать `GOWORK=off` из `lint-inventory`: либо добавить `services/inventory` в цикл `lint-workspace` (строки 29: `for m in pkg services/analytics services/audit`→ + `services/inventory`) и удалить отдельный `lint-inventory`, либо оставить отдельную команду без `GOWORK=off`. (2) pre-push — добавить команду `test-inventory: run: cd services/inventory && go test ./...` — БЕЗ `-tags=integration` (D-15: pre-push гоняет ТОЛЬКО unit, без Docker; testcontainers за тегом). (3) Удалить комментарий-блок «services/inventory is INTENTIONALLY NOT tested… Do not add an inventory test command» (строки ~44-47) — он обосновывал отменённое исключение (D-02). Обновить комментарии про GOWORK=off/«вне go.work» (строки 16-19, 35) под D-01. ПРИМЕЧАНИЕ для SUMMARY: применение хуков требует разового локального `lefthook install` (state-инвентаризация RESEARCH).</action>
  <verify>
    <automated>grep -q 'cd services/inventory && go test ./...' lefthook.yml && ! grep -q 'GOWORK=off' lefthook.yml && ! grep -qi 'INTENTIONALLY NOT tested' lefthook.yml && ! grep -q 'tags=integration' lefthook.yml && echo OK</automated>
  </verify>
  <done>pre-push гоняет inventory unit (без integration-тега); GOWORK=off и комментарий-исключение удалены (D-01/D-02/D-15).</done>
</task>

<task type="auto">
  <name>Task 2: DOC-02 — audit-рецепт build.md на go vet (exit 0), эмпирически верифицировать</name>
  <files>knowledge/build.md</files>
  <read_first>
    - knowledge/build.md (целевой: § audit-сборка строки 61-66 `cd services/audit && go build ./...`; § предусловие/bootstrap про pre-push исключение строки 42-46; § inventory GOWORK=off строки 68-81)
    - .planning/phases/05-dev/05-RESEARCH.md § DOC-02 (эмпирически: `go build ./...`/`go build ./cmd` падают `build output "cmd" already exists`; `go vet ./...` exit 0) + § Common Pitfalls / Pitfall 2
    - .planning/phases/05-dev/05-PATTERNS.md § Canon docs (строки 293-311: таблица правок D-04)
    - knowledge/authoring.md (MUST/SHOULD/WON'T, парность запрет→do, pointer-over-copy)
  </read_first>
  <action>В `knowledge/build.md`: ЭМПИРИЧЕСКИ проверить рецепт `cd services/audit && go vet ./...` → `echo $?` должно быть `0` (SC5 требует реального exit 0; RESEARCH подтвердил, но executor верифицирует на месте). Заменить падающий `cd services/audit && go build ./...` на `go vet ./...` (каноническая валидация без бинаря; НЕ `go build ./cmd` — падает тем же сообщением, Pitfall 2). Переписать раздел «inventory — WIP, GOWORK=off» (строки 68-81): inventory теперь полноправный член go.work (D-01), собирается общим `go build ./...`/`go vet ./...` из корня workspace БЕЗ `GOWORK=off`; убрать WIP-формулировку «не гарантированно компилируется» (D-03: инвариант «inventory всегда компилируется»). Обновить § про pre-push (строки 44-46): pre-push теперь ВКЛЮЧАЕТ inventory unit (убрать «исключён намеренно»). Соблюсти стиль authoring.md (MUST/SHOULD/WON'T, парность, не дублировать команды из structure.md — ссылка).</action>
  <verify>
    <automated>cd services/audit && go vet ./...; test $? -eq 0 && cd ../.. && grep -q 'go vet' knowledge/build.md && ! grep -q 'GOWORK=off' knowledge/build.md && echo OK</automated>
  </verify>
  <done>build.md audit-рецепт = `go vet ./...` (эмпирически exit 0, SC5/DOC-02); раздел inventory переписан под workspace-build; GOWORK=off удалён; pre-push описан как включающий inventory.</done>
</task>

<task type="auto">
  <name>Task 3: Каноны structure.md/boundaries.md + ROADMAP SC1 под D-01/D-02/D-04</name>
  <files>knowledge/structure.md, knowledge/boundaries.md, .planning/ROADMAP.md</files>
  <read_first>
    - knowledge/structure.md (целевой: § workspace «ровно три» строки 19-24; § «inventory — вне workspace, WIP» строки 36-48)
    - knowledge/boundaries.md (целевой: § «Не трогать WIP-леса» строки 12-21 с примером inventory; карта владения / pre-push исключение строки 63-72)
    - .planning/ROADMAP.md § Phase 5 (строка 93: SC1 «с GOWORK=off» — убрать)
    - .planning/phases/05-dev/05-PATTERNS.md § Canon docs (строки 293-311: точные строки/разделы под правку D-04)
    - knowledge/authoring.md (стиль; pointer-over-copy; один факт = один канон)
  </read_first>
  <action>`knowledge/structure.md`: § workspace «Активные модули — ровно три» → четыре (+ `services/inventory`); переписать § «inventory — вне workspace, WIP» (строки 36-48) — inventory полноправный член go.work (D-01), собирается/тестируется со всеми; снять «намеренно НЕ входит», «GOWORK=off», WIP-«не гарантированно компилируется» (D-03 инвариант). `knowledge/boundaries.md`: в § «Не трогать WIP-леса» (строки 18-21) убрать `services/inventory` как пример WIP-лесов вне go.work (теперь эталонный компилируемый член); в карте владения (строка 72) убрать факт «исключение inventory из pre-push»; обновить строку 63 (WIP-статус inventory) под новый статус. `.planning/ROADMAP.md` § Phase 5 SC1 (строка 93): убрать «с `GOWORK=off`» — оставить «go build/go vet зелёные» (D-04). Соблюсти authoring.md (парность запрет→do; не дублировать команды — ссылки на build.md). Не плодить phantom: описывать только реальное состояние (inventory реально в go.work — verified).</action>
  <verify>
    <automated>grep -qi 'inventory' knowledge/structure.md && ! grep -q 'GOWORK=off' knowledge/structure.md && ! grep -q 'GOWORK=off' knowledge/boundaries.md && ! grep -q 'GOWORK=off' .planning/ROADMAP.md && echo OK</automated>
  </verify>
  <done>structure.md: inventory = четвёртый член workspace; boundaries.md: убран факт исключения inventory из pre-push и пример WIP-лесов; ROADMAP SC1 без «GOWORK=off» (D-04).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git pre-push hook → CI-инвариант | hook гейтит push; inventory теперь в гейте (D-02/D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Tampering | pre-push гейт (inventory unit) | mitigate | inventory обязан компилироваться (D-03 инвариант); integration за build-tag (D-15) → pre-push не тянет Docker; зависимость плана от Plan 01 (atomic build green) гарантирует зелёный гейт |
| T-05-12 | Repudiation | канон-доки vs реальное состояние (drift) | mitigate | DOC-02 рецепт верифицируется эмпирически (exit 0); каноны приводятся к verified go.work-состоянию (no-phantom, boundaries.md) |
</threat_model>

<verification>
- `cd services/audit && go vet ./...; echo $?` == 0 (SC5/DOC-02).
- `grep 'cd services/inventory && go test' lefthook.yml` — pre-push включает inventory unit (D-02).
- `! grep GOWORK=off` в lefthook.yml / build.md / structure.md / boundaries.md / ROADMAP.md (D-01/D-04).
- ROADMAP Phase 5 SC1 без «с GOWORK=off».
- Полный pre-push smoke (после Plan 01-04): `cd services/inventory && go test ./...` зелёный без Docker.
</verification>

<success_criteria>
- build.md audit-рецепт реально проходит exit 0 (SC5/DOC-02).
- pre-push включает inventory unit-тесты, GOWORK=off снят везде (D-01/D-02).
- Каноны build.md/structure.md/boundaries.md + ROADMAP SC1 описывают inventory как члена workspace (D-04).
</success_criteria>

<output>
Create `.planning/phases/05-dev/05-05-SUMMARY.md` when done
</output>
