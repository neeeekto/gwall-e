---
phase: 05-dev
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - Makefile
autonomous: false
requirements: [SVC-07, SVC-06]
user_setup: []
must_haves:
  truths:
    - "docker compose up поднимает Kafka (confluent-local, KRaft) + Mongo (mongo:7 single-node RS) — транзакции доступны"
    - "Mongo RS инициализируется идемпотентно через healthcheck (rs.status||rs.initiate)"
    - "Makefile несёт пиннутую MOCKERY_VERSION и dev-таргеты dev-up/topics/test-integration/generate-mocks"
    - "Образы запиннены конкретными тегами (confluent-local:7.5.0, mongo:7), не :latest"
  artifacts:
    - path: "docker-compose.yml"
      provides: "confluent-local + mongo:7 RS healthcheck (ручной дев-стенд, D-05)"
      contains: "confluentinc/confluent-local"
    - path: "Makefile"
      provides: "MOCKERY_VERSION pin + таргеты dev-up/topics/test-integration/generate-mocks"
      contains: "MOCKERY_VERSION"
  key_links:
    - from: "docker-compose.yml mongo healthcheck"
      to: "rs.initiate"
      via: "idempotent try{rs.status()}catch{rs.initiate()}"
      pattern: "rs.initiate"
    - from: "Makefile generate-mocks target"
      to: "mockery"
      via: "go install .../mockery/v3@$(MOCKERY_VERSION) pattern"
      pattern: "MOCKERY_VERSION"
---

<objective>
Создать ручной дев-стенд `docker-compose.yml` (Kafka `confluent-local` KRaft + Mongo `mongo:7` single-node RS с идемпотентным `rs.initiate`-healthcheck, D-05/D-07/D-08) и расширить корневой `Makefile` пиннингом mockery + dev-таргетами (`dev-up`/`topics`/`test-integration`/`generate-mocks`, D-09/D-15/SVC-06).

Purpose: SC2 (`docker compose up` поднимает стенд, транзакции доступны) + инфра-обвязка для SC3/SC4 (make-таргеты запуска bootstrap-CLI и интеграционных тестов). SC2 — единственная manual-only верификация (требует Docker engine + compose-плагин), поэтому план `autonomous: false`.
Output: `docker-compose.yml`, обновлённый `Makefile`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev/05-CONTEXT.md
@.planning/phases/05-dev/05-RESEARCH.md
@.planning/phases/05-dev/05-PATTERNS.md
@.planning/phases/05-dev/05-VALIDATION.md
@Makefile
</context>

<artifacts_produced>
Этот план создаёт:
- `docker-compose.yml` (корень) — сервисы `kafka` (confluentinc/confluent-local:7.5.0, :9092) и `mongo` (mongo:7, --replSet rs0, :27017, idempotent RS healthcheck).
- Makefile-переменную `MOCKERY_VERSION := v3.7.1` + установку в `tools`-таргет.
- Makefile-таргеты: `dev-up`, `topics`, `test-integration`, `generate-mocks`.
- Клиентский URI дев-стенда: `mongodb://localhost:27017/?replicaSet=rs0&directConnection=true`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: docker-compose.yml — confluent-local + mongo:7 single-node RS</name>
  <files>docker-compose.yml</files>
  <read_first>
    - .planning/phases/05-dev/05-RESEARCH.md § Code Examples / docker-compose (строки 531-553: confluent-local + mongo:7 RS healthcheck) + § Common Pitfalls / Pitfall 1 (directConnection, member-host 127.0.0.1) + Assumptions A1/A2 (KAFKA_* env executor верифицирует)
    - .planning/phases/05-dev/05-PATTERNS.md § docker-compose.yml (строки 274-290: целевой паттерн, конкретные теги не :latest)
  </read_first>
  <action>Создать корневой `docker-compose.yml` (ручной дев-стенд, D-05). Сервис `kafka`: образ `confluentinc/confluent-local:7.5.0` (KRaft из коробки, 1 broker, D-07), порт `9092:9092`. confluent-local авто-конфигурит KRaft; если при `docker compose up` Kafka не advertise'ит на localhost:9092, добавить `KAFKA_ADVERTISED_LISTENERS` эмпирически (RESEARCH A2 — executor верифицирует, SC2 требует реального подъёма). Сервис `mongo`: образ `mongo:7` (D-08), `command: ["--replSet","rs0","--bind_ip_all","--port","27017"]`, порт `27017:27017`. Healthcheck идемпотентный: `mongosh --port 27017 --quiet --eval` со скриптом `try { rs.status() } catch (e) { rs.initiate({_id:'rs0',members:[{_id:0,host:'127.0.0.1:27017'}]}) }` (Pitfall 1 — member-host `127.0.0.1`), `interval: 5s`, `timeout: 30s`, `retries: 30`. Образы — конкретные теги, НЕ `:latest` (security V14 / D-07/D-08). В комментарии зафиксировать клиентский URI стенда `mongodb://localhost:27017/?replicaSet=rs0&directConnection=true`.</action>
  <verify>
    <automated>grep -q 'confluentinc/confluent-local:7.5.0' docker-compose.yml && grep -q 'mongo:7' docker-compose.yml && grep -q 'rs.initiate' docker-compose.yml && ! grep -q ':latest' docker-compose.yml && echo OK</automated>
  </verify>
  <done>docker-compose.yml существует с confluent-local:7.5.0 + mongo:7 RS + идемпотентным healthcheck; образы запиннены тегами.</done>
</task>

<task type="auto">
  <name>Task 2: Makefile — MOCKERY_VERSION pin + dev-таргеты</name>
  <files>Makefile</files>
  <read_first>
    - Makefile (целевой файл — паттерн `*_VERSION` + `go install ...@$(VERSION)` в `tools`-таргете, строки 8-17; комментарии на английском)
    - .planning/phases/05-dev/05-PATTERNS.md § Makefile (строки 121-148: паттерн пиннинга, НЕ корневой go.mod tool-блок, английские комментарии)
    - .planning/phases/05-dev/05-RESEARCH.md § Open Question 3 (docker compose v2 plugin) + § Validation Architecture (test-integration = go test -tags=integration)
    - .planning/phases/05-dev/05-PATTERNS.md § DOC-02 / package-main build-коллизия (НЕ `go build ./cmd`)
  </read_first>
  <action>Добавить в корневой `Makefile` по существующему паттерну (комментарии на английском): `MOCKERY_VERSION := v3.7.1`; в `tools`-таргете строку `go install github.com/vektra/mockery/v3@$(MOCKERY_VERSION)`. НЕ добавлять `tool`-блок в корневой go.mod (rotten leftover, go 1.23.6 вне go.work). Добавить `.PHONY`-таргеты: `dev-up` → `docker compose up -d` (v2-плагин); `topics` → запуск bootstrap-CLI inventory (`cd services/inventory && go run ./cmd ...` с env `KAFKA_BROKERS=localhost:9092` — конкретный путь к CLI согласуется с Plan 04, который создаёт CLI; здесь таргет ссылается на него); `test-integration` → `cd services/inventory && go test -tags=integration ./...` (D-15); `generate-mocks` → `mockery` (бинарь из tools). Использовать `docker compose` (не `docker-compose`). НЕ использовать `go build ./cmd` нигде (Pitfall 2 — падает `build output "cmd" already exists`).</action>
  <verify>
    <automated>grep -q 'MOCKERY_VERSION := v3.7.1' Makefile && grep -q 'mockery/v3@$(MOCKERY_VERSION)' Makefile && grep -q 'test-integration' Makefile && grep -q 'generate-mocks' Makefile && grep -q 'dev-up' Makefile && ! grep -q 'go build ./cmd' Makefile && echo OK</automated>
  </verify>
  <done>Makefile несёт MOCKERY_VERSION pin + таргеты dev-up/topics/test-integration/generate-mocks; нет go build ./cmd; нет tool-блока в go.mod.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Ручной smoke дев-стенда (SC2 — Docker)</name>
  <action>Manual-only верификация SC2 (требует Docker engine + compose-плагин, отсутствует в sandbox; см. 05-VALIDATION.md § Manual-Only Verifications). Автоматизировать нечего — стенд уже создан в Task 1-2; этот шаг — человеческая проверка реального подъёма по how-to-verify ниже. Если confluent-local не advertise'ит на :9092, вернуться к Task 1 и добавить KAFKA_ADVERTISED_LISTENERS.</action>
  <what-built>docker-compose.yml (confluent-local KRaft + mongo:7 single-node RS с идемпотентным rs.initiate-healthcheck) + Makefile-таргет dev-up.</what-built>
  <how-to-verify>
    1. Из корня репозитория: `make dev-up` (или `docker compose up -d`).
    2. Дождаться healthy Mongo (~30 с): `docker compose ps` — статус mongo `healthy`.
    3. Проверить RS инициализирован: `mongosh "mongodb://localhost:27017/?directConnection=true" --quiet --eval 'rs.status().ok'` → должно вернуть `1`.
    4. Проверить Kafka доступен: брокер слушает на `localhost:9092` (`docker compose logs kafka` без фатальных ошибок; порт открыт).
    5. Останов: `docker compose down`.
  </how-to-verify>
  <resume-signal>Напишите "approved" если стенд поднялся и rs.status().ok==1, иначе опишите ошибку (напр. нужен KAFKA_ADVERTISED_LISTENERS).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docker registry → compose images | образы тянутся из registry; supply-chain |
| dev-стенд (plaintext Kafka/Mongo, без auth) | НЕ для prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | docker-образы (confluent-local, mongo) | mitigate | Конкретные теги `confluentinc/confluent-local:7.5.0`, `mongo:7` (НЕ `:latest`) — детерминизм; verify-гейт grep `! :latest` |
| T-05-04 | Information Disclosure | plaintext Kafka/Mongo дев-стенд (без SASL/TLS/auth) | accept | Дев-стенд явно dev-only (комментарий в compose); prod-auth/TLS — вне Phase 5 |
| T-05-05 | Tampering | mockery install (go install в Makefile) | mitigate | Пиннинг `MOCKERY_VERSION := v3.7.1` (verified go-proxy, RESEARCH § Package Legitimacy Audit, Approved); НЕ `@latest`; НЕ rotten go.mod tool-блок |
</threat_model>

<verification>
- `grep confluentinc/confluent-local:7.5.0 docker-compose.yml` + `grep mongo:7` + `grep rs.initiate` — присутствуют; нет `:latest`.
- `grep MOCKERY_VERSION Makefile`, таргеты dev-up/topics/test-integration/generate-mocks присутствуют.
- Manual (SC2, Task 3): `make dev-up` → `rs.status().ok==1`; Kafka на :9092.
</verification>

<success_criteria>
- `docker compose up` поднимает Kafka KRaft + Mongo single-node RS, транзакции доступны (SC2).
- Bootstrap-топики провижнятся через `make topics`-путь (инфра готова; CLI — Plan 04).
- Makefile несёт пиннутый mockery + dev/test-таргеты (SVC-06/D-15).
</success_criteria>

<output>
Create `.planning/phases/05-dev/05-02-SUMMARY.md` when done
</output>
