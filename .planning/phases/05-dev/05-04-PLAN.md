---
phase: 05-dev
plan: 04
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - services/inventory/cmd/main.go
  - services/inventory/internal/kafka/topology/topology_integration_test.go
autonomous: true
requirements: [SVC-07, SVC-06]
must_haves:
  truths:
    - "Bootstrap-CLI в cmd/ парсит KAFKA_BROKERS+partitions и зовёт topology.Bootstrap (НЕ дублирует топологию, D-09)"
    - "Интеграционный тест за build-tag integration поднимает Kafka KRaft + Mongo RS через testcontainers"
    - "Тест зовёт ТУ ЖЕ topology.Bootstrap и mongoconn.Connect (single source, D-06) и ассертит провижн топиков"
    - "go test ./... (без тега) и pre-push НЕ компилируют integration-тест (D-15)"
  artifacts:
    - path: "services/inventory/cmd/main.go"
      provides: "тонкий bootstrap-CLI: env/flags → kgo.NewClient → kadm.NewClient → topology.Bootstrap"
      contains: "topology.Bootstrap"
    - path: "services/inventory/internal/kafka/topology/topology_integration_test.go"
      provides: "testcontainers smoke (kafka+mongodb) → Bootstrap + Connect, за build-tag integration"
      contains: "//go:build integration"
  key_links:
    - from: "services/inventory/cmd/main.go"
      to: "internal/kafka/topology.Bootstrap"
      via: "CLI зовёт общую функцию (D-09, не дублирует)"
      pattern: "topology\\.Bootstrap"
    - from: "services/inventory/internal/kafka/topology/topology_integration_test.go"
      to: "topology.Bootstrap + mongoconn.Connect"
      via: "testcontainers Brokers()/ConnectionString() → общие функции"
      pattern: "Bootstrap|Connect"
---

<objective>
Замкнуть single-source топологию (D-06): тонкий bootstrap-CLI в `cmd/` (D-09) и интеграционный smoke-тест на testcontainers (D-15) — ОБА зовут одни и те же `topology.Bootstrap` (из Plan 01) и `mongoconn.Connect`. Тест за build-tag `integration` поднимает Kafka KRaft + Mongo single-node RS, провижнит топики и подключается к Mongo.

Purpose: SC3 (Bootstrap провижнит `*.events`/`*.state` с нужной cleanup-policy — ассерт через DescribeTopicConfigs) + SC4 (testcontainers стартует и подключается). CLI даёт manual-путь провижна (`make topics` после `make dev-up`). Тест изолирован тегом, чтобы pre-push (теперь включающий inventory, D-02) не тянул Docker (Pitfall 3).
Output: `cmd/main.go` (CLI), `topology_integration_test.go`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev/05-CONTEXT.md
@.planning/phases/05-dev/05-RESEARCH.md
@.planning/phases/05-dev/05-PATTERNS.md
@.planning/phases/05-dev/05-VALIDATION.md
@services/inventory/internal/kafka/topology/topology.go
@services/inventory/internal/repository/mongoconn/conn.go
@knowledge/testing.md
@knowledge/style.md
</context>

<artifacts_produced>
Этот план создаёт:
- `cmd/main.go` (replace stub) — bootstrap-CLI: читает env `KAFKA_BROKERS` (+ partitions, дефолт 6) → `kgo.NewClient(kgo.SeedBrokers(...))` → `kadm.NewClient(cl)` → `topology.Bootstrap(ctx, adm, partitions)`.
- `internal/kafka/topology/topology_integration_test.go` (build-tag `integration`) — testcontainers kafka+mongodb smoke, зовёт `topology.Bootstrap` + `mongoconn.Connect`.
- Build-tag `integration` (запускается `make test-integration`).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap-CLI в cmd/ (зовёт topology.Bootstrap)</name>
  <files>services/inventory/cmd/main.go</files>
  <read_first>
    - services/inventory/cmd/main.go (текущий стаб `func main(){return}`)
    - services/inventory/internal/kafka/topology/topology.go (сигнатура `Bootstrap(ctx, adm *kadm.Client, partitions int32)` — из Plan 01)
    - .planning/phases/05-dev/05-RESEARCH.md § kadm bootstrap (kgo.NewClient/SeedBrokers → kadm.NewClient)
    - .planning/phases/05-dev/05-PATTERNS.md § bootstrap CLI (строки 170-186: тонкий CLI, НЕ дублировать топологию, Pitfall 2 — НЕ go build ./cmd)
    - knowledge/style.md (русские доменные комментарии; sentinel+`%w`)
  </read_first>
  <action>Заменить стаб `cmd/main.go` тонким bootstrap-CLI (D-09). Читать env `KAFKA_BROKERS` (CSV, дефолт `localhost:9092`) и `KAFKA_PARTITIONS` (дефолт `6`, D-11 дев-дефолт). Создать `kgo.NewClient(kgo.SeedBrokers(brokers...))`, обернуть в `kadm.NewClient(cl)`, вызвать `topology.Bootstrap(ctx, adm, int32(partitions))`. НЕ дублировать имена/политики топиков — вся топология живёт в topology-пакете (D-06). При ошибке — лог + ненулевой exit; ошибки оборачивать `%w`. context — `context.Background()` (one-shot CLI). Доменные комментарии — на русском. ВНИМАНИЕ: `cmd/` содержит `package main` → `go build ./cmd` / `go build ./...` падает `build output "cmd" already exists` (Pitfall 2); CLI запускается через `go run ./cmd` (make-таргет `topics`), валидация сборки — через `go vet ./...`.</action>
  <verify>
    <automated>cd services/inventory && go vet ./... && grep -q 'topology.Bootstrap' cmd/main.go && echo OK</automated>
  </verify>
  <done>cmd/main.go — тонкий CLI, зовёт topology.Bootstrap, не дублирует топологию; go vet зелёный (НЕ go build ./cmd).</done>
</task>

<task type="auto">
  <name>Task 2: Интеграционный smoke-тест (build-tag integration, testcontainers)</name>
  <files>services/inventory/internal/kafka/topology/topology_integration_test.go</files>
  <read_first>
    - .planning/phases/05-dev/05-RESEARCH.md § Code Examples / testcontainers smoke (строки 469-529: kafka.Run/Brokers, mongodb.Run/WithReplicaSet/ConnectionString, kgo.Ping, TerminateContainer) + § Common Pitfalls / Pitfall 1 (доверять ConnectionString, не хардкодить хост)
    - .planning/phases/05-dev/05-PATTERNS.md § integration_test (строки 206-234: //go:build integration первой строкой, suite-каркас, DeferCleanup)
    - services/inventory/internal/kafka/topology/topology.go + internal/repository/mongoconn/conn.go (вызываемые функции из Plan 01)
    - knowledge/testing.md (suite-каркас; английские комментарии в тестах)
  </read_first>
  <action>Создать `topology_integration_test.go` ПЕРВОЙ строкой `//go:build integration` (D-15 — `go test ./...` и pre-push НЕ компилируют → без Docker в pre-push, критично после D-02). Suite-каркас `RegisterFailHandler(Fail)` + `RunSpecs`; dot-import ginkgo/gomega; комментарии на английском. Spec 1 (Kafka): `kafka.Run(ctx, "confluentinc/confluent-local:7.5.0", kafka.WithClusterID("test-cluster"))`, `DeferCleanup(testcontainers.TerminateContainer)`, `kc.Brokers(ctx)` → `kgo.NewClient(kgo.SeedBrokers(...))` → `cl.Ping(ctx)` (smoke) → `kadm.NewClient(cl)` → `topology.Bootstrap(ctx, adm, 6)` (Expect Succeed) → ассертить провижн через `adm.ListTopics`/`DescribeTopicConfigs`: `inventory.host.events` cleanup.policy=delete, `inventory.host.state` cleanup.policy=compact, partitions=6 (SC3). Spec 2 (Mongo): `mongodb.Run(ctx, "mongo:7", mongodb.WithReplicaSet("rs0"))`, `mc.ConnectionString(ctx)` (ДОВЕРЯТЬ — не хардкодить хост, Pitfall 1) → `mongoconn.Connect(ctx, uri)` → Expect ToNot HaveOccurred → `DeferCleanup(client.Disconnect)`. Если test-deps testcontainers ещё не в go.mod — `go get`/`go mod tidy` подтянет при первом импорте.</action>
  <verify>
    <automated>cd services/inventory && grep -q '//go:build integration' internal/kafka/topology/topology_integration_test.go && go vet -tags=integration ./internal/kafka/topology/... && go test ./internal/kafka/topology/... && echo "unit-pass-no-docker"</automated>
  </verify>
  <done>Integration-тест за build-tag integration компилируется (`go vet -tags=integration`); `go test ./...` без тега НЕ запускает его (зелёный без Docker); тест зовёт общие topology.Bootstrap + mongoconn.Connect и ассертит провижн (запуск с Docker — `make test-integration`).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| testcontainers → Docker daemon | эфемерные контейнеры; dev/test-only |
| CLI env (KAFKA_BROKERS) → Kafka admin | dev-оператор, доверенный ввод |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-08 | Tampering | testcontainers-образы (confluent-local:7.5.0, mongo:7) | mitigate | Конкретные теги в `Run(ctx, img, ...)` (паритет с compose, D-07); образы verified |
| T-05-09 | Information Disclosure | plaintext эфемерный Kafka/Mongo в тесте | accept | Эфемерные контейнеры, Terminate после теста; нет секретов; dev/test-only |
| T-05-10 | Elevation of Privilege | CLI-флаги/env (KAFKA_BROKERS) | accept | Dev-only оператор (V5 не применима, RESEARCH § Security Domain); ввод доверенный |
</threat_model>

<verification>
- `grep '//go:build integration' .../topology_integration_test.go` — присутствует первой строкой.
- `cd services/inventory && go test ./...` — зелёный без Docker (integration не компилируется, D-15).
- `go vet -tags=integration ./internal/kafka/topology/...` — компилируется.
- `grep topology.Bootstrap cmd/main.go` — CLI зовёт общую функцию (D-06/D-09).
- С Docker: `make test-integration` → kgo.Ping + mongo Connect проходят, топики провижнятся (SC3/SC4).
</verification>

<success_criteria>
- Bootstrap провижнит `*.events`(delete)+`*.state`(compact), partitions=6 — ассерт в integration-тесте (SC3).
- testcontainers (Kafka KRaft + Mongo RS) стартует и подключается (SC4).
- CLI и тест зовут одну topology.Bootstrap — single source (D-06/D-09); integration изолирован тегом (D-15).
</success_criteria>

<output>
Create `.planning/phases/05-dev/05-04-SUMMARY.md` when done
</output>
