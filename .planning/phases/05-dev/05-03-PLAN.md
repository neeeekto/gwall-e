---
phase: 05-dev
plan: 03
type: execute
wave: 2
depends_on: ['05-02']
files_modified:
  - .mockery.yaml
  - services/inventory/internal/example/provisioner.go
  - services/inventory/internal/example/mocks/ExampleProvisioner.go
  - services/inventory/internal/example/provisioner_test.go
autonomous: true
requirements: [SVC-06]
must_haves:
  truths:
    - ".mockery.yaml (v3-конфиг) существует и нацелен на throwaway example-интерфейс"
    - "make generate-mocks генерирует мок для ExampleProvisioner без ошибок"
    - "Unit-spec с сгенерированным моком (NewMockExampleProvisioner(GinkgoT()) + EXPECT) зелёный"
  artifacts:
    - path: ".mockery.yaml"
      provides: "v3-конфиг (template testify, .SrcPackageName/.SrcPackagePath), нацелен на example-пакет"
      contains: "template: testify"
    - path: "services/inventory/internal/example/provisioner.go"
      provides: "throwaway ExampleProvisioner интерфейс (доменных портов ещё нет)"
      contains: "ExampleProvisioner"
    - path: "services/inventory/internal/example/provisioner_test.go"
      provides: "unit-spec с mockery-моком через Gomega (без build-tag, гоняется pre-push'ем)"
      min_lines: 15
  key_links:
    - from: "services/inventory/internal/example/provisioner_test.go"
      to: "NewMockExampleProvisioner"
      via: "GinkgoT() + EXPECT().Return"
      pattern: "NewMockExampleProvisioner"
    - from: ".mockery.yaml"
      to: "services/inventory/internal/example"
      via: "packages указывает на example-пакет"
      pattern: "internal/example"
---

<objective>
Завести mockery v3 smoke (SVC-06): `.mockery.yaml` (v3-конфиг) + один throwaway example-интерфейс (реальных доменных портов нет — они в Phase 6/7), сгенерировать мок и доказать его рабочим unit-спеком через Gomega.

Purpose: часть SC4 — Ginkgo v2 + Gomega + mockery подключены и проходят smoke-прогон. Доказывает работоспособность кодогена моков до появления реальных портов.
Output: `.mockery.yaml`, example-пакет с интерфейсом, сгенерированный мок, unit-spec.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev/05-CONTEXT.md
@.planning/phases/05-dev/05-RESEARCH.md
@.planning/phases/05-dev/05-PATTERNS.md
@knowledge/testing.md
@knowledge/style.md
</context>

<artifacts_produced>
Этот план создаёт:
- `.mockery.yaml` (корень) — v3-конфиг (template testify, formatter goimports, all:false), packages → example-пакет inventory.
- `internal/example/provisioner.go` — throwaway `ExampleProvisioner` интерфейс.
- `internal/example/mocks/ExampleProvisioner.go` — сгенерированный mockery-мок (`make generate-mocks`).
- `internal/example/provisioner_test.go` — unit-spec (Ginkgo suite + Gomega + testify/mock), без build-tag.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: .mockery.yaml (v3) + throwaway ExampleProvisioner интерфейс</name>
  <files>.mockery.yaml, services/inventory/internal/example/provisioner.go</files>
  <read_first>
    - .planning/phases/05-dev/05-RESEARCH.md § Code Examples / mockery v3 (.mockery.yaml, строки 555-570) + § Common Pitfalls / Pitfall 5 (v2 vs v3: .SrcPackageName НЕ .PackageName, моки рядом с кодом, 1 файл/пакет)
    - .planning/phases/05-dev/05-PATTERNS.md § .mockery.yaml (строки 252-261) + § throwaway example-интерфейс (строки 265-270)
    - knowledge/testing.md (мокинг через mockery; throwaway-иллюстрация OrderRepository — паттерн интерфейса)
    - knowledge/style.md (типизированные ID; sentinel-ошибки; русские доменные комментарии)
  </read_first>
  <action>Создать корневой `.mockery.yaml` (v3-синтаксис, Pitfall 5): `all: false`, `formatter: goimports`, `template: testify`, `dir: "{{.InterfaceDir}}/mocks"`, `filename: "{{.InterfaceName}}.go"`, `pkgname: "mocks"`. В `packages:` указать пакет `github.com/gwall-e/services/inventory/internal/example` с `interfaces: { ExampleProvisioner: {} }`. Использовать v3-ключи `.SrcPackageName`/`.SrcPackagePath` если нужны в шаблоне (НЕ v2 `.PackageName`/`.PackagePath`). Создать `internal/example/provisioner.go` (package example): объявить throwaway-интерфейс `ExampleProvisioner` с одним методом (напр. `Provision(ctx context.Context, name string) error`) — пометить комментарием на русском, что это пример-порт для smoke mockery, реальные доменные порты появятся в Phase 6/7. Если фигурирует ID — типизированный (`type ExampleID string`, style.md); ошибки — sentinel+`%w` где уместно.</action>
  <verify>
    <automated>grep -q 'template: testify' .mockery.yaml && grep -q 'internal/example' .mockery.yaml && cd services/inventory && grep -rq 'ExampleProvisioner' internal/example/provisioner.go && go build ./internal/example/... && echo OK</automated>
  </verify>
  <done>.mockery.yaml (v3) нацелен на example-пакет; ExampleProvisioner-интерфейс компилируется.</done>
</task>

<task type="auto">
  <name>Task 2: Сгенерировать мок + unit-spec через Gomega</name>
  <files>services/inventory/internal/example/mocks/ExampleProvisioner.go, services/inventory/internal/example/provisioner_test.go</files>
  <read_first>
    - knowledge/testing.md § Мокинг портов (строки 103-143: `NewMockX(GinkgoT())`, `EXPECT().Method().Return()`, `testify/mock` обычный импорт, ginkgo/gomega dot-import)
    - pkg/http/http_test.go (suite-каркас `RegisterFailHandler(Fail)` + `RunSpecs`)
    - pkg/http/middlewares_test.go (Describe/Context/It + Gomega-ассерты)
    - .planning/phases/05-dev/05-PATTERNS.md § unit-spec с моком (строки 237-248)
  </read_first>
  <action>Сгенерировать мок: `make generate-mocks` (вызывает `mockery`, бинарь из `make tools`) → создаёт `internal/example/mocks/ExampleProvisioner.go` (mockery v3, testify-template). Создать `internal/example/provisioner_test.go` (package example или example_test) — БЕЗ build-tag (unit, гоняется pre-push'ем после D-02): suite-каркас `RegisterFailHandler(Fail)` + `RunSpecs`; dot-import ginkgo/gomega; `import "github.com/stretchr/testify/mock"` обычным (НЕ dot) импортом. Spec: `m := NewMockExampleProvisioner(GinkgoT())` (авто-Cleanup сверит ожидания); `m.EXPECT().Provision(mock.Anything, mock.Anything).Return(nil)`; вызвать метод; результат проверить через Gomega `Expect(err).ToNot(HaveOccurred())`. Комментарии в тесте — на английском (style.md).</action>
  <verify>
    <automated>cd services/inventory && go test ./internal/example/... && go vet ./internal/example/...</automated>
  </verify>
  <done>make generate-mocks генерит мок; unit-spec с моком зелёный без Docker и без build-tag (часть SC4: mockery smoke).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mockery codegen → сгенерированный код | кодоген коммитится; источник — пиннутый mockery v3.7.1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | mockery бинарь (codegen) | mitigate | Бинарь пиннут `MOCKERY_VERSION := v3.7.1` в Makefile (Plan 02); verified go-proxy (RESEARCH § Package Legitimacy Audit, Approved) |
| T-05-07 | Tampering | testify/mock зависимость (втягивается тест-кодом) | accept | Зрелый stretchr/testify; уже идиома проекта (knowledge/testing.md); попадёт в go.sum с хешем |
</threat_model>

<verification>
- `grep 'template: testify' .mockery.yaml` + `grep internal/example .mockery.yaml` — присутствуют.
- `cd services/inventory && go test ./internal/example/...` — зелёный (mockery smoke, SC4-часть).
- Сгенерированный `internal/example/mocks/ExampleProvisioner.go` существует.
</verification>

<success_criteria>
- mockery v3 настроен (.mockery.yaml) и доказан smoke'ом: мок генерится, unit-spec зелёный (SVC-06 / SC4-часть).
</success_criteria>

<output>
Create `.planning/phases/05-dev/05-03-SUMMARY.md` when done
</output>
