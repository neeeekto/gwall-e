---
phase: 05-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/inventory/go.mod
  - services/inventory/go.sum
  - services/inventory/internal/repository/mongoconn/conn.go
  - services/inventory/internal/kafka/topology/topology.go
  - services/inventory/internal/kafka/topology/topology_test.go
autonomous: true
requirements: [SVC-05, SVC-07]
must_haves:
  truths:
    - "services/inventory собирается с mongo-driver/v2; v1.17.9 удалён из go.mod"
    - "go build ./... и go vet ./... из корня workspace зелёные (inventory — член go.work, D-01)"
    - "Существует RS-aware connection-helper (Connect+Ping), без repository/UoW/Outbox"
    - "Существует единый topology-пакет: data-driven константы агрегатов + Bootstrap(ctx, adm, partitions) на kadm"
    - "Unit-тест констант топологии (имена топиков + cleanup-политики) зелёный без Docker"
  artifacts:
    - path: "services/inventory/go.mod"
      provides: "mongo-driver/v2 v2.7.0, franz-go v1.21.4, kadm v1.18.0; нет mongo-driver v1.17.9"
      contains: "go.mongodb.org/mongo-driver/v2 v2.7.0"
    - path: "services/inventory/internal/repository/mongoconn/conn.go"
      provides: "Connect(ctx, uri) (*mongo.Client, error) — RS-aware фабрика + health-ping"
      min_lines: 20
    - path: "services/inventory/internal/kafka/topology/topology.go"
      provides: "константы агрегатов + Bootstrap на kadm (single source топологии)"
      contains: "func Bootstrap"
    - path: "services/inventory/internal/kafka/topology/topology_test.go"
      provides: "unit-тест констант/имён топиков и cleanup-политик (без Docker)"
      min_lines: 15
  key_links:
    - from: "services/inventory/internal/kafka/topology/topology.go"
      to: "github.com/twmb/franz-go/pkg/kadm"
      via: "adm.CreateTopics с config-map cleanup.policy/delete.retention.ms"
      pattern: "CreateTopics"
    - from: "services/inventory/internal/repository/mongoconn/conn.go"
      to: "go.mongodb.org/mongo-driver/v2/mongo"
      via: "mongo.Connect(options...) без ctx (v2)"
      pattern: "mongo\\.Connect"
---

<objective>
Заложить стек-фундамент inventory: атомарно мигрировать `go.mod` на `mongo-driver/v2` (+ franz-go/kadm), создать тонкий RS-aware Mongo connection-helper (D-14) и единый topology-пакет с `Bootstrap`-функцией на `kadm` (D-06) — единственный источник истины имён/политик/партиций топиков.

Purpose: SC1 (inventory собирается с v2, build/vet зелёные) и фундамент SC3 (Bootstrap провижнит топики). Inventory теперь член go.work (D-01) — свап делается атомарно, иначе ломается весь workspace-build (D-03/Pitfall 3).
Output: обновлённый go.mod/go.sum, `mongoconn`-helper, `topology`-пакет + unit-тест констант.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-dev/05-CONTEXT.md
@.planning/phases/05-dev/05-RESEARCH.md
@.planning/phases/05-dev/05-PATTERNS.md
@knowledge/style.md
@knowledge/architecture.md
</context>

<artifacts_produced>
Этот план создаёт следующие символы (downstream-фазы и Plan 04 ссылаются на них):
- Пакет `internal/kafka/topology`: `func Bootstrap(ctx context.Context, adm *kadm.Client, partitions int32) error`; константы/переменная `aggregates []string{"host"}`; суффиксы топиков `.events`/`.state`; политики `cleanup.policy=delete` / `cleanup.policy=compact` + `delete.retention.ms=86400000`.
- Пакет `internal/repository/mongoconn`: `func Connect(ctx context.Context, uri string) (*mongo.Client, error)`.
- go.mod require: `go.mongodb.org/mongo-driver/v2 v2.7.0`, `github.com/twmb/franz-go v1.21.4`, `github.com/twmb/franz-go/pkg/kadm v1.18.0`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Атомарный свап go.mod на mongo-driver/v2 + franz-go/kadm</name>
  <files>services/inventory/go.mod, services/inventory/go.sum</files>
  <read_first>
    - services/inventory/go.mod (целевой файл — текущий require-блок: `go.mongodb.org/mongo-driver v1.17.9` + indirect v1-deps)
    - .planning/phases/05-dev/05-PATTERNS.md § `services/inventory/go.mod` (строки 42-64: целевой require-блок, инвариант атомарности D-03)
    - .planning/phases/05-dev/05-RESEARCH.md § Standard Stack / Installation (точные версии + предупреждение D-13)
  </read_first>
  <action>Из каталога `services/inventory` (go.work активен, БЕЗ `GOWORK=off` — D-01): удалить `go.mongodb.org/mongo-driver v1.17.9` из `require`; добавить `go.mongodb.org/mongo-driver/v2@v2.7.0`, `github.com/twmb/franz-go@v1.21.4`, `github.com/twmb/franz-go/pkg/kadm@v1.18.0` через `go get`. Сохранить `github.com/google/uuid v1.6.0`. Прогнать `go mod tidy` — indirect v1-deps (`xdg-go/*`, `montanaflynn/stats`, `golang/snappy`, `youmark/pkcs8`) уйдут (импортов v1 нет, `internal/` пуст до создания файлов в Task 2-3, поэтому import-правок старого кода нет). НЕ трогать корневой go.mod (rotten leftover). Свап делать вместе с Task 2-3 так, чтобы итоговый `go build ./...` был зелёным в одном проходе (D-03/Pitfall 3 — WIP-ошибка компиляции ломает весь workspace).</action>
  <verify>
    <automated>cd services/inventory && grep -q 'mongo-driver/v2 v2.7.0' go.mod && ! grep -q 'mongo-driver v1.17.9' go.mod && echo OK</automated>
  </verify>
  <done>go.mod содержит mongo-driver/v2 v2.7.0 + franz-go v1.21.4 + kadm v1.18.0; нет mongo-driver v1.17.9; uuid сохранён; go.sum пересобран.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Topology-пакет — константы агрегатов + Bootstrap на kadm</name>
  <files>services/inventory/internal/kafka/topology/topology.go, services/inventory/internal/kafka/topology/topology_test.go</files>
  <read_first>
    - .planning/phases/05-dev/05-RESEARCH.md § Code Examples / kadm bootstrap (строки 405-438: verbatim-сигнатура `kadm.CreateTopics(ctx, partitions int32, rf int16, configs map[string]*string, topics...)`, `kadm.StringPtr`)
    - .planning/phases/05-dev/05-PATTERNS.md § topology (строки 151-167: инварианты D-06/D-10/D-11/D-12, single-source, data-driven)
    - knowledge/style.md (доменные комментарии на русском; идентификаторы английские; sentinel + `%w`-wrap для ошибок — errorlint-hook)
    - pkg/http/http_test.go (suite-каркас `RegisterFailHandler(Fail)` + `RunSpecs`)
  </read_first>
  <behavior>
    - Test: для агрегата "host" функция деривации имён даёт ровно `inventory.host.events` и `inventory.host.state`.
    - Test: cleanup-конфиг `*.events` содержит `cleanup.policy=delete` и НЕ содержит `compact`.
    - Test: cleanup-конфиг `*.state` содержит `cleanup.policy=compact` и `delete.retention.ms=86400000` (24h, ≥24h по D-12).
    - Test: список агрегатов содержит ровно `host` в Phase 5 (data-driven, D-10).
  </behavior>
  <action>Создать пакет `internal/kafka/topology`. Объявить data-driven `var aggregates = []string{"host"}` (D-10 — добавление агрегата = одна строка). Объявить чистые helper-функции деривации имён топиков (суффиксы `.events`/`.state`, префикс `inventory.`) и cleanup-конфигов как `map[string]*string` через `kadm.StringPtr`: `*.events` → `cleanup.policy=delete`; `*.state` → `cleanup.policy=compact` + `delete.retention.ms=86400000` (D-12). Экспортировать `func Bootstrap(ctx context.Context, adm *kadm.Client, partitions int32) error`: для каждого агрегата вызвать `adm.CreateTopics(ctx, partitions, int16(1), eventsCfg, "inventory."+agg+".events")` и аналогично для `.state`; rf = `int16(1)` (single broker, D-07); partitions — параметр (дев-дефолт 6 задаёт вызывающий, D-11). Ошибки оборачивать `fmt.Errorf("создать топик %s.events: %w", agg, err)`. Этот пакет — единственный источник истины топологии (D-06): дублировать имена/политики в CLI или тесте запрещено. Доменные комментарии — на русском. В `topology_test.go` (suite + Ginkgo/Gomega dot-import, комментарии на английском) проверить чистые helper-функции имён/конфигов из блока behavior — БЕЗ Docker (unit, гоняется pre-push'ем).</action>
  <verify>
    <automated>cd services/inventory && go test ./internal/kafka/topology/... && go vet ./internal/kafka/topology/...</automated>
  </verify>
  <done>topology.go компилируется и экспортирует `Bootstrap`; topology_test.go зелёный без Docker; имена/политики топиков соответствуют D-10/D-12; список агрегатов data-driven.</done>
</task>

<task type="auto">
  <name>Task 3: RS-aware Mongo connection-helper (v2)</name>
  <files>services/inventory/internal/repository/mongoconn/conn.go</files>
  <read_first>
    - .planning/phases/05-dev/05-RESEARCH.md § Code Examples / Mongo connection-helper v2 (строки 440-467: v2 `mongo.Connect(opts...)` БЕЗ ctx, `writeconcern.Majority()`, `Ping(ctx, nil)`, `directConnection`)
    - .planning/phases/05-dev/05-PATTERNS.md § Mongo connection-helper (строки 190-204: инварианты D-14, граница «без UoW/repo»)
    - knowledge/architecture.md (слой repositories; MUST NOT возрождать TxManager/tx.go — это Phase 7)
    - knowledge/style.md (русские доменные комментарии; sentinel+`%w`)
  </read_first>
  <action>Создать пакет `internal/repository/mongoconn` с `func Connect(ctx context.Context, uri string) (*mongo.Client, error)`. Импорты v2: `go.mongodb.org/mongo-driver/v2/mongo`, `.../mongo/options`, `.../mongo/writeconcern`. Тело: `mongo.Connect(options.Client().ApplyURI(uri).SetWriteConcern(writeconcern.Majority()))` — в v2 `Connect` БЕЗ `ctx`; при ошибке `fmt.Errorf("подключение к Mongo: %w", err)`. Затем `cl.Ping(ctx, nil)` (health-ping, readpref nil = primary); при ошибке — `cl.Disconnect(ctx)` + `fmt.Errorf("ping Mongo: %w", err)`. Вернуть клиент. Документировать в комментарии, что URI локального single-node RS требует `?replicaSet=rs0&directConnection=true` (Pitfall 1 — обход RS-discovery), но сам helper хост НЕ хардкодит (для testcontainers доверяем `ConnectionString`). ГРАНИЦА (D-14): только client-фабрика + ping. БЕЗ repository/UoW/Outbox/транзакций — это Phase 7. Доменные комментарии — на русском.</action>
  <verify>
    <automated>cd services/inventory && go build ./... && go vet ./...</automated>
  </verify>
  <done>mongoconn.Connect компилируется и зелёный под `go build`/`go vet` из inventory; helper не содержит repository/UoW; весь модуль inventory собирается атомарно с Task 1-2 (workspace-build зелёный, D-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| go module proxy → go.sum | внешние зависимости втягиваются в сборку; цепочка поставки |
| dev-код → Mongo/Kafka (plaintext) | дев-стенд без SASL/TLS — НЕ для prod |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | go.mod зависимости (mongo-driver/v2, franz-go, kadm) | mitigate | Точные версии (v2.7.0/v1.21.4/v1.18.0), верифицированы по go-proxy (RESEARCH § Package Legitimacy Audit, все verdict=OK); `go.sum` фиксирует хеши; НЕ `@latest` |
| T-05-02 | Information Disclosure | plaintext Mongo connection-helper (без auth/TLS) | accept | Дев-стенд явно dev-only; auth/TLS — отдельная prod-задача (вне Phase 5); helper не несёт секретов |
| T-05-SC | Tampering | go get installs (proxy supply-chain) | mitigate | Все пакеты Approved в RESEARCH § Package Legitimacy Audit (Go proxy = авторитетный источник для Go); нет `[ASSUMED]`/`[SUS]`/`[SLOP]` → checkpoint не требуется |
</threat_model>

<verification>
- `cd services/inventory && go build ./... && go vet ./...` — зелёные (SC1/D-01).
- `grep 'mongo-driver/v2 v2.7.0' services/inventory/go.mod` — присутствует; `grep 'mongo-driver v1.17.9'` — отсутствует (SC1).
- `cd services/inventory && go test ./internal/kafka/topology/...` — зелёный без Docker (unit).
</verification>

<success_criteria>
- inventory собирается с mongo-driver/v2, v1 удалён, build/vet из корня workspace зелёные (SC1).
- topology-пакет — единый источник истины с `Bootstrap` на kadm + unit-тест констант (фундамент SC3).
- mongoconn-helper существует (RS-aware Connect+Ping), без UoW/repo (фундамент SC4).
</success_criteria>

<output>
Create `.planning/phases/05-dev/05-01-SUMMARY.md` when done
</output>
