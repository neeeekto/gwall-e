---
phase: 01-knowledge-base-layout
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - AGENTS.md
  - CLAUDE.md
autonomous: true
requirements: [KB-01, KB-03]
must_haves:
  truths:
    - "Существует AGENTS.md как тонкая КАНОНИЧЕСКАЯ точка входа (источник истины, D-05): короткая шапка проекта + Core Value + таблица-ссылки в knowledge/*.md + указатель на authoring.md, без дублирования контента (KB-03)"
    - "Корневой CLAUDE.md урезан до тонкого гибрида (<~150 строк): короткая шапка + указатель на AGENTS.md и knowledge/ + СОХРАНЁННЫЙ GSD workflow-блок; тяжёлые авто-секции (stack/architecture/conventions/skills) удалены (KB-01, D-06)"
    - "AGENTS.md и CLAUDE.md — отдельные файлы (не symlink); CLAUDE.md ССЫЛАЕТСЯ на AGENTS.md/knowledge, контент не дублируется (D-07)"
    - "В AGENTS.md и CLAUDE.md нет битых ссылок: ссылки в knowledge/ ведут только на существующие после Phase 1 файлы (README.md, authoring.md); нет ссылок на несуществующий memory-bank"
  artifacts:
    - path: "AGENTS.md"
      provides: "Канонический кросс-тульный тонкий вход: шапка + Core Value + таблица-ссылки в knowledge/ + указатель на authoring.md"
      contains: "knowledge/"
      min_lines: 20
    - path: "CLAUDE.md"
      provides: "Тонкий гибрид: шапка + указатель на AGENTS.md/knowledge + сохранённый GSD workflow-блок"
      contains: "AGENTS.md"
  key_links:
    - from: "CLAUDE.md"
      to: "AGENTS.md"
      via: "указатель/ссылка (источник истины)"
      pattern: "AGENTS\\.md"
    - from: "AGENTS.md"
      to: "knowledge/README.md"
      via: "относительная markdown-ссылка"
      pattern: "knowledge/README\\.md"
---

<objective>
Создать тонкие точки входа: AGENTS.md как источник истины (канонический кросс-тульный вход) и урезать корневой CLAUDE.md до тонкого гибрида, сохранив GSD workflow-блок и убрав тяжёлые авто-секции. Закрывает KB-03 и KB-01.

Purpose: Реализовать progressive disclosure (тонкий вход → topic-доки в knowledge/) и снять контекст-бюджетную перегрузку (Pitfall 2). AGENTS.md покрывает не-Claude агентов как открытый стандарт; CLAUDE.md остаётся Claude-специфичным носителем GSD-блока, ссылаясь на AGENTS.md без дублирования (D-05, D-06, D-07).
Output: AGENTS.md (новый), CLAUDE.md (урезан с ~205 до <~150 строк)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-knowledge-base-layout/01-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/FEATURES.md
@CLAUDE.md
@knowledge/README.md
@knowledge/authoring.md
</context>

<artifacts_this_phase_produces>
Эта фаза (Phase 1) создаёт/изменяет ровно эти файлы (по всем планам фазы):
- `knowledge/authoring.md` (Plan 01)
- `knowledge/README.md` (Plan 01)
- `AGENTS.md` (Plan 02 — этот план)
- `CLAUDE.md` (Plan 02 — этот план, урезание до тонкого гибрида)

Этот план зависит от Plan 01: ссылки на knowledge/ ведут только на реально существующие после Plan 01 файлы (README.md, authoring.md). Будущие topic-доки (Phase 2–3) упоминаются без markdown-ссылок.
</artifacts_this_phase_produces>

<interface_context>
Текущий CLAUDE.md (корень, ~205 строк) содержит управляемые GSD-маркеры:
- `<!-- GSD:project-start ... -->` … `<!-- GSD:project-end -->` — шапка проекта (источник для шапки AGENTS.md)
- `<!-- GSD:stack-start ... -->` … `<!-- GSD:stack-end -->` — ТЯЖЁЛЫЙ авто-дамп STACK (УДАЛИТЬ; контент уйдёт в knowledge/ Phase 2–3)
- `<!-- GSD:conventions-start ... -->` … `<!-- GSD:conventions-end -->` — заглушка conventions (УДАЛИТЬ)
- `<!-- GSD:architecture-start ... -->` … `<!-- GSD:architecture-end -->` — заглушка architecture (УДАЛИТЬ)
- `<!-- GSD:skills-start ... -->` … `<!-- GSD:skills-end -->` — заглушка skills (УДАЛИТЬ)
- `<!-- GSD:workflow-start ... -->` … `<!-- GSD:workflow-end -->` — GSD Workflow Enforcement (СОХРАНИТЬ дословно, per D-06)
- `<!-- GSD:profile-start -->` … `<!-- GSD:profile-end -->` — Developer Profile (управляется generate-claude-profile; СОХРАНИТЬ — маленький, управляемый отдельно)

ВАЖНО (риск из CONTEXT.md): `gsd-tools query generate-claude-md` повторно раздувает удалённые секции (stack/architecture/conventions/skills) из research. НЕ запускать generate-claude-md в этой фазе. Оставить в CLAUDE.md HTML-комментарий-предупреждение о том, что эти секции намеренно урезаны и авто-генерацию для них запускать нельзя (правило будет закреплено в Phase 2 boundaries.md).
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Создать AGENTS.md — канонический тонкий вход (KB-03, D-05/D-07)</name>
  <files>AGENTS.md</files>
  <read_first>
    - .planning/PROJECT.md — раздел «What This Is» + «Core Value» (источник шапки AGENTS.md, per Claude's Discretion в CONTEXT.md)
    - knowledge/README.md — какие доки существуют (ссылка) и какие запланированы (без ссылок)
    - knowledge/authoring.md — указатель на стандарт авторинга
    - .planning/phases/01-knowledge-base-layout/01-CONTEXT.md — D-05 (AGENTS.md = источник истины), D-07 (отдельный файл, не дублировать), D-02/D-03
    - .planning/research/PITFALLS.md — Pitfall 2 (тонкий вход ~80–150 строк), Pitfall 8 (без phantom-ссылок), Pitfall 4 (boundary: не верить stale README/Makefile/compose — короткое упоминание допустимо)
  </read_first>
  <action>
Создать AGENTS.md в корне репо — КАНОНИЧЕСКИЙ тонкий кросс-тульный вход (источник истины, per D-05). Открытый стандарт, читается Codex/Cursor/Copilot/Aider/Gemini и т.д. Содержимое на русском (D-03), тонкое, pointer-based — НЕ дублировать контент topic-доков (D-07).

Структура:
1. Короткая шапка проекта — взять из PROJECT.md «What This Is» (1–2 абзаца, сжать) + строка Core Value. Без подробного stack-дампа.
2. Указатель на стандарт авторинга — 1 строка: правила в knowledge/*.md тегируются MUST/SHOULD/WON'T, каждый запрет с «do»; см. `knowledge/authoring.md`.
3. Таблица-ссылки в базу знаний — колонки вроде `Док | О чём | Статус`. Строки:
   - `knowledge/README.md` (индекс/навигация) — со ссылкой, статус «есть».
   - `knowledge/authoring.md` (стандарт авторинга) — со ссылкой, статус «есть».
   - Будущие доки (glossary/structure/build/git/boundaries — Phase 2; style/testing/architecture/patterns — Phase 3) — БЕЗ ссылок, статус «запланировано (Phase 2/3)». (Pitfall 8: ссылки только на существующее.)
   Рекомендация: для навигации в основном указывать на `knowledge/README.md` как на индекс, а полный перечень держать там.
4. Короткий блок «Точки входа» — пояснить: AGENTS.md = источник истины для всех агентов; CLAUDE.md = тонкий Claude-специфичный вход, ссылается сюда и несёт GSD-блок.
5. (Опционально, кратко) одна-две boundary-строки из Pitfall 4 как WON'T: не «чинить» WIP-леса inventory; stale `README.md`/`Makefile`/`docker-compose.yml` в корне НЕ авторитетны — полное правило придёт в `knowledge/boundaries.md` (Phase 2). Не раздувать — это лишь указатель.

КРИТИЧНО: никаких ссылок на несуществующие файлы (включая memory-bank — его нет). Только относительные ссылки на knowledge/README.md и knowledge/authoring.md. Целевой объём — тонкий (~30–80 строк).
  </action>
  <verify>
    <automated>test -f AGENTS.md && grep -q 'knowledge/README.md' AGENTS.md && grep -q 'authoring.md' AGENTS.md && ! grep -q 'memory-bank' AGENTS.md && for f in $(grep -oE '\(knowledge/[a-z-]+\.md\)' AGENTS.md | sed -E 's/[()]//g'); do test -f "$f" || { echo "BROKEN LINK: $f"; exit 1; }; done</automated>
  </verify>
  <done>AGENTS.md существует; тонкая шапка из PROJECT.md + Core Value; указатель на authoring.md; таблица-ссылки в knowledge/ (только на существующие файлы со ссылкой, будущие — без ссылок со статусом «запланировано»); явно обозначен как источник истины; нет ссылок на memory-bank или несуществующие доки; ≤ ~100 строк.</done>
</task>

<task type="auto">
  <name>Task 2: Урезать CLAUDE.md до тонкого гибрида (KB-01, D-06/D-07)</name>
  <files>CLAUDE.md</files>
  <read_first>
    - CLAUDE.md — текущий ~205-строчный файл с GSD-маркерами (см. interface_context этого плана: какие блоки удалить/сохранить)
    - AGENTS.md — на него CLAUDE.md теперь указывает (создан в Task 1)
    - knowledge/README.md — на него тоже указывает
    - .planning/phases/01-knowledge-base-layout/01-CONTEXT.md — D-06 (тонкий гибрид <~150 строк, сохранить GSD workflow-блок, убрать тяжёлые авто-секции), D-07 (отдельный файл, не дублировать), code_context (риск регенерации generate-claude-md)
    - .planning/research/PITFALLS.md — Pitfall 2 (root < ~150 строк, progressive disclosure)
  </read_first>
  <action>
Урезать корневой CLAUDE.md до ТОНКОГО ГИБРИДА (per KB-01, D-06). Это редактирование существующего файла, НЕ генерация заново. НЕ запускать `gsd-tools query generate-claude-md` (он повторно раздует удалённые секции).

Удалить целиком (вместе с их GSD-маркерами `*-start`/`*-end`):
- блок `GSD:stack` (тяжёлый STACK-дамп) — контент уйдёт в knowledge/ Phase 2–3;
- блок `GSD:conventions` (заглушка);
- блок `GSD:architecture` (заглушка);
- блок `GSD:skills` (заглушка).

Сохранить:
- блок `GSD:workflow` (GSD Workflow Enforcement) — дословно, per D-06 (он Claude-специфичен);
- блок `GSD:profile` (Developer Profile) — управляется generate-claude-profile, маленький, оставить как есть.

Переписать верхнюю часть (шапка + GSD:project-блок) в тонкий вид:
1. Короткая шапка проекта (1–2 строки) — что такое gwall-e + Core Value (сжать; не дублировать длинный текст из AGENTS.md).
2. Явный указатель: «Источник истины для конвенций — `AGENTS.md`; детальные правила — в `knowledge/` (progressive disclosure), начни с `knowledge/README.md`». CLAUDE.md НЕ дублирует контент — только ссылается (D-07).
3. Можно оставить компактный GSD:project-блок (короткие Constraints), но БЕЗ повторения детального stack — для деталей ссылаться на knowledge/ и AGENTS.md. Если Constraints дублируют AGENTS.md — заменить на ссылку.
4. Добавить HTML-комментарий-предупреждение вверху (например `<!-- ВНИМАНИЕ: CLAUDE.md намеренно урезан до тонкого гибрида (Phase 1, KB-01/D-06). НЕ запускать generate-claude-md — он повторно раздует удалённые секции stack/architecture/conventions/skills. Детали — в knowledge/. Правило закрепляется в knowledge/boundaries.md (Phase 2). -->`).

Итог: файл < ~150 строк (KB-01), ссылается на AGENTS.md и knowledge/README.md, сохранил GSD workflow/profile блоки, не содержит ссылок на memory-bank или несуществующие доки, не дублирует контент knowledge/.
  </action>
  <verify>
    <automated>test -f CLAUDE.md && [ "$(wc -l < CLAUDE.md)" -lt 150 ] && grep -q 'AGENTS.md' CLAUDE.md && grep -q 'knowledge/' CLAUDE.md && grep -q 'GSD:workflow-start' CLAUDE.md && ! grep -q 'GSD:stack-start' CLAUDE.md && ! grep -q 'GSD:architecture-start' CLAUDE.md && ! grep -q 'GSD:conventions-start' CLAUDE.md && ! grep -q 'memory-bank' CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md < 150 строк; удалены блоки stack/conventions/architecture/skills (вместе с маркерами); сохранён GSD:workflow и GSD:profile; есть указатель на AGENTS.md и knowledge/README.md; добавлено предупреждение про generate-claude-md; нет ссылок на memory-bank; контент не дублирует knowledge/.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| репозиторий → коммит/публичный git remote | AGENTS.md и CLAUDE.md попадают в git (origin → github.com/neeeekto/gwall-e) и читаются ИИ-агентами/командой |

## STRIDE Threat Register

Эта фаза производит только Markdown-точки-входа (нет исполняемого кода, нет сети/данных). Поверхность минимальна.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | AGENTS.md, CLAUDE.md | mitigate | Не включать секреты/креды/токены/внутренние URL; точки входа содержат только шапку, указатели и ссылки на knowledge/ |
| T-01-04 | Tampering | авто-регенерация CLAUDE.md | mitigate | Не запускать generate-claude-md; HTML-комментарий-предупреждение в CLAUDE.md фиксирует запрет re-bloat (правило закрепляется в boundaries.md, Phase 2) |
</threat_model>

<verification>
- `wc -l CLAUDE.md` < 150.
- `grep -RInE 'memory-bank' AGENTS.md CLAUDE.md` пусто.
- Все относительные ссылки `knowledge/*.md` в AGENTS.md и CLAUDE.md разрешаются в существующие файлы (README.md, authoring.md).
- CLAUDE.md содержит блок GSD:workflow и не содержит блоков GSD:stack/architecture/conventions/skills.
- AGENTS.md явно помечен как источник истины; CLAUDE.md указывает на AGENTS.md.
</verification>

<success_criteria>
- KB-03: AGENTS.md — тонкий кросс-тульный указатель (источник истины) без дублирования контента.
- KB-01: CLAUDE.md урезан до <~150 строк, ссылается на knowledge/*.md (progressive disclosure), сохранил GSD workflow-блок.
- D-07: AGENTS.md и CLAUDE.md — отдельные файлы, контент не дублируется.
- Нет битых/phantom-ссылок; защита от повторного раздувания CLAUDE.md задокументирована.
</success_criteria>

<output>
Create `.planning/phases/01-knowledge-base-layout/01-02-SUMMARY.md` when done
</output>
