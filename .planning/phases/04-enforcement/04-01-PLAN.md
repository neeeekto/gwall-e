---
phase: 04-enforcement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .golangci.yml
  - Makefile
autonomous: true
requirements: [ENF-01]
must_haves:
  truths:
    - "`golangci-lint config verify` exits 0 against the new `.golangci.yml` (valid v2 schema)"
    - "depguard `no-cqrs-bus` ban bites today: any import of `github.com/gwall-e/pkg/mediatr` fails lint (D-06)"
    - "gofumpt + gci run embedded inside golangci-lint v2 `formatters` block, never standalone (D-02)"
    - "`make tools` installs golangci-lint v2.12.2 / lefthook v2.1.9 / buf v1.71.0 at pinned versions (D-11)"
  artifacts:
    - path: ".golangci.yml"
      provides: "v2-schema lint+format config: linters.default standard + errorlint + depguard; formatters gofumpt+gci"
      contains: "version: \"2\""
    - path: "Makefile"
      provides: "reproducible pinned tool install via `make tools` (D-11)"
      contains: "GOLANGCI_VERSION"
  key_links:
    - from: ".golangci.yml"
      to: "github.com/gwall-e/pkg/mediatr"
      via: "depguard no-cqrs-bus deny (biting)"
      pattern: "pkg/mediatr"
    - from: "Makefile"
      to: "github.com/golangci/golangci-lint/v2/cmd/golangci-lint"
      via: "go install pinned version"
      pattern: "golangci-lint/v2"
---

<objective>
Ship the single root `.golangci.yml` (ENF-01) — golangci-lint **v2** schema with `linters.default: standard` + opt-in `errorlint`/`depguard`, and a `formatters` block running gofumpt + gci — plus a `Makefile` `make tools` target pinning every Go tool reproducibly (D-11). This is the live lint/format gate the lefthook pre-commit hook (plan 04-03) will call, and the source of truth whose statuses ENF-05 (plan 04-04) flips the `knowledge/*.md` marks to.

Purpose: the knowledge base stops being purely declarative — `%w`-wrapping (errorlint), import-direction layering (depguard, forward/dormant), and resurrection bans on the removed CQRS bus / TxManager (depguard, biting today) become mechanically enforceable from one config that future CI reuses unchanged (D-02).
Output: `.golangci.yml`, `Makefile`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-enforcement/04-CONTEXT.md
@.planning/phases/04-enforcement/04-RESEARCH.md
@.planning/phases/04-enforcement/04-PATTERNS.md
@knowledge/architecture.md
@knowledge/style.md
@knowledge/boundaries.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan creates (of the full phase set):
- `.golangci.yml` — ENF-01, v2 schema, single source of truth for lint+format
- `Makefile` — D-11, `make tools` pinned install target

(Other phase artifacts — `lefthook.yml`, `package.json`, `commitlint.config.*`, `buf.yaml`, `buf.gen.yaml`, and the edited `knowledge/*.md` enforcement-status flips — are produced by plans 04-02 / 04-03 / 04-04.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create root `.golangci.yml` (v2 schema, ENF-01)</name>
  <files>.golangci.yml</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §Code Examples `.golangci.yml` (~L335-393) — authoritative target content, version-verified this session
    - .planning/phases/04-enforcement/04-RESEARCH.md §Common Pitfalls 1/2/4 (~L282-304) — `/v2/` path, v2-only schema, gofumpt-embedded
    - .planning/phases/04-enforcement/04-PATTERNS.md §`.golangci.yml` (~L49-60) — critical constraints
    - knowledge/architecture.md L150-154 — the removed-package WON'T rules the depguard bans back
  </read_first>
  <action>
    Create `.golangci.yml` at repo root by copying the version-verified target from RESEARCH §Code Examples `.golangci.yml`. Set `version: "2"` and `linters.default: standard` (v2 schema — NOT v1 `enable-all`/`disable-all`, per Pitfall 2). Under `linters.enable` add `errorlint` and `depguard` (D-06). Configure `errorlint` settings (`errorf: true`, `asserts: true`, `comparison: true`) so `%w`-wrapping is required. Configure `depguard.rules` with THREE rule sets per D-05/D-06: (1) `no-cqrs-bus` — `list-mode: lax`, `files: ["$all"]`, deny `github.com/gwall-e/pkg/mediatr` with a `desc` citing architecture.md §MUST NOT CQRS — this BITES today; (2) `no-tx-manager` — deny the forward TxManager/`tx` dispatcher path with a `desc` citing the UnitOfWork port — forward guard (the exact removed import path is gone from the repo per Assumption A3, so use the best-guess `.../inventory/internal/tx` path); (3) `domain-imports-inward-only` — `list-mode: strict`, `files: ["**/internal/domain/**"]`, allow `$gostd` + `github.com/gwall-e/pkg`, deny `github.com/gwall-e/services` with a `desc` that states the rule is DORMANT until layer code exists (D-05 — no `internal/domain` Go files exist yet, so it matches nothing; do NOT claim it actively enforces anything). Add a top-level `formatters` block (NOT under `linters`, per Pitfall 2) with `enable: [gofumpt, gci]`; configure `gci.sections` as `standard`, `default`, `prefix(github.com/gwall-e)` with `custom-order: true`, and `gofumpt.module-path: github.com/gwall-e`. Do NOT install or reference standalone gofumpt — it runs embedded in golangci-lint v2.12.2 (gofumpt 0.9.2), avoiding the gofumpt-0.10.0 Go-1.25 requirement (Pitfall 4). Do NOT place fenced code inside this config beyond the YAML file itself.
  </action>
  <verify>
    <automated>command -v golangci-lint >/dev/null 2>&1 && golangci-lint config verify || echo "MISSING — run 'make tools' first (Task 2), then: golangci-lint config verify (exits 0); grep -q 'version: \"2\"' .golangci.yml && grep -q 'pkg/mediatr' .golangci.yml && grep -q 'gofumpt' .golangci.yml"</automated>
  </verify>
  <acceptance_criteria>
    - `.golangci.yml` exists at repo root with `version: "2"` and `linters.default: standard`
    - `golangci-lint config verify` exits 0 (after `make tools`)
    - `depguard` `no-cqrs-bus` denies `github.com/gwall-e/pkg/mediatr` (biting)
    - `domain-imports-inward-only` rule `desc` states it is DORMANT until layer code lands
    - `formatters` block (top-level) enables `gofumpt` + `gci`; no standalone gofumpt install
  </acceptance_criteria>
  <done>Valid v2 `.golangci.yml` exists; `config verify` passes; biting CQRS-bus ban and dormant layer rules present; gofumpt/gci embedded in formatters.</done>
</task>

<task type="auto">
  <name>Task 2: Create `Makefile` with pinned `make tools` target (D-11)</name>
  <files>Makefile</files>
  <read_first>
    - .planning/phases/04-enforcement/04-RESEARCH.md §Code Examples `Makefile` (~L496-509) — authoritative pinned-install target
    - .planning/phases/04-enforcement/04-RESEARCH.md §Pitfall 1 (~L282-286) golangci `/v2/` path; §Pitfall 3 (~L294-298) lefthook v2; §Pitfall 4 (~L300-304) no standalone gofumpt
    - .planning/phases/04-enforcement/04-PATTERNS.md §`Makefile` (~L116-126) — do NOT add `tool` block to rotten root go.mod
  </read_first>
  <action>
    Create `Makefile` at repo root (verified absent today) by copying the target from RESEARCH §Code Examples `Makefile`. Declare pinned version vars: `GOLANGCI_VERSION := v2.12.2`, `LEFTHOOK_VERSION := v2.1.9`, `BUF_VERSION := v1.71.0` (all version-verified June 2026; do NOT use `@latest`, D-11). Add a `.PHONY: tools` target that runs `go install` for: `github.com/golangci/golangci-lint/v2/cmd/golangci-lint@$(GOLANGCI_VERSION)` (MUST include `/v2/`, Pitfall 1), `github.com/evilmartians/lefthook/v2@$(LEFTHOOK_VERSION)` (v2, Pitfall 3), and `github.com/bufbuild/buf/cmd/buf@$(BUF_VERSION)`. Do NOT add a `go install mvdan.cc/gofumpt` line — gofumpt runs embedded in golangci-lint v2 (Pitfall 4); add a comment saying so. End the target with an `@echo` reminding the next bootstrap steps: `npm install` (commitlint) and `lefthook install` (git hooks). Do NOT add a `tool` directive to the rotten root `github.com/gwall-e` go.mod (outside go.work, stale ginkgo v1/mongo v1) — the Makefile is the chosen pinning mechanism (D-11 discretion).
  </action>
  <verify>
    <automated>grep -q 'GOLANGCI_VERSION := v2.12.2' Makefile && grep -q 'golangci-lint/v2/cmd/golangci-lint' Makefile && grep -q 'lefthook/v2@' Makefile && grep -q 'bufbuild/buf' Makefile && ! grep -q 'gofumpt' Makefile && grep -q '.PHONY: tools' Makefile</automated>
  </verify>
  <acceptance_criteria>
    - `Makefile` exists with pinned version vars (golangci v2.12.2, lefthook v2.1.9, buf v1.71.0)
    - `tools` target installs golangci-lint via the `/v2/` path and lefthook via `/v2`
    - NO standalone gofumpt install line
    - NO `tool` block added to the root go.mod
    - Echo line names `npm install` + `lefthook install` next steps
  </acceptance_criteria>
  <done>`make tools` installs all three Go tools at pinned versions; gofumpt embedded (not installed); root go.mod untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Go module proxy | `make tools` fetches tool binaries from upstream module paths (supply chain) |
| committed config → future CI | the same `.golangci.yml` is reused by CI unchanged; config drift would diverge enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | `make tools` pulling a drifted/malicious tool version | mitigate | Exact-pin golangci v2.12.2 / lefthook v2.1.9 / buf v1.71.0 in `Makefile` (no `@latest`); install only from canonical module paths verified vs official GitHub release pages (RESEARCH §Sources) |
| T-4-02 | Tampering | gofumpt standalone (Go 1.25) introducing a divergent format binary | accept→mitigate | Run gofumpt embedded in golangci-lint v2 `formatters`; never install standalone (Pitfall 4) — eliminates the divergent-binary surface |
| T-4-03 | Repudiation/Info | depguard layer rule presented as active when dormant (phantom enforcement) | mitigate | `domain-imports-inward-only` rule `desc` explicitly marks DORMANT; ENF-05 (plan 04-04) flips the doc mark to `hook (dormant)` — no-phantom (D-05) |
| T-4-SC | Tampering | npm/go installs | mitigate | This plan installs only Go tools from canonical paths (no npm here); commitlint npm audit handled in plan 04-02. Keep `T-4-SC` visible across the phase. |
</threat_model>

<verification>
After `make tools`:
- `golangci-lint config verify` → exits 0 (ENF-01 config valid v2)
- `cd pkg && golangci-lint run ./...` → runs against the real ginkgo module (smoke; pre-existing code may surface findings, acceptable)
- Resurrection-ban smoke: temporarily add `import _ "github.com/gwall-e/pkg/mediatr"` to a throwaway file → `golangci-lint run` fails on the depguard `no-cqrs-bus` deny; revert.
</verification>

<success_criteria>
- `.golangci.yml` is valid v2 and `golangci-lint config verify` exits 0
- depguard resurrection ban on `pkg/mediatr` bites; layer rules documented dormant
- `make tools` pins golangci v2.12.2 / lefthook v2.1.9 / buf v1.71.0; no standalone gofumpt; root go.mod untouched
</success_criteria>

<output>
Create `.planning/phases/04-enforcement/04-01-SUMMARY.md` when done
</output>
