---
phase: 03-conventions-architecture-docs
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [knowledge/architecture.md]
autonomous: true
requirements: [DOC-05]
must_haves:
  truths:
    - "knowledge/architecture.md существует и явно заявляет DDD + гексагон БЕЗ CQRS-шины (D-04; Success Criteria 3)"
    - "Содержит правила слоёв и текстовую диаграмму/таблицу направления импортов: domain ← usecases ← api/repositories; domain не импортирует наружу (D-06)"
    - "Фиксирует инварианты: usecases-interactor Execute, query-lite read-side в DTO, порт UnitOfWork, transactional outbox + relay, фабрики агрегатов + PullEvents (D-04)"
    - "Явный MUST NOT возрождать CQRS-диспетчер / pkg/mediatr / TxManager (D-04)"
    - "Все Go-сниппеты ИЛЛЮСТРАТИВНЫЕ на плейсхолдере Order, помечены «целевой вид / иллюстрация — не из компилируемого файла» (D-01, D-05, no-phantom)"
    - "Механизируемые правила несут forward-enforcement-метку (D-11)"
  artifacts:
    - path: "knowledge/architecture.md"
      provides: "Канон целевой архитектуры: слои, импорты, Execute, query-lite, UnitOfWork, outbox+relay, PullEvents, MUST NOT CQRS"
      min_lines: 80
      contains: "UnitOfWork"
  key_links:
    - from: "knowledge/architecture.md"
      to: "knowledge/style.md"
      via: "ссылка на typed IDs/ошибки/DTO→домен при упоминании"
      pattern: "style.md"
---

<objective>
Создать `knowledge/architecture.md` — канон целевой архитектуры (DOC-05): DDD + гексагон БЕЗ CQRS-шины. Инварианты/why (правила), а не how-to (рецепты — в patterns.md, Wave 3).

Purpose: Зафиксировать единственный канон архитектурных правил: слои и направление импортов, usecases-interactor (`Execute`), query-lite read-side в DTO, порт `UnitOfWork`, transactional outbox + relay, фабрики агрегатов + `PullEvents`, и явный MUST NOT возрождать снесённые CQRS-диспетчер / `pkg/mediatr` / `TxManager`. patterns.md будет ссылаться на эти правила (D-04, pointer-over-copy).
Output: `knowledge/architecture.md` (~80–200 строк; при >200 — дробить per authoring.md, выделив события в под-топик и связав ссылкой), сниппеты иллюстративные на плейсхолдере `Order`, с диаграммой импортов и enforcement-метками.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conventions-architecture-docs/03-CONTEXT.md
@.planning/phases/03-conventions-architecture-docs/03-VALIDATION.md
@knowledge/authoring.md
@knowledge/boundaries.md
@knowledge/structure.md
@knowledge/style.md
</context>

<artifacts_this_phase_produces>
This plan produces: `knowledge/architecture.md` (NEW). Depends on style.md (03-01) for cross-links to code-level rules. Consumed by patterns.md (03-04).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Написать knowledge/architecture.md (DDD+гексагон БЕЗ CQRS — правила/инварианты)</name>
  <files>knowledge/architecture.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт (MUST/SHOULD/WON'T, парность запрет→do, pointer-over-copy, размер/дробление ~150–200 строк, no-phantom, статус enforcement)
    - knowledge/structure.md — заявляет, что внутренняя слоистая раскладка — канон architecture.md (обратная ссылка уже есть в structure.md L11-14)
    - knowledge/style.md — typed IDs/sentinel-vs-wrapped errors/DTO→домен (ссылаться при упоминании, НЕ копировать); создан 03-01
    - knowledge/boundaries.md — no-phantom правило (сниппеты не выдавать за существующий код)
    - .planning/PROJECT.md — Key Decisions: DDD+гексагон БЕЗ CQRS, write-side 1 use case = struct + Execute, read-side query-lite в DTO, порт UnitOfWork, transactional outbox + relay; снос internal/pkg/mediatr/tx.go; целевая раскладка слоёв domain/usecases/query/repositories/api/cron, app=composition root, cmd=main
    - .planning/phases/03-conventions-architecture-docs/03-CONTEXT.md — D-01 (сниппеты иллюстративные, помеченные), D-04 (правила vs рецепты — architecture.md держит инварианты), D-05 (плейсхолдер Order), D-06 (диаграмма импортов), D-11 (enforcement-метки)
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — §Architecture Patterns B (целевой сервис), Code Examples §1 (Execute/UnitOfWork/PullEvents — иллюстрация), §State of the Art (снесённые CQRS/TxManager — MUST NOT), §Security Domain (НЕ документировать будущие платформенные security-контроли как существующие), Pitfall 2 (мега-файл — дробить при >200)
  </read_first>
  <action>
    Создать `knowledge/architecture.md` (русская проза; EN тех-термины/код), следуя authoring.md. Держать на уровне ИНВАРИАНТОВ/WHY (правила), НЕ пошаговых how-to (это patterns.md, D-04).
    Шапка-абзац: architecture.md — канон целевой архитектуры gwall-e: DDD + гексагональная архитектура БЕЗ CQRS-шины. Заявить явно слово «гексагон» и «CQRS». Рецепты «как добавить» — ссылка на patterns.md (упомянуть, что patterns.md ссылается сюда за правилами).
    Обязательные блоки/правила (каждое — MUST/SHOULD/WON'T тег per authoring; enforcement-метки D-11):
      1. Слои и направление импортов (D-06): MUST зависимости всегда внутрь на `domain`; `domain` НЕ импортирует наружу. Текстовая диаграмма ИЛИ таблица «кто кого может импортировать» (`domain ← usecases ← api`/`repositories`; query/cron; app=composition root ручной DI, cmd=main). Enforcement: `planned: CI-gated Phase 4 (depguard)`.
      2. Write-side use case (Execute): MUST 1 use case = 1 struct + метод `Execute(ctx, in) (out, error)`; маппинг DTO→домен — ссылка на style.md. Иллюстративный сниппет `RegisterOrderUseCase` (помечен «иллюстрация»).
      3. Порт UnitOfWork: MUST транзакционная граница через порт `UnitOfWork` в `domain/` (`Do(ctx, fn)`); запись агрегата и outbox-событий — в одной tx.
      4. Read-side query-lite: MUST/SHOULD чтение — query-lite напрямую в DTO (мимо агрегатов), отдельно от write-side.
      5. Transactional outbox + relay: MUST доменные события писать в outbox в той же tx; async relay публикует и помечает published.
      6. Фабрики агрегатов + PullEvents: MUST агрегат создаётся фабрикой (инварианты) и накапливает события → `PullEvents()` отдаёт их для outbox.
      7. Явный MUST NOT (anti-patterns, §State of the Art): WON'T/MUST NOT возрождать CQRS-диспетчер / `CommandDispatcher`/`QueryDispatcher` / `pkg/mediatr` / `TxManager` / `tx.go` — снесены намеренно; вместо них — inbound-адаптеры зовут use case напрямую + порт UnitOfWork (парность запрет→do).
    No-phantom (D-01): ВСЕ Go-сниппеты помечены «целевой вид / иллюстрация — НЕ из компилируемого файла», плейсхолдер `Order` (D-05, то же имя, что в style.md/patterns.md). НЕ ссылаться на несуществующие пути `services/.../order.go`.
    Security no-phantom (RESEARCH §Security Domain): НЕ документировать будущие платформенные security-контроли (ownership-race, SSH-grants, audit) как существующие правила. SHOULD-упоминание «валидация на edge (protovalidate в api/), домен не валидирует транспорт» допустимо как конвенция (не реализация).
    НЕ дублировать правила языка/ошибок (style.md) — ссылка. НЕ писать пошаговые how-to (patterns.md).
    Размер: цель ≤ ~200 строк. Если превышает — выделить блок событий (outbox/relay/PullEvents) в `knowledge/architecture-events.md` и связать ссылкой; обновить индекс отметить в SUMMARY (Claude's Discretion per D-открытый вопрос 1).
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && test -f knowledge/architecture.md && grep -qi 'гексагон' knowledge/architecture.md && grep -qi 'CQRS' knowledge/architecture.md && grep -qiE 'MUST NOT|WON.T' knowledge/architecture.md && MISS=0; for k in Execute UnitOfWork outbox PullEvents query; do grep -qi "$k" knowledge/architecture.md || { echo "MISSING $k"; MISS=1; }; done; grep -qiE 'domain.*←|импорт|import' knowledge/architecture.md && grep -qiE 'иллюстрац|целевой вид' knowledge/architecture.md && grep -qiE 'mediatr|TxManager|диспетчер' knowledge/architecture.md && grep -qiE 'convention-only|planned.*Phase 4|CI-gated|hook' knowledge/architecture.md && [ "$MISS" = 0 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f knowledge/architecture.md` истинно.
    - DOC-05 явно: `grep -qi 'гексагон'` И `grep -qi 'CQRS'` И `grep -qiE 'MUST NOT|WON.T'` зелёные.
    - DOC-05 элементы: для каждого `k` из {Execute, UnitOfWork, outbox, PullEvents, query} — `grep -qi "$k"` зелёный (без MISSING).
    - D-06 диаграмма: `grep -qiE 'domain.*←|импорт|import' knowledge/architecture.md` (визуальная корректность — ручной просмотр: domain не импортирует наружу).
    - MUST NOT CQRS-возрождение: `grep -qiE 'mediatr|TxManager|диспетчер' knowledge/architecture.md`.
    - D-01 no-phantom: `grep -qiE 'иллюстрац|целевой вид' knowledge/architecture.md`.
    - D-11: `grep -qiE 'convention-only|planned.*Phase 4|CI-gated|hook' knowledge/architecture.md`.
    - Размер: `wc -l knowledge/architecture.md` ≤ ~200 (иначе дробление + индекс в Wave 5).
    - Плейсхолдер Order, нет доменных host/VM/owner как плейсхолдера-агрегата (ручной просмотр).
  </acceptance_criteria>
  <done>knowledge/architecture.md существует, проходит все DOC-05 presence-проверки, содержит диаграмму импортов, MUST NOT CQRS/mediatr/TxManager, иллюстративные помеченные сниппеты на Order, enforcement-метки, ≤ ~200 строк (или раздроблен со ссылкой).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reader (ИИ/команда) | Читатель доверяет правилу как факту репозитория |
| правило → будущая платформа | Описание будущих security-контролей как существующих правил вводит в заблуждение |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Repudiation/Spoofing | architecture.md сниппеты/правила | mitigate | no-phantom (D-01, Pitfall 1): все сниппеты помечены «целевой вид / иллюстрация — не из компилируемого файла» на плейсхолдере Order; нет ссылок на несуществующие пути |
| T-03-04 | Repudiation/Spoofing | будущие security-контроли (ownership-race, SSH-grants, audit) | mitigate | architecture.md НЕ документирует будущие платформенные security-правила как существующие (RESEARCH §Security Domain); только конвенция «валидация на edge» как SHOULD-упоминание |
| T-03-05 | Tampering | утечка инфраструктуры/транспорта в домен | mitigate | Anti-Pattern: только порты в домене, реализации в адаптерах; domain не импортирует наружу (диаграмма D-06) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Документационная фаза — пакеты НЕ устанавливаются (RESEARCH §Package Legitimacy Audit; install — Phase 4) |
</threat_model>

<verification>
- DOC-05 presence + элементы + диаграмма команды из 03-VALIDATION.md зелёные.
- Ручной просмотр диаграммы импортов (D-06) и меток «иллюстрация» (no-phantom) — Manual-Only из VALIDATION.md.
- Архитектурные правила только здесь (uniqueness — финал Wave 5).
- Размер ≤ ~200 строк (иначе дробление).
- Dimension-8 (validation): фаза-частично-зелёная при зелёных DOC-05 shell-проверках.
</verification>

<success_criteria>
knowledge/architecture.md создан, удовлетворяет DOC-05 (Success Criteria 3 ROADMAP), следует authoring-стандарту, фиксирует все инварианты + MUST NOT CQRS, готов служить целью ссылок из patterns.md.
</success_criteria>

<output>
Create `.planning/phases/03-conventions-architecture-docs/03-03-SUMMARY.md` when done.
</output>
