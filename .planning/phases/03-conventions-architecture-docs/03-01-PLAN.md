---
phase: 03-conventions-architecture-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [knowledge/style.md]
autonomous: true
requirements: [DOC-04]
must_haves:
  truths:
    - "knowledge/style.md существует и является единственным каноном правила про язык комментариев (D-07; Success Criteria 1)"
    - "style.md фиксирует: RU-комментарии/доменная терминология, EN-имена идентификаторов, EN-комментарии в тестах (D-07)"
    - "style.md содержит правила typed IDs, sentinel vs обёрнутые ошибки (%w), маппинг DTO→домен внутри хендлера, каждое в формате «правило + плохо/хорошо» (D-08)"
    - "Общий Go-стиль (формат/нейминг) НЕ дублируется — ссылка на gofumpt Phase 4 + Effective Go (D-07)"
    - "Механизируемые правила несут forward-enforcement-метку (D-11)"
  artifacts:
    - path: "knowledge/style.md"
      provides: "Канон языка кода/комментариев, typed IDs, ошибки, DTO→домен"
      min_lines: 60
      contains: "%w"
  key_links:
    - from: "knowledge/style.md"
      to: "Effective Go / gofumpt (Phase 4)"
      via: "ссылка вместо дублирования общего Go-стиля"
      pattern: "gofumpt|Effective Go"
---

<objective>
Создать `knowledge/style.md` — канон языка и проект-специфичного Go-стиля gwall-e (DOC-04).

Purpose: Зафиксировать единственное место правила про язык комментариев (RU-комментарии/EN-имена) и проект-специфичные правила кода (typed IDs, sentinel-vs-wrapped errors, DTO→домен в хендлере), чтобы остальные доки ссылались, а не копировали (pointer-over-copy, карта владения).
Output: `knowledge/style.md` (~60–150 строк), следующий authoring-стандарту, с forward-enforcement-метками. Это фундамент Wave 1: на него ссылаются testing.md (EN-комментарии) и architecture.md/patterns.md (typed IDs/errors/DTO→домен).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conventions-architecture-docs/03-CONTEXT.md
@.planning/phases/03-conventions-architecture-docs/03-VALIDATION.md
@knowledge/authoring.md
@knowledge/boundaries.md
@knowledge/structure.md
@knowledge/build.md
</context>

<artifacts_this_phase_produces>
This plan produces: `knowledge/style.md` (NEW).
(Sibling plans produce: testing.md [02], architecture.md [03], patterns.md [04]; index/ownership edits to README.md + boundaries.md + AGENTS.md [05].)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Написать knowledge/style.md (язык + проект-специфичный Go-стиль)</name>
  <files>knowledge/style.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт MUST/SHOULD/WON'T, парность «запрет → do», pointer-over-copy, размер ~150–200 строк, статус enforcement (обязателен)
    - knowledge/structure.md, knowledge/build.md — структурные аналоги (тон, длина, формат ссылок) + куда ссылаться за версией Go (НЕ прописывать версию Go в style.md)
    - knowledge/boundaries.md — карта владения фактами (L67 уже резервирует style.md за языком кода)
    - .planning/phases/03-conventions-architecture-docs/03-CONTEXT.md — D-07 (охват: только проект-специфика), D-08 (формат «правило + плохо/хорошо»), D-11 (enforcement-метки)
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — Code Examples §1 (typed ID, sentinel %w + обёртка, DTO→домен в хендлере), Pattern 1 (формат «плохо/хорошо»), Pattern 4 (forward enforcement-метка)
  </read_first>
  <action>
    Создать `knowledge/style.md` (русская проза; EN тех-термины/код), следуя authoring.md.
    Шапка-абзац: style.md — единственный канон языка кода и проект-специфичного стиля gwall-e; общий Go-стиль (формат/нейминг) НЕ дублируется — ссылка на gofumpt (planned Phase 4) и Effective Go. Сослаться на authoring.md за форматом правил.
    Обязательные правила (каждое — MUST/SHOULD тег + мини-пример «плохо → хорошо» per D-08, формат как Pattern 1 в RESEARCH §Code Examples §1):
      1. Язык (D-07): MUST комментарии в коде и доменная терминология — на русском; MUST имена идентификаторов — английские; MUST комментарии в тестах — английские. Явно заявить: style.md — единственное место правила про язык комментариев (карта владения).
      2. Типизированные ID (D-08): MUST использовать typed ID (`type OrderID string`) вместо голой строки. Пример плохо/хорошо на нейтральном плейсхолдере `Order` (ТО ЖЕ имя, что в architecture.md/patterns.md — D-05).
      3. Sentinel vs обёрнутые ошибки: MUST объявлять sentinel-ошибки (`var ErrOrderNotFound = errors.New(...)`) и оборачивать через `%w` (`fmt.Errorf("...: %w", err)`), сохраняя цепочку. Пример плохо (потеря цепочки `%v`) → хорошо (`%w`).
      4. Маппинг DTO→домен ВНУТРИ хендлера: MUST декодить/валидировать транспорт на edge и маппить DTO→домен в хендлере (домен не знает транспорт). Пример плохо/хорошо.
    Каждое механизируемое правило получает forward-enforcement-метку (D-11): `convention-only` / `planned: CI-gated Phase 4` / `planned: hook Phase 4 (gofumpt)` (язык комментариев — `convention-only`/review-enforced; формат — `planned: hook Phase 4`).
    Использовать НЕЙТРАЛЬНЫЙ плейсхолдер `Order` во всех Go-сниппетах — НЕ доменные термины gwall-e (host/VM/owner) (D-05). Имя плейсхолдера `Order` фиксируется здесь и переиспользуется в architecture.md/patterns.md.
    НЕ дублировать архитектурные правила (это architecture.md) и команды (build.md) — при необходимости ссылка.
    НЕ вставлять fenced полное содержимое из чужого канона; код — короткие иллюстративные мини-примеры.
    Цель размера — ≤ ~200 строк (authoring); дробить НЕ требуется (style.md компактен).
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && test -f knowledge/style.md && grep -qiE 'русск|коммент' knowledge/style.md && grep -qE '%w' knowledge/style.md && grep -qiE 'типизирован|typed' knowledge/style.md && grep -qiE 'DTO' knowledge/style.md && grep -qiE 'convention-only|planned.*Phase 4|CI-gated|hook' knowledge/style.md && grep -qiE 'gofumpt|Effective Go' knowledge/style.md && grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/style.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f knowledge/style.md` истинно.
    - DOC-04 presence: `grep -qiE 'русск|коммент'` И `grep -qE '%w'` И `grep -qiE 'типизирован|typed'` И `grep -qiE 'DTO'` — все зелёные.
    - D-11: `grep -qiE 'convention-only|planned.*Phase 4|CI-gated|hook' knowledge/style.md`.
    - D-07 не-дублирование общего стиля: `grep -qiE 'gofumpt|Effective Go' knowledge/style.md`.
    - Authoring теги: `grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/style.md`.
    - Размер: `wc -l knowledge/style.md` ≤ ~200 (флаг при >200 → дробить per authoring.md).
    - Нейтральный плейсхолдер: сниппеты используют `Order`, нет host/VM/owner: `! grep -qiE '\bhost\b|\bVM\b|owner' knowledge/style.md` (или только в контексте, не как плейсхолдер-агрегат — ручной просмотр).
    - Uniqueness (проверяется в Wave финализации): правило языка только в style.md.
  </acceptance_criteria>
  <done>knowledge/style.md существует, проходит все DOC-04 presence-проверки, несёт enforcement-метки, использует плейсхолдер Order, ≤ ~200 строк.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reader (ИИ/команда) | Читатель доверяет нормативному правилу как факту репозитория |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Repudiation/Spoofing | style.md правило/сниппет | mitigate | no-phantom: style.md фиксирует только реально применимые конвенции; сниппеты на плейсхолдере Order помечены как иллюстрация, не выдаются за существующий файл |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Документационная фаза — пакеты НЕ устанавливаются (RESEARCH §Package Legitimacy Audit: Phase 3 ничего не ставит; mockery install — Phase 4). Аудит-таблица присутствует в RESEARCH.md |
</threat_model>

<verification>
- DOC-04 presence-команды из 03-VALIDATION.md зелёные.
- Размер ≤ ~200 строк.
- Ручной просмотр: каждое правило с тегом силы, нет хеджирования («обычно/желательно/prefer») — `grep -iE 'обычно|желательно|prefer' knowledge/style.md` должно быть пусто в нормативных строках.
- Dimension-8 (validation): фаза-частично-зелёная, когда shell-проверки DOC-04 проходят.
</verification>

<success_criteria>
knowledge/style.md создан, удовлетворяет DOC-04 (Success Criteria 1 ROADMAP), следует authoring-стандарту, фиксирует плейсхолдер Order для downstream-доков.
</success_criteria>

<output>
Create `.planning/phases/03-conventions-architecture-docs/03-01-SUMMARY.md` when done.
</output>
