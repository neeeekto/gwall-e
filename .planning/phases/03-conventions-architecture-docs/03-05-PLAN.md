---
phase: 03-conventions-architecture-docs
plan: 05
type: execute
wave: 4
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified: [knowledge/README.md, knowledge/boundaries.md, AGENTS.md]
autonomous: true
requirements: [DOC-04, DOC-03, DOC-05, PAT-01]
must_haves:
  truths:
    - "knowledge/README.md индекс ссылается на style/testing/architecture/patterns; статус «запланировано (Phase 3)» снят; порядок чтения обновлён (no broken links)"
    - "knowledge/boundaries.md карта владения регистрирует testing.md, architecture.md, patterns.md (style.md уже там) со статусом «существует»"
    - "AGENTS.md таблица статусов синхронизирована: 4 дока Phase 3 — со ссылками, не «запланировано» (no-phantom для статусов; RESEARCH Open Question 2)"
    - "Все markdown-ссылки в knowledge/*.md ведут на существующие файлы (link integrity)"
    - "Правило языка кода зарегистрировано как канон только в style.md (uniqueness — нет дубля в других топик-доках)"
  artifacts:
    - path: "knowledge/README.md"
      provides: "Обновлённый индекс с 4 ссылками без статуса «запланировано»"
      contains: "patterns.md"
    - path: "knowledge/boundaries.md"
      provides: "Карта владения с 3 новыми зарегистрированными фактами"
      contains: "architecture.md"
  key_links:
    - from: "knowledge/README.md"
      to: "knowledge/style.md, testing.md, architecture.md, patterns.md"
      via: "markdown-ссылки в индексе и порядке чтения"
      pattern: "\\[(style|testing|architecture|patterns)\\.md\\]"
    - from: "knowledge/boundaries.md"
      to: "knowledge/testing.md, architecture.md, patterns.md"
      via: "строки карты владения фактами"
      pattern: "(testing|architecture|patterns)\\.md"
---

<objective>
Интеграция: вписать 4 новых дока в индекс (README.md), карту владения фактами (boundaries.md) и таблицу статусов точки входа (AGENTS.md) — БЕЗ битых ссылок (ссылки появляются только теперь, когда все 4 файла существуют).

Purpose: Снять статус «запланировано (Phase 3)» и добавить ссылки/строки владения, чтобы индекс и карта владения отражали реальность (no-phantom для ссылок и статусов). Это финальная волна — гарантирует link integrity и uniqueness каноном.
Output: правки README.md (индекс + порядок чтения), boundaries.md (+3 строки карты владения), AGENTS.md (статусы 4 доков).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conventions-architecture-docs/03-CONTEXT.md
@.planning/phases/03-conventions-architecture-docs/03-VALIDATION.md
@knowledge/authoring.md
@knowledge/README.md
@knowledge/boundaries.md
@AGENTS.md
</context>

<artifacts_this_phase_produces>
This plan edits (no new files): `knowledge/README.md` (index links + reading order), `knowledge/boundaries.md` (+3 ownership-map rows), `AGENTS.md` (status table sync for the 4 Phase 3 docs). Requires style.md/testing.md/architecture.md/patterns.md to already exist (Waves 1–3).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Обновить knowledge/README.md (индекс + порядок чтения)</name>
  <files>knowledge/README.md</files>
  <read_first>
    - knowledge/README.md — текущий индекс (строки 32–35 со статусом «запланировано (Phase 3)»; строка 45 порядка чтения с курсивом «(Phase 3)»)
    - knowledge/authoring.md — no broken links (ссылка только на существующий файл)
    - knowledge/style.md, knowledge/testing.md, knowledge/architecture.md, knowledge/patterns.md — должны уже существовать (Waves 1–3)
  </read_first>
  <action>
    В таблице «Индекс» заменить back-tick-имена `style.md`/`testing.md`/`architecture.md`/`patterns.md` на markdown-ссылки `[style.md](style.md)` и т.д.; в колонке «Статус» сменить «запланировано (Phase 3)» → «существует».
    Если architecture.md/patterns.md были раздроблены (под-топики, напр. architecture-events.md) — добавить и эти ссылки/строки.
    В разделе «Порядок чтения» (строка ~45) снять курсив-пометку «*(Phase 3)*» и сделать имена 4 доков кликабельными ссылками.
    НЕ ломать существующие ссылки; НЕ добавлять ссылку на ещё не созданный файл.
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && grep -qE '\[style\.md\]' knowledge/README.md && grep -qE '\[testing\.md\]' knowledge/README.md && grep -qE '\[architecture\.md\]' knowledge/README.md && grep -qE '\[patterns\.md\]' knowledge/README.md && ! grep -qE 'запланировано \(Phase 3\)' knowledge/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - README содержит ссылки: `grep -E '\[(style|testing|architecture|patterns)\.md\]' knowledge/README.md` находит все четыре.
    - Статус «запланировано (Phase 3)» снят: `! grep -qE 'запланировано \(Phase 3\)' knowledge/README.md`.
    - Link integrity: для каждой `[X.md](X.md)` → `test -f knowledge/X.md` истинно.
  </acceptance_criteria>
  <done>README.md индекс и порядок чтения ссылаются на 4 существующих дока, без статуса «запланировано (Phase 3)», без битых ссылок.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить knowledge/boundaries.md (карта владения фактами)</name>
  <files>knowledge/boundaries.md</files>
  <read_first>
    - knowledge/boundaries.md — текущая карта владения (L61–67); L67 уже резервирует style.md за языком кода со статусом «запланировано (Phase 3)»
    - knowledge/authoring.md — pointer-over-copy, no broken links
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — §Architectural Responsibility Map (какой факт чей канон)
  </read_first>
  <action>
    В таблице «Карта владения фактами»:
      - Строку style.md (L67) перевести в статус «существует» и сделать имя ссылкой `[style.md](style.md)`.
      - Добавить 3 строки:
        - «Конвенции тестов: Ginkgo v2+Gomega, спек-структура, мокинг портов» → `[testing.md](testing.md)` → существует.
        - «Слои/направление импортов, Execute, query-lite, UnitOfWork, outbox+relay, PullEvents, MUST NOT CQRS» → `[architecture.md](architecture.md)` → существует.
        - «Пошаговые рецепты add use case/query/aggregate/repository» → `[patterns.md](patterns.md)` → существует.
    Сохранить принцип «один факт = один канон» (pointer-over-copy). Ссылки — только на существующие файлы.
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && grep -q 'testing.md' knowledge/boundaries.md && grep -q 'architecture.md' knowledge/boundaries.md && grep -q 'patterns.md' knowledge/boundaries.md && grep -q 'style.md' knowledge/boundaries.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Карта владения регистрирует все 4: `grep -q 'testing.md'` И `grep -q 'architecture.md'` И `grep -q 'patterns.md'` И `grep -q 'style.md'` зелёные.
    - Link integrity: каждая `[X.md](X.md)` в boundaries.md → `test -f knowledge/X.md`.
    - style.md строка больше не «запланировано»: ручной просмотр — статус «существует».
  </acceptance_criteria>
  <done>boundaries.md карта владения регистрирует testing/architecture/patterns (style — «существует»), без битых ссылок.</done>
</task>

<task type="auto">
  <name>Task 3: Синхронизировать таблицу статусов в AGENTS.md</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md — таблица «База знаний» (L32–40); L37 перечисляет style/testing/architecture/patterns со статусом «запланировано (Phase 3)»
    - knowledge/README.md — образец формата ссылок/статусов
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — Open Question 2 (синхронность статусов AGENTS.md обязательна, no-phantom для статусов)
  </read_first>
  <action>
    В таблице «База знаний» AGENTS.md строку с `style.md`, `testing.md`, `architecture.md`, `patterns.md` (L37) обновить: сделать имена кликабельными ссылками (`[knowledge/style.md](knowledge/style.md)` и т.д.) и сменить статус «запланировано (Phase 3)» → «есть».
    НЕ трогать строку Phase 2 доков сверх скоупа этой фазы, если она вне Phase 3 (это отдельная неточность, не предмет DOC-03/04/05/PAT-01) — допускается оставить как есть; фокус — снять «запланировано (Phase 3)» для 4 наполненных доков.
    Ссылки — только на существующие файлы (no broken links).
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && grep -qE 'knowledge/style\.md|knowledge/testing\.md|knowledge/architecture\.md|knowledge/patterns\.md' AGENTS.md && ! grep -qE 'запланировано \(Phase 3\)' AGENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - AGENTS.md ссылается на наполненные доки: `grep -qE 'knowledge/(style|testing|architecture|patterns)\.md' AGENTS.md`.
    - Статус Phase 3 снят: `! grep -qE 'запланировано \(Phase 3\)' AGENTS.md`.
    - Link integrity: ссылки ведут на существующие `knowledge/*.md`.
  </acceptance_criteria>
  <done>AGENTS.md таблица статусов отражает реальность: 4 дока Phase 3 со ссылками, без «запланировано (Phase 3)».</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| индекс/статус → reader | Читатель доверяет статусу «существует» и ссылке; битая ссылка или ложный статус «запланировано» рассинхронизирует базу с реальностью |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Repudiation/Spoofing | README/boundaries/AGENTS статусы и ссылки | mitigate | no-phantom для ссылок/статусов (Pitfall 4): ссылка добавляется только когда файл существует (эта волна — последняя, depends_on все 4 дока); статус «существует» проставляется только реально созданным докам; link-check над всеми knowledge/*.md |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Документационная фаза — пакеты НЕ устанавливаются (RESEARCH §Package Legitimacy Audit; install — Phase 4) |
</threat_model>

<verification>
Полный набор cross-cutting проверок из 03-VALIDATION.md (запускать из корня репо):
- README индекс обновлён: `grep -E '\[(style|testing|architecture|patterns)\.md\]' knowledge/README.md`.
- Ownership-map: `grep -q 'testing.md' && grep -q 'architecture.md' && grep -q 'patterns.md'` в boundaries.md.
- AGENTS.md синхронизирован.
- Link integrity: для каждой `[..](X.md)` в knowledge/*.md → `test -f knowledge/X.md` (нет битых).
- Uniqueness языка кода: `! grep -rl 'комментарии.*на русском\|RU комментарии' knowledge/ | grep -v 'style.md\|boundaries.md\|README.md'` — правило языка только в каноне.
- Размер всех 4 доков ≤ ~200 строк: `for f in style testing architecture patterns; do wc -l knowledge/$f.md; done`.
- Dimension-8 (validation): фаза зелёная, когда ВСЕ presence/uniqueness/link-integrity/ownership/README-проверки из VALIDATION.md проходят.
</verification>

<success_criteria>
Все 4 дока вписаны в индекс, карту владения и таблицу статусов без битых ссылок; uniqueness каноном соблюдён; cross-cutting проверки 03-VALIDATION.md зелёные.
</success_criteria>

<output>
Create `.planning/phases/03-conventions-architecture-docs/03-05-SUMMARY.md` when done.
</output>
