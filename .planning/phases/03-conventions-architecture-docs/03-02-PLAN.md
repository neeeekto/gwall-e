---
phase: 03-conventions-architecture-docs
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified: [knowledge/testing.md]
autonomous: true
requirements: [DOC-03]
must_haves:
  truths:
    - "knowledge/testing.md существует и фиксирует Ginkgo v2 + Gomega как канон тестов (D-09; Success Criteria 2)"
    - "MUST на каркас: suite-бутстрап RegisterFailHandler(Fail) + RunSpecs(t, \"...\"), EN-комментарии в тестах, *_test.go рядом с кодом (D-09)"
    - "SHOULD на структуру: Describe/Context/It, DescribeTable (D-09)"
    - "Мокинг портов через mockery (vektra/mockery) + Gomega-ассерты; обвязка помечена planned Phase 4 (D-10, no-phantom)"
    - "EN-комментарии — короткая ссылка на style.md (не дубль); команды прогона — ссылка на build.md (не дубль)"
  artifacts:
    - path: "knowledge/testing.md"
      provides: "Конвенции тестов: Ginkgo v2+Gomega, спек-структура, mockery-конвенция"
      min_lines: 60
      contains: "RunSpecs"
  key_links:
    - from: "knowledge/testing.md"
      to: "knowledge/style.md"
      via: "ссылка на канон EN-комментариев"
      pattern: "style.md"
    - from: "knowledge/testing.md"
      to: "knowledge/build.md"
      via: "ссылка на команды прогона тестов"
      pattern: "build.md"
---

<objective>
Создать `knowledge/testing.md` — канон конвенций тестов (DOC-03): Ginkgo v2 + Gomega, suite-бутстрап, структура спеков, мокинг портов через mockery.

Purpose: Зафиксировать MUST-каркас (реальный, из pkg/http) и SHOULD-структуру тестов, а также выбор mockery как канонического генератора моков портов (UnitOfWork, repositories) с Gomega-ассертами — без выдачи mockery за «уже настроенный» (no-phantom; обвязка planned Phase 4).
Output: `knowledge/testing.md` (~60–150 строк), ссылается на style.md (EN-комментарии) и build.md (команды), с forward-enforcement-метками.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conventions-architecture-docs/03-CONTEXT.md
@.planning/phases/03-conventions-architecture-docs/03-VALIDATION.md
@knowledge/authoring.md
@knowledge/build.md
@knowledge/style.md
@pkg/http/http_test.go
@pkg/http/middlewares_test.go
@pkg/go.mod
</context>

<artifacts_this_phase_produces>
This plan produces: `knowledge/testing.md` (NEW). Depends on style.md (03-01) for the EN-comments cross-link.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Написать knowledge/testing.md (Ginkgo v2 + Gomega + mockery-конвенция)</name>
  <files>knowledge/testing.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт (MUST/SHOULD/WON'T, парность, pointer-over-copy, no-phantom, статус enforcement)
    - knowledge/style.md — канон EN-комментариев (ссылаться, НЕ копировать); создан в 03-01
    - knowledge/build.md — команды прогона тестов `cd pkg && go test ./...` (ссылаться, НЕ копировать)
    - pkg/http/http_test.go — РЕАЛЬНЫЙ suite-бутстрап: `RegisterFailHandler(Fail)` + `RunSpecs(t, "HTTP Package Suite")`, dot-imports `. "github.com/onsi/ginkgo/v2"` / `. "...gomega"`
    - pkg/http/middlewares_test.go — РЕАЛЬНЫЙ спек: Describe/Context/It, Expect(...).To(...), EN-комментарии
    - pkg/go.mod — pinned ginkgo v2.23.4, gomega v1.38.0
    - .planning/phases/03-conventions-architecture-docs/03-CONTEXT.md — D-09 (MUST каркас + SHOULD структура), D-10 (mockery + Gomega; обвязка planned Phase 4), D-11 (enforcement-метки)
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — Code Examples §2 (реальный suite-бутстрап), §3 (mockery v3 + Gomega конвенция, GinkgoT(), expecter API — помечать planned Phase 4), Pitfall 5 (mockery как «уже настроено» — WON'T)
  </read_first>
  <action>
    Создать `knowledge/testing.md` (русская проза; EN тех-термины/код), следуя authoring.md.
    Шапка-абзац: testing.md — канон конвенций тестов; фреймворк — Ginkgo v2 + Gomega (версии pinned в pkg/go.mod — НЕ дублировать номера как жёсткий факт, упомянуть «pinned в pkg/go.mod»). Команды прогона — ссылка на [build.md](build.md). EN-комментарии в тестах — ссылка на канон [style.md](style.md), без повтора правила.
    MUST (каркас, D-09) — цитировать РЕАЛЬНЫЙ эталон pkg/http:
      - MUST suite-бутстрап: `RegisterFailHandler(Fail)` + `RunSpecs(t, "<Suite Name>")` в `TestXxx(t *testing.T)`. Короткий реальный сниппет (с пометкой Source: pkg/http/http_test.go — реальный, компилируется).
      - MUST dot-imports `. "github.com/onsi/ginkgo/v2"` и `. "github.com/onsi/gomega"`.
      - MUST `*_test.go` рядом с кодом (в том же пакете / external `_test`).
      - MUST комментарии в тестах — английские → ссылка на style.md.
    SHOULD (структура, D-09):
      - SHOULD `Describe`(юнит) / `Context`(сценарий) / `It`(утверждение); `DescribeTable` для табличных кейсов.
    Мокинг портов (D-10):
      - SHOULD/MUST мокать порты (`UnitOfWork`, repositories) через mockery (vektra/mockery) + ассертить результат через Gomega (`Expect(...).To(...)`). Показать конвенцию (expecter API `mock.EXPECT().Method().Return()`, `NewMockX(GinkgoT())` авто-Cleanup) как КОНВЕНЦИЮ.
      - Явно пометить: установка/`.mockery.yaml`/`go:generate` — `planned: Phase 4`; mockery в репо ЕЩЁ НЕТ — НЕ показывать рабочую команду генерации как проверенную (Pitfall 5, no-phantom). Плейсхолдер `Order` (D-05) в иллюстративном mock-сниппете, помеченном «иллюстрация / целевой вид».
    Forward-enforcement-метки (D-11) на механизируемых правилах: напр. структура спеков — `convention-only`; генерация моков — `planned: Phase 4 (go:generate)`.
    НЕ дублировать команды (build.md) и язык (style.md) — ссылки. НЕ жёстко прописывать версию Go (RESEARCH ⚠️ расхождение 1.23.6/1.24.6) — ссылаться на structure.md/build.md.
    Цель размера ≤ ~200 строк.
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && test -f knowledge/testing.md && grep -qi 'ginkgo' knowledge/testing.md && grep -qi 'gomega' knowledge/testing.md && grep -qi 'mockery' knowledge/testing.md && grep -q 'RegisterFailHandler' knowledge/testing.md && grep -q 'RunSpecs' knowledge/testing.md && grep -q 'style.md' knowledge/testing.md && grep -q 'build.md' knowledge/testing.md && grep -qiE 'planned.*Phase 4|convention-only|hook|CI-gated' knowledge/testing.md && grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/testing.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f knowledge/testing.md` истинно.
    - DOC-03 presence: `grep -qi 'ginkgo'` И `grep -qi 'gomega'` И `grep -qi 'mockery'` зелёные.
    - DOC-03 каркас: `grep -q 'RegisterFailHandler'` И `grep -q 'RunSpecs'` зелёные.
    - Cross-link (no-dup): `grep -q 'style.md'` (EN-комментарии) И `grep -q 'build.md'` (команды) — ссылки присутствуют.
    - D-10 no-phantom: mockery-обвязка помечена `planned`: `grep -qiE 'planned.*Phase 4' knowledge/testing.md`.
    - D-11: `grep -qiE 'convention-only|planned.*Phase 4|CI-gated|hook' knowledge/testing.md`.
    - Authoring теги: `grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/testing.md`.
    - Размер: `wc -l knowledge/testing.md` ≤ ~200.
    - Link integrity (предв., финал — в Wave 5): ссылки `[style.md](style.md)`/`[build.md](build.md)` → `test -f knowledge/style.md` И `test -f knowledge/build.md`.
  </acceptance_criteria>
  <done>knowledge/testing.md существует, проходит DOC-03 presence + каркас-проверки, ссылается на style.md и build.md, mockery помечен planned Phase 4, ≤ ~200 строк.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reader (ИИ/команда) | Читатель доверяет правилу; phantom-конвенция (mockery «уже настроен») провоцирует ложные действия |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Repudiation/Spoofing | testing.md mockery-конвенция | mitigate | no-phantom (Pitfall 5): mockery не установлен — обвязка помечена `planned: Phase 4`; рабочая команда генерации НЕ подаётся как проверенная; mock-сниппет помечен «иллюстрация» на плейсхолдере Order |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Документационная фаза — пакеты НЕ устанавливаются (RESEARCH §Package Legitimacy Audit: ginkgo/gomega уже pinned и компилируются; mockery install — Phase 4). Аудит-таблица присутствует в RESEARCH.md |
</threat_model>

<verification>
- DOC-03 presence + каркас-команды из 03-VALIDATION.md зелёные.
- mockery помечен planned (no-phantom).
- EN-комментарии и команды — ссылки, не копии (uniqueness — финальная проверка Wave 5).
- Размер ≤ ~200 строк; теги силы у каждого правила.
- Dimension-8 (validation): фаза-частично-зелёная при зелёных DOC-03 shell-проверках.
</verification>

<success_criteria>
knowledge/testing.md создан, удовлетворяет DOC-03 (Success Criteria 2 ROADMAP), следует authoring-стандарту, корректно ссылается на style.md/build.md, mockery документирован как конвенция planned Phase 4.
</success_criteria>

<output>
Create `.planning/phases/03-conventions-architecture-docs/03-02-SUMMARY.md` when done.
</output>
