---
phase: 03-conventions-architecture-docs
plan: 04
type: execute
wave: 3
depends_on: ["03-03", "03-01"]
files_modified: [knowledge/patterns.md]
autonomous: true
requirements: [PAT-01]
must_haves:
  truths:
    - "knowledge/patterns.md существует и даёт копируемые пошаговые рецепты, согласованные с architecture.md (D-04; Success Criteria 4)"
    - "Покрыты 4 рецепта: add use case, add query (read-side DTO), add aggregate (+ фабрика, PullEvents), add repository (Mongo-реализация порта + UnitOfWork) (D-03)"
    - "Каждый рецепт — вертикальный срез до wiring: struct + Execute → порты/репозиторий → composition root (app, ручной DI) → gRPC-адаптер в api (D-02)"
    - "patterns.md ссылается на architecture.md за правилами, не дублирует их (D-04, pointer-over-copy)"
    - "Все Go-сниппеты ИЛЛЮСТРАТИВНЫЕ на плейсхолдере Order, помечены «целевой вид / иллюстрация — не из компилируемого файла» (D-01, D-05, no-phantom)"
  artifacts:
    - path: "knowledge/patterns.md"
      provides: "Копируемые рецепты add use case/query/aggregate/repository"
      min_lines: 80
      contains: "architecture.md"
  key_links:
    - from: "knowledge/patterns.md"
      to: "knowledge/architecture.md"
      via: "ссылка на архитектурные правила (D-04)"
      pattern: "architecture.md"
---

<objective>
Создать `knowledge/patterns.md` — копируемый каталог рецептов (PAT-01): как добавить use case / query / aggregate / repository.

Purpose: Дать пошаговые how-to (вертикальный срез до wiring), которые ссылаются на инварианты architecture.md, а не дублируют их (D-04, pointer-over-copy). «Копируемость» обеспечивается иллюстративными помеченными сниппетами на едином плейсхолдере `Order` (D-01, no-phantom).
Output: `knowledge/patterns.md` (~80–200 строк), 4 рецепта, ссылки на architecture.md (правила) и style.md (typed IDs/ошибки/DTO→домен), помеченные иллюстративные сниппеты.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-conventions-architecture-docs/03-CONTEXT.md
@.planning/phases/03-conventions-architecture-docs/03-VALIDATION.md
@knowledge/authoring.md
@knowledge/architecture.md
@knowledge/style.md
@knowledge/boundaries.md
</context>

<artifacts_this_phase_produces>
This plan produces: `knowledge/patterns.md` (NEW). Depends on architecture.md (03-03) as the rules canon it links to, and style.md (03-01) for code-level rules.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Написать knowledge/patterns.md (4 рецепта, вертикальный срез, ссылки на architecture.md)</name>
  <files>knowledge/patterns.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт (MUST/SHOULD/WON'T, парность, pointer-over-copy, размер ~150–200, no-phantom, enforcement-статус)
    - knowledge/architecture.md — канон правил, на который patterns.md ССЫЛАЕТСЯ (слои/импорты, Execute, UnitOfWork, query-lite, outbox+relay, фабрики+PullEvents); создан 03-03. Использовать ТО ЖЕ имя плейсхолдера Order
    - knowledge/style.md — typed IDs / sentinel-vs-wrapped errors / DTO→домен (ссылаться, НЕ копировать); создан 03-01
    - knowledge/boundaries.md — no-phantom (сниппеты не выдавать за существующий файл/сервис)
    - .planning/PROJECT.md — Key Decisions (целевая раскладка слоёв; app=composition root ручной DI; cmd=main; gRPC-адаптеры в api)
    - .planning/phases/03-conventions-architecture-docs/03-CONTEXT.md — D-01 (иллюстративные помеченные сниппеты), D-02 (вертикальный срез до wiring), D-03 (4 рецепта), D-04 (рецепты ссылаются на architecture.md, не дублируют), D-05 (плейсхолдер Order)
    - .planning/phases/03-conventions-architecture-docs/03-RESEARCH.md — Code Examples §1 (иллюстративные сниппеты RegisterOrderUseCase/UnitOfWork/PullEvents на Order), Pattern 2 (правила vs рецепты — ссылка на architecture.md), Pattern 3 (единый плейсхолдер), Pitfall 1 (phantom-сниппет — метить), §Architecture Patterns B (вертикальный поток gRPC→usecase→uow→repo→outbox)
  </read_first>
  <action>
    Создать `knowledge/patterns.md` (русская проза; EN тех-термины/код), следуя authoring.md.
    Шапка-абзац: patterns.md — копируемые рецепты «как добавить слой». За ПРАВИЛАМИ (инварианты/why) — ссылка на [architecture.md](architecture.md); patterns.md правила НЕ дублирует (D-04). Все сниппеты ИЛЛЮСТРАТИВНЫЕ (метка «целевой вид / иллюстрация — не из компилируемого файла»), плейсхолдер `Order` (D-05).
    4 рецепта (D-03), КАЖДЫЙ — вертикальный срез до wiring включительно (D-02): шаги «struct + Execute → объявить/использовать порты и репозиторий → зарегистрировать в composition root (`app`, ручной DI) → выставить gRPC-адаптер в `api`». За транзакционной границей / событиями / слоями — ссылка на конкретные разделы architecture.md (напр. «правило транзакционной границы см. architecture.md §UnitOfWork»):
      1. Add use case (write-side): создать `RegisterOrderUseCase` struct + `Execute`; маппинг DTO→домен (ссылка style.md); запись в `uow.Do(...)` (ссылка architecture.md §UnitOfWork); регистрация в app; gRPC-адаптер в api.
      2. Add query (read-side, DTO): query-lite, читает напрямую в DTO мимо агрегатов (ссылка architecture.md §query-lite); wiring в app + api.
      3. Add aggregate (+ фабрика, PullEvents): фабрика `NewOrder(...)` с инвариантами; накопление событий → `PullEvents()` (ссылка architecture.md §фабрики/PullEvents/outbox).
      4. Add repository (Mongo-реализация порта + UnitOfWork): реализация порта `OrderRepository` в `repositories/` (Mongo); участие в одной tx через UnitOfWork; outbox-append в той же tx (ссылка architecture.md §UnitOfWork/outbox); wiring в app.
    No-phantom (D-01, Pitfall 1): каждый сниппет помечен «иллюстрация / целевой вид — не из компилируемого файла»; НЕ ссылаться на несуществующие пути типа `services/.../order.go` как на существующие.
    НЕ дублировать архитектурные правила (architecture.md) и правила языка/ошибок (style.md) — только ссылки. Теги силы у нормативных шагов; где уместно — enforcement-метки (D-11).
    Размер: цель ≤ ~200 строк; при превышении — дробить (authoring.md) и связать ссылкой; отметить в SUMMARY + индексе (Wave 5).
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && test -f knowledge/patterns.md && [ "$(grep -c 'architecture.md' knowledge/patterns.md)" -gt 0 ] && grep -qiE 'use case|usecase' knowledge/patterns.md && grep -qiE 'query' knowledge/patterns.md && grep -qiE 'aggregate|агрегат' knowledge/patterns.md && grep -qiE 'repository|репозитор' knowledge/patterns.md && grep -qiE 'иллюстрац|целевой вид' knowledge/patterns.md && grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/patterns.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f knowledge/patterns.md` истинно.
    - PAT-01 ссылка на канон: `grep -c 'architecture.md' knowledge/patterns.md` > 0.
    - PAT-01 4 рецепта присутствуют: `grep -qiE 'use case|usecase'` И `grep -qiE 'query'` И `grep -qiE 'aggregate|агрегат'` И `grep -qiE 'repository|репозитор'` — все зелёные.
    - D-01 no-phantom: `grep -qiE 'иллюстрац|целевой вид' knowledge/patterns.md`.
    - D-02 вертикальный срез: каждый рецепт упоминает wiring/composition root/api-адаптер — `grep -qiE 'composition root|app|gRPC|api' knowledge/patterns.md` (ручной просмотр полноты среза).
    - Authoring теги: `grep -qiE '\bMUST\b|\bSHOULD\b|WON.T' knowledge/patterns.md`.
    - Размер: `wc -l knowledge/patterns.md` ≤ ~200 (иначе дробление + индекс Wave 5).
    - Link integrity (предв.): `[architecture.md](architecture.md)` → `test -f knowledge/architecture.md` И `[style.md](style.md)` (если есть) → `test -f knowledge/style.md`.
    - Плейсхолдер Order, нет host/VM/owner как агрегата (ручной просмотр).
  </acceptance_criteria>
  <done>knowledge/patterns.md существует, 4 рецепта вертикальным срезом, ссылается на architecture.md (правила) и style.md, сниппеты помечены иллюстративными, проходит PAT-01 presence, ≤ ~200 строк.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doc → reader (ИИ/команда) | Читатель копирует рецепт; phantom-сниппет (выдан за существующий файл) провоцирует «доделать» несуществующий код |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Repudiation/Spoofing | patterns.md рецепты/сниппеты | mitigate | no-phantom (D-01, Pitfall 1): каждый сниппет помечен «целевой вид / иллюстрация — не из компилируемого файла», плейсхолдер Order; нет ссылок на несуществующие пути services/.../order.go; рецепты ссылаются на architecture.md за правилами (не дублируют) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | Документационная фаза — пакеты НЕ устанавливаются (RESEARCH §Package Legitimacy Audit; install — Phase 4) |
</threat_model>

<verification>
- PAT-01 presence + 4 рецепта команды из 03-VALIDATION.md зелёные.
- no-phantom метки «иллюстрация» (Manual-Only из VALIDATION.md).
- Архитектурные правила НЕ дублированы (uniqueness — финал Wave 5); patterns ссылается на architecture.md.
- Размер ≤ ~200 строк.
- Dimension-8 (validation): фаза-частично-зелёная при зелёных PAT-01 shell-проверках.
</verification>

<success_criteria>
knowledge/patterns.md создан, удовлетворяет PAT-01 (Success Criteria 4 ROADMAP), 4 рецепта вертикальным срезом, согласованы с architecture.md через ссылки.
</success_criteria>

<output>
Create `.planning/phases/03-conventions-architecture-docs/03-04-SUMMARY.md` when done.
</output>
