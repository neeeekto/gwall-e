---
phase: 06-inventory
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - services/inventory/internal/domain/id.go
  - services/inventory/internal/domain/aggregate.go
  - services/inventory/internal/domain/events.go
  - services/inventory/internal/domain/errors.go
  - services/inventory/internal/domain/ports.go
  - services/inventory/internal/domain/id_test.go
  - services/inventory/internal/domain/aggregate_test.go
  - services/inventory/internal/domain/domain_suite_test.go
  - .mockery.yaml
  - services/inventory/go.mod
autonomous: true
requirements: [INV-03, EVT-01, EVT-02, INV-08, INV-10, SVC-01]
must_haves:
  truths:
    - "Каждый агрегат имеет типизированный ID-VO над uuid.UUID; компилятор различает HostID/ProjectID/DCID/ModuleID/RackID (D-05/INV-03)"
    - "aggregateBase копит голые DomainEvent через record() (version++ в одной точке) и сливает их через PullEvents() (отдаёт-и-очищает) (D-14)"
    - "EventEnvelope несёт eventId/entityId/eventType/version/occurredAt/actor{id,source} как форму домена; envelope-мета НЕ заполняется в агрегате (D-14/D-15/EVT-02)"
    - "Порты UnitOfWork/Outbox/HostRepository/ProjectRepository/локационные repo/ActiveHostByFQDN/HostsInProject/MatchAdvisor/Clock/IDGenerator объявлены в domain; реальных реализаций нет (D-02)"
    - "mockery v3 генерирует моки доменных портов; go build/vet зелёные"
  artifacts:
    - path: "services/inventory/internal/domain/id.go"
      provides: "5 типизированных ID-VO над uuid.UUID + фабрики/parse/zero"
      contains: "type HostID"
    - path: "services/inventory/internal/domain/aggregate.go"
      provides: "встраиваемый aggregateBase: record/PullEvents/Version"
      contains: "func (b *aggregateBase) PullEvents"
    - path: "services/inventory/internal/domain/events.go"
      provides: "DomainEvent интерфейс + EventEnvelope + Actor"
      contains: "type EventEnvelope"
    - path: "services/inventory/internal/domain/errors.go"
      provides: "доменные sentinel-ошибки + типизированные конфликты"
      contains: "ErrFQDNConflict"
    - path: "services/inventory/internal/domain/ports.go"
      provides: "все доменные порты (интерфейсы)"
      contains: "type UnitOfWork interface"
    - path: ".mockery.yaml"
      provides: "mockery v3 packages-записи на доменные порты"
      contains: "internal/domain"
  key_links:
    - from: "services/inventory/internal/domain/aggregate.go"
      to: "events.go DomainEvent"
      via: "record(e DomainEvent) копит факты"
      pattern: "record\\(.*DomainEvent"
    - from: ".mockery.yaml"
      to: "services/inventory/internal/domain ports"
      via: "make generate-mocks → internal/domain/mocks"
      pattern: "internal/domain"
---

<objective>
Заложить доменное ядро Inventory — примитивы, на которых стоят все агрегаты и usecases: типизированные ID-VO (D-05), встраиваемый aggregateBase с накоплением событий (D-14), форма DomainEvent + EventEnvelope + Actor (D-15/EVT-02), доменные sentinel-ошибки и типизированные конфликты, и полный набор портов (D-02). Плюс Wave-0 тест-инфраструктура: расширить `.mockery.yaml` реальными доменными портами и сгенерировать моки, бутстрап Ginkgo-suite для пакета domain.

Purpose: это фундамент фазы — без ID-VO/aggregateBase/портов/событий ни один агрегат и usecase не компилируется. Форма envelope и портов фиксирует контракт, который Phase 7 свапает на Mongo без изменения usecase (D-02), а Phase 8 — на protobuf.
Output: `internal/domain/{id,aggregate,events,errors,ports}.go` + suite-бутстрап + прямые unit-тесты на ID-VO и aggregateBase + расширенный `.mockery.yaml` + сгенерированные моки портов.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory/06-CONTEXT.md
@.planning/phases/06-inventory/06-RESEARCH.md
@.planning/phases/06-inventory/06-PATTERNS.md
@knowledge/architecture.md
@knowledge/style.md
@knowledge/patterns.md
@knowledge/testing.md
@services/inventory/internal/example/provisioner.go
@services/inventory/internal/example/provisioner_test.go
@.mockery.yaml
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Typed ID-VO types (domain):** `HostID`, `ProjectID`, `DCID`, `ModuleID`, `RackID` — each `struct{ v uuid.UUID }` with `NewXID()`, `ParseXID(string) (XID, error)`, `String() string`, `IsZero() bool`.
**Aggregate base:** `aggregateBase` (embedded), `DomainEvent` interface (`EventType() string`, `EntityID() string`).
**Event envelope (domain shape):** `EventEnvelope` (`EventID`, `EntityID`, `EventType`, `Version`, `OccurredAt`, `Actor`, `Payload`), `Actor` (`ID`, `Source`).
**Sentinel errors / typed conflicts:** `ErrInvalidID`, `ErrInvalidTransition`, `ErrAlreadyDecommissioned`, `ErrFQDNConflict{FQDN, ExistingID, Candidates}`, `ErrProjectNotEmpty{ProjectID, HostCount}` (+ `ErrPositionOccupied` if position-uniqueness adopted in 06-02).
**Port interfaces (domain):** `UnitOfWork`, `Outbox`, `HostRepository`, `ProjectRepository`, `DCRepository`, `ModuleRepository`, `RackRepository`, `ActiveHostByFQDN`, `HostsInProject`, `MatchAdvisor`, `Clock`, `IDGenerator`.
**Mock names (generated):** `internal/domain/mocks/{HostRepository,ProjectRepository,DCRepository,ModuleRepository,RackRepository,UnitOfWork,Outbox,ActiveHostByFQDN,HostsInProject,MatchAdvisor,Clock,IDGenerator}.go`.
**New files:** `internal/domain/{id,aggregate,events,errors,ports,id_test,aggregate_test,domain_suite_test}.go`; modified `.mockery.yaml`, `services/inventory/go.mod`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Типизированные ID-VO + sentinel-ошибки</name>
  <files>services/inventory/internal/domain/id.go, services/inventory/internal/domain/errors.go, services/inventory/internal/domain/id_test.go, services/inventory/internal/domain/domain_suite_test.go, services/inventory/go.mod</files>
  <read_first>
    - services/inventory/internal/example/provisioner.go (typed-id + sentinel precedent, ExampleID/ErrExampleProvisionFailed)
    - knowledge/style.md (§«Типизированные ID» строки ~51-64; §«Sentinel vs обёрнутые» ~66-84; язык: RU-комментарии, EN-идентификаторы/строки ошибок)
    - .planning/phases/06-inventory/06-RESEARCH.md (Pattern 1 ID-VO; §errors.go; Don't Hand-Roll — uuid.New/uuid.Parse)
    - .planning/phases/06-inventory/06-PATTERNS.md (§id.go — D-05 deviation struct-over-uuid; §errors.go typed conflicts)
  </read_first>
  <behavior>
    - HostID/ProjectID/DCID/ModuleID/RackID — каждый `struct{ v uuid.UUID }`; NewXID() даёт непустой ID; два вызова дают разные ID (INV-03)
    - ParseXID валидной uuid-строки восстанавливает равный ID (round-trip через String()); ParseXID битой строки → ошибка, обёрнутая %w в ErrInvalidID, сравнимая через errors.Is
    - IsZero() == true для zero-value ID-VO, false для сгенерированного
    - Компилятор различает типы: присваивание HostID переменной ProjectID не компилируется (документируется тестом-комментарием, не выполняется)
  </behavior>
  <action>
    Создай `internal/domain/id.go` (package domain): пять типизированных ID-VO `HostID`/`ProjectID`/`DCID`/`ModuleID`/`RackID`, каждый `struct{ v uuid.UUID }` (форма D-05 «обёртка над uuid.UUID» — переопределяет `type X string` прецедент ExampleID). На каждый ID: фабрика `NewHostID() HostID { return HostID{v: uuid.New()} }` (v4 random, INV-03 непереиспользуемый), `ParseHostID(s string) (HostID, error)` через `uuid.Parse`, оборачивая ошибку `fmt.Errorf("parse host id %q: %w", s, ErrInvalidID)`, метод `String() string` (для будущего Kafka-ключа Phase 8 и логов) и `IsZero() bool` (`id.v == uuid.Nil`, guard в инвариантах). Импорт `github.com/google/uuid` (уже indirect в go.mod). Комментарии русские, идентификаторы/строки английские (style.md).
    Создай `internal/domain/errors.go` (package domain): sentinel `var ErrInvalidID = errors.New("invalid id")`, `var ErrInvalidTransition = errors.New("invalid lifecycle transition")`, `var ErrAlreadyDecommissioned = errors.New("host already decommissioned")`; типизированные конфликты как struct, реализующие `error`: `ErrFQDNConflict{FQDN string; ExistingID HostID; Candidates []HostID}` (метод `Error() string`, EN-строка) и `ErrProjectNotEmpty{ProjectID ProjectID; HostCount int}`. Это контракт под D-11/Pitfall 7 — НЕ сырой DB E11000; сравнимы через errors.As. (Если 06-02 примет инвариант позиции — там добавится `ErrPositionOccupied`; объяви его здесь как заглушку-struct, если решишь зафиксировать форму сразу — на усмотрение executor'а 06-02.)
    Создай `internal/domain/domain_suite_test.go`: Ginkgo-бутстрап `func TestDomainSuite(t *testing.T) { RegisterFailHandler(Fail); RunSpecs(t, "Inventory Domain Suite") }`; dot-import ginkgo/v2 + gomega (testing.md канон).
    Создай `internal/domain/id_test.go`: прямые unit-спеки (Describe/Context/It + DescribeTable по пяти ID-типам) под behavior выше; EN-комментарии в тестах; ассерты Gomega (`Equal`, `BeFalse`/`BeTrue`, `MatchError(domain.ErrInvalidID)`). Без моков (D-03 — чистые функции).
    Запусти `cd services/inventory && go mod tidy` (промоутит google/uuid indirect→direct).
  </action>
  <verify>
    <automated>cd services/inventory && go mod tidy && go test ./internal/domain/... -run TestDomainSuite -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 со спеками ID-VO зелёными
    - `grep -q "type HostID struct" services/inventory/internal/domain/id.go` (struct-over-uuid форма)
    - `grep -q "ErrFQDNConflict" services/inventory/internal/domain/errors.go`
    - `grep -q "google/uuid" services/inventory/go.mod` и uuid стал direct (нет `// indirect` на строке uuid)
  </acceptance_criteria>
  <done>5 ID-VO с фабрикой/parse/zero компилируются и протестированы; доменные sentinel + типизированные конфликты объявлены; suite-бутстрап работает; uuid promoted в go.mod.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: aggregateBase + DomainEvent + EventEnvelope/Actor</name>
  <files>services/inventory/internal/domain/aggregate.go, services/inventory/internal/domain/events.go, services/inventory/internal/domain/aggregate_test.go</files>
  <read_first>
    - knowledge/architecture.md (§«Доменные события» ~125-132 — PullEvents→outbox в одной tx)
    - knowledge/patterns.md (Рецепт 3 ~128-143 — фабрика держит инварианты + record + PullEvents)
    - .planning/phases/06-inventory/06-RESEARCH.md (Pattern 2 aggregateBase; Pattern 4 envelope; §events.go; Pitfall 3 version-в-одной-точке; Pitfall 5 PullEvents очищает)
    - .planning/phases/06-inventory/06-PATTERNS.md (§aggregate.go; §events.go — D-13 имена событий, D-14 envelope не в агрегате)
  </read_first>
  <behavior>
    - record(e) добавляет e в буфер И инкрементит version в ОДНОЙ точке; после N record() Version()==N и len(PullEvents())==N (Pitfall 3)
    - PullEvents() возвращает накопленные события и очищает буфер (events=nil); второй PullEvents() подряд возвращает пустой слайс (Pitfall 5)
    - EventEnvelope — структура с полями EventID/EntityID/EventType/Version/OccurredAt/Actor/Payload; Actor.Source принимает значения human|api|integration|system (форма, не валидация)
  </behavior>
  <action>
    Создай `internal/domain/events.go` (package domain): интерфейс `DomainEvent { EventType() string; EntityID() string }` — голый семантический факт БЕЗ envelope-меты (D-14). Структура `Actor { ID string; Source string }` (source: human|api|integration|system — D-15) и `EventEnvelope { EventID string; EntityID string; EventType string; Version int; OccurredAt time.Time; Actor Actor; Payload DomainEvent }`. Важно: envelope-мета (EventID/OccurredAt/Actor) здесь — только форма; заполнение — на границе usecase (06-04 envelope.go), НЕ в агрегате (D-14). Объяви здесь же константы имён семантических событий ИЛИ оставь их в каждом event-struct'е (на усмотрение executor'а; имена фиксируются в DOC-07: HostRegistered/HostHardwareChanged/HostReassigned/HostRelocated/HostDecommissioned/HostDeleted, Project*, DC*/Module*/Rack* — D-13).
    Создай `internal/domain/aggregate.go` (package domain): встраиваемый `aggregateBase struct { version int; events []DomainEvent }`. Метод `record(e DomainEvent)` — `b.version++; b.events = append(b.events, e)` (единая точка инкремента — Pitfall 3, anti-pattern: version++ вне record). Метод `PullEvents() []DomainEvent` — `out := b.events; b.events = nil; return out` (отдаёт-и-очищает — Pitfall 5). Метод `Version() int`. aggregateBase встраивается всеми 5 агрегатами; НЕ выносить в pkg/ — inventory-специфичен (RESEARCH A3, MEMORY shared-code-in-pkg только для truly-generic).
    Создай `internal/domain/aggregate_test.go`: прямые unit-спеки под behavior выше. Объяви в тесте локальный fake-DomainEvent (struct с EventType/EntityID), несколько record() → проверь Version()/len(PullEvents()); двойной PullEvents() → второй пуст. EN-комментарии; без моков (D-03).
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/domain/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 (спеки aggregateBase зелёные: version==count, второй PullEvents пуст)
    - `grep -q "type EventEnvelope" services/inventory/internal/domain/events.go` и `grep -q "Actor " services/inventory/internal/domain/events.go`
    - `grep -q "func (b \*aggregateBase) PullEvents" services/inventory/internal/domain/aggregate.go`
    - `grep -vc '^//' services/inventory/internal/domain/aggregate.go` подтверждает version++ присутствует только внутри record (визуальная проверка executor'ом)
  </acceptance_criteria>
  <done>aggregateBase копит/инкрементит/сливает события в одной точке; DomainEvent/EventEnvelope/Actor — форма домена; тесты Pitfall 3/5 зелёные.</done>
</task>

<task type="auto">
  <name>Task 3: Доменные порты + расширение .mockery.yaml + генерация моков</name>
  <files>services/inventory/internal/domain/ports.go, .mockery.yaml</files>
  <read_first>
    - knowledge/architecture.md (§«UnitOfWork» ~94-100 — порт в domain, impl в repositories, tx-граница)
    - knowledge/patterns.md (Рецепты 1/4 — usecase зависит только от портов)
    - services/inventory/internal/example/provisioner.go (port shape: ctx-first method, интерфейс в пакете)
    - .mockery.yaml (v3-инварианты: template testify, dir "{{.InterfaceDir}}/mocks", packages-map, example-запись как образец)
    - .planning/phases/06-inventory/06-RESEARCH.md (Pattern 6 порты+фейки; §ports.go список портов; Pitfall 6 Clock/IDGen-порты)
    - .planning/phases/06-inventory/06-PATTERNS.md (§ports.go — полный список портов; §.mockery.yaml — как расширять v3)
  </read_first>
  <action>
    Создай `internal/domain/ports.go` (package domain), ctx-первым аргументом везде (architecture.md):
    - `UnitOfWork { Do(ctx context.Context, fn func(ctx context.Context) error) error }` — граница транзакции (D-02; фейк просто зовёт fn).
    - `Outbox { Append(ctx context.Context, events []EventEnvelope) error }`.
    - `HostRepository { Save(ctx, *Host) error; Load(ctx, HostID) (*Host, error); Delete(ctx, HostID) error }` — Delete = физическое удаление (INV-07/D-09, НЕ state=deleted).
    - `ProjectRepository { Save(ctx, *Project) error; Load(ctx, ProjectID) (*Project, error); Delete(ctx, ProjectID) error }`.
    - `DCRepository`/`ModuleRepository`/`RackRepository` — Save/Load/Delete по своему ID-VO (LOC-01 CRUD).
    - `ActiveHostByFQDN { ActiveHostByFQDN(ctx context.Context, fqdn string) (existing HostID, found bool, err error) }` — query-порт уникальности (D-11/INV-10).
    - `HostsInProject { HostsInProject(ctx context.Context, id ProjectID) (int, error) }` — для Project delete-only-if-empty (INV-10).
    - `MatchAdvisor { Candidates(ctx context.Context, fqdn string, hw HostHardware) ([]HostID, error) }` — порт-хук советочного матчинга (D-12/INV-08; no-op заглушка в 06-04). NB: ссылается на тип `HostHardware` из 06-02 — этот плагин Wave 1, а HostHardware в Wave 2; чтобы ports.go компилировался сейчас, объяви сигнатуру MatchAdvisor через `fqdn string` + минимальный набор без HostHardware, ЛИБО вынеси MatchAdvisor-объявление в 06-02 вместе с HostHardware. Executor выбирает: предпочтительно — MatchAdvisor.Candidates(ctx, fqdn string) ([]HostID, error) (hw добавится при наличии advisory-движка, SEED-001), это держит ports.go компилируемым в Wave 1 без forward-зависимости.
    - `Clock { Now() time.Time }` и `IDGenerator { NewID() string }` — для детерминизма envelope-тестов (Pitfall 6; фейки в 06-04).
    Расширь корневой `.mockery.yaml`: добавь packages-запись `github.com/gwall-e/services/inventory/internal/domain:` с `interfaces:` перечисляющим HostRepository/ProjectRepository/DCRepository/ModuleRepository/RackRepository/UnitOfWork/Outbox/ActiveHostByFQDN/HostsInProject/MatchAdvisor/Clock/IDGenerator (каждый `: {}`), форма как у example-записи. Оставь example-запись на месте (удаление — решение 06-04). Запусти `make generate-mocks` (mockery v3.7.1 pinned) → моки появятся в `internal/domain/mocks/`.
  </action>
  <verify>
    <automated>cd /Users/yymoroz3/Projects/personal/gwall-e && make generate-mocks 2>&1 | tail -5; cd services/inventory && go build ./... && go vet ./internal/domain/... 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go build ./...` из services/inventory exits 0 (ports.go компилируется в Wave 1 без HostHardware forward-зависимости)
    - `grep -q "type UnitOfWork interface" services/inventory/internal/domain/ports.go`
    - `grep -q "internal/domain" .mockery.yaml`
    - `ls services/inventory/internal/domain/mocks/UnitOfWork.go` существует (mockery сгенерировал)
    - `grep -q "DO NOT EDIT" services/inventory/internal/domain/mocks/UnitOfWork.go` (сгенерированный, не ручной)
  </acceptance_criteria>
  <done>Все доменные порты объявлены в domain; .mockery.yaml расширен; моки портов сгенерированы и компилируются; go vet зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| фабрика агрегата → доменное ядро | единственная точка рождения идентичности (ID-VO); невалидный вход не должен дать валидный агрегат |
| усечённый-в-Phase-6 usecase → outbox-форма | envelope-мета (actor) приходит снаружи; домен-ядро не должно её принимать (D-14) |

> Phase 6 — чистый домен на фейках: сетевой поверхности нет. Моделируем integrity/correctness-инварианты (RESEARCH Security Domain). Реальное enforcement части из них — Phase 7 (partial FQDN-index, gRPC identity-interceptor).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | ID-VO (id.go) | mitigate | Типизированные ID per aggregate — компилятор ловит перепутанную ссылку (Host.RackID ≠ ProjectID); zero-value guard IsZero() в инвариантах фабрик (D-05) |
| T-06-02 | Spoofing | uuid.Parse в ParseXID | mitigate | RFC-валидация через uuid.Parse; битый/пустой → ErrInvalidID (%w), не паника, не сырая строка |
| T-06-03 | Repudiation | EventEnvelope.Actor | mitigate | Поле Actor{id,source} в envelope с первого дня (EVT-02/D-15/SEED-002) — событие атрибутируемо; форма зафиксирована до relay (Phase 8) |
| T-06-04 | Information Disclosure | errors.go типизированные конфликты | mitigate | ErrFQDNConflict/ErrProjectNotEmpty — доменные struct-ошибки, не сырой DB E11000 (Pitfall 7); реализация БД не протекает наружу |
| T-06-SC | Tampering | mockery `make generate-mocks` | mitigate | Ноль новых внешних пакетов (RESEARCH Package Legitimacy — пусто); google/uuid уже в go.mod (Phase 5 verified); mockery pinned v3.7.1 в Makefile; моки помечены DO NOT EDIT |
</threat_model>

<verification>
- `cd services/inventory && go build ./... && go vet ./internal/domain/...` exits 0
- `go test ./internal/domain/... -count=1` зелёный (ID-VO + aggregateBase спеки)
- Моки портов сгенерированы в `internal/domain/mocks/` (минимум UnitOfWork.go, Outbox.go, HostRepository.go)
- `.mockery.yaml` содержит запись internal/domain
</verification>

<success_criteria>
- 5 типизированных ID-VO над uuid.UUID с фабрикой/parse/zero; компилятор различает типы (INV-03/D-05)
- aggregateBase: record (version++ в одной точке) + PullEvents (отдаёт-и-очищает); тесты Pitfall 3/5 зелёные (EVT-01)
- DomainEvent + EventEnvelope{eventId,entityId,eventType,version,occurredAt,actor} + Actor{id,source} — форма домена; мета не в агрегате (EVT-02/D-14/D-15)
- Доменные sentinel + типизированные конфликты ErrFQDNConflict/ErrProjectNotEmpty (D-11/Pitfall 7)
- Все порты объявлены; mockery-моки сгенерированы и компилируются (D-02/D-03)
</success_criteria>

<output>
Create `.planning/phases/06-inventory/06-01-SUMMARY.md` when done
</output>
