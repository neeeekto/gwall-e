---
phase: 06-inventory
plan: 06
type: execute
wave: 4
depends_on: ["06-02", "06-03"]
files_modified:
  - knowledge/glossary.md
  - knowledge/README.md
autonomous: true
requirements: [DOC-07, SVC-01]
must_haves:
  truths:
    - "knowledge/glossary.md существует и фиксирует ubiquitous language домена Inventory (DOC-07)"
    - "glossary проводит границу «факт существования (Inventory) vs динамическое состояние (State/Health)» (DOC-07)"
    - "glossary определяет термины Project/Host/Owner/Module/Rack/DC/Connection (forward-term), идентичность (INV-03), decommission ≠ delete (D-08/D-09)"
    - "glossary фиксирует имена семантических событий (HostRegistered/…/HostDeleted, Project*, DC*/Module*/Rack*) как ubiquitous language (D-13)"
    - "knowledge/README.md индексирует glossary со статусом «существует» (был «отложено»)"
  artifacts:
    - path: "knowledge/glossary.md"
      provides: "ubiquitous language домена Inventory"
      contains: "decommission"
    - path: "knowledge/README.md"
      provides: "обновлённый индекс knowledge/ (glossary существует)"
      contains: "glossary.md"
  key_links:
    - from: "knowledge/glossary.md"
      to: "семантические события (events.go/host.go/project.go)"
      via: "имена событий = ubiquitous language"
      pattern: "HostRegistered"
    - from: "knowledge/README.md"
      to: "knowledge/glossary.md"
      via: "индекс-строка статус «существует»"
      pattern: "glossary"
---

<objective>
Написать `knowledge/glossary.md` (DOC-07) — ubiquitous language домена Inventory: граница «факт существования vs динамическое состояние», ключевые термины (Project/Host/Owner/Module/Rack/DC/Connection-forward), идентичность (INV-03), `decommission ≠ delete` (D-08/D-09), и имена семантических событий (D-13). Обновить индекс `knowledge/README.md` (статус glossary: «отложено» → «существует»). Сверить, что доменный код разложен по канон-слоям (SVC-01).

Purpose: glossary фиксирует единый язык, который связывает имена событий (домен) ↔ protobuf-схемы (Phase 8) ↔ DTO (Phase 7). Без него термины расходятся между фазами. Это единственное место в knowledge/, где разрешён forward-term (Connection — Phase 9), по исключению из no-phantom (authoring.md/boundaries.md).
Output: `knowledge/glossary.md` + обновлённый `knowledge/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory/06-CONTEXT.md
@.planning/phases/06-inventory/06-RESEARCH.md
@.planning/phases/06-inventory/06-02-SUMMARY.md
@.planning/phases/06-inventory/06-03-SUMMARY.md
@knowledge/authoring.md
@knowledge/boundaries.md
@knowledge/README.md
@.planning/REQUIREMENTS.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Glossary doc:** `knowledge/glossary.md` — sections: «Граница домена» (факт существования vs динамическое состояние), «Термины» (Project/Host/Owner/Module/Rack/DC/Connection-forward/идентичность/decommission≠delete), «Семантические события» (полный список имён из 06-02/06-03).
**Index update:** `knowledge/README.md` — glossary row status «существует».
**New files:** `knowledge/glossary.md`; modified `knowledge/README.md`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Написать knowledge/glossary.md (DOC-07)</name>
  <files>knowledge/glossary.md</files>
  <read_first>
    - knowledge/authoring.md (стандарт MUST/SHOULD/WON'T + pointer-over-copy; glossary — исключение из no-phantom)
    - knowledge/boundaries.md (карта владения фактами; glossary может фиксировать forward-термины)
    - .planning/phases/06-inventory/06-RESEARCH.md (§DOC-07 glossary структура ~574-590; §Граница домена ~579-588)
    - .planning/REQUIREMENTS.md (заголовок §«Граница домена»: Inventory = факт существования, НЕ динамика; DOC-07 текст; Out of Scope — что не Inventory)
    - .planning/phases/06-inventory/06-02-SUMMARY.md (фактические имена Host-событий)
    - .planning/phases/06-inventory/06-03-SUMMARY.md (фактические имена Project/Location-событий)
  </read_first>
  <action>
    Создай `knowledge/glossary.md` по стандарту authoring.md (русский контент, EN тех-термины; MUST/SHOULD/WON'T где уместно). Разделы:
    - **Граница домена** — «факт существования» (Inventory владеет: идентичность, ЖЦ-как-актив, состав железа, локация, топология) vs «динамическое состояние» (НЕ Inventory → State/Health: runtime-health, failed/maintenance, firmware/BIOS). Это ключевая граница DOC-07.
    - **Термины** — Project, Host, Owner (непрозрачный внешний string-ID группы, резолв вне scope — INV-09), Module, Rack, DC, **Connection** (forward-term Phase 9 — отметить как «вперёд», разрешено glossary-исключением), **Идентичность** (внутренний постоянный непереиспользуемый ID — единственный носитель, INV-03), **decommission ≠ delete** (decommission = смена ЖЦ, хост видим, терминально; delete = hard-удаление, история только в append-only *.events — D-08/D-09).
    - **Семантические события** — полный список имён, СВЕРЕННЫЙ с фактическими типами из 06-02/06-03 SUMMARY (HostRegistered/HostHardwareChanged/HostReassigned/HostRelocated/HostDecommissioned/HostDeleted; ProjectCreated/ProjectRenamed/ProjectOwnerChanged/ProjectDeleted; DCCreated/DCUpdated/DCDeleted; ModuleCreated/…; RackCreated/…). Отметь: имена = ubiquitous language, общий контракт для protobuf (Phase 8) и DTO (Phase 7).
    Источники цитировать pointer-over-copy (ссылки на architecture.md/REQUIREMENTS, не дублировать правила). НЕ изобретать термины вне D-08…D-15 и REQUIREMENTS.
  </action>
  <verify>
    <automated>test -f knowledge/glossary.md && grep -qi "decommission" knowledge/glossary.md && grep -q "HostRegistered" knowledge/glossary.md && grep -qi "факт существования" knowledge/glossary.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/glossary.md` существует
    - Содержит границу «факт существования vs динамическое состояние»: `grep -qi "факт существования" knowledge/glossary.md`
    - Содержит decommission≠delete: `grep -qi "decommission" knowledge/glossary.md`
    - Содержит имена семантических событий: `grep -q "HostRegistered" knowledge/glossary.md` и `grep -q "HostDeleted" knowledge/glossary.md`
    - Содержит термины Project/Host/Owner/Module/Connection: `grep -q "Connection" knowledge/glossary.md`
  </acceptance_criteria>
  <done>glossary.md фиксирует границу домена, термины, идентичность, decommission≠delete и имена семантических событий, сверенные с реализованным кодом.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить индекс knowledge/README.md + сверка канон-слоёв (SVC-01)</name>
  <files>knowledge/README.md</files>
  <read_first>
    - knowledge/README.md (§«Индекс» — строка glossary.md статус «отложено (v2 / domain-milestone)»)
    - .planning/phases/06-inventory/06-04-SUMMARY.md (фактическая раскладка usecase-слоя)
    - .planning/phases/06-inventory/06-05-SUMMARY.md (фактическая раскладка usecase-слоя)
    - knowledge/structure.md (inventory как компилируемый член go.work)
  </read_first>
  <action>
    Обнови `knowledge/README.md`: в таблице §«Индекс» строку `glossary.md` — статус «отложено (v2 / domain-milestone)» → «существует»; при необходимости поправь колонку «Когда читать» (оставь «Перед работой с доменом»). Сделай ссылку на файл рабочей (`[glossary.md](glossary.md)`), как у других существующих доков.
    Сверка SVC-01 (не правка кода, а проверка раскладки): убедись, что доменный код Phase 6 лежит в канон-слоях `internal/domain` (агрегаты/VO/события/порты) и `internal/usecase` (interactor'ы), а `api`/`cron`/`app`/`repositories`/`query` остаются скелетом/пустыми до Phase 7 (это ожидаемо — Claude's Discretion в CONTEXT.md). Если обнаружишь доменный код вне канон-слоёв — отметь это в SUMMARY как отклонение (не чинить здесь, эскалировать).
  </action>
  <verify>
    <automated>grep -q "glossary" knowledge/README.md && grep -A1 "glossary.md" knowledge/README.md | grep -qi "существует" && echo OK; cd services/inventory && go build ./... && go vet ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/README.md` индексирует glossary со статусом «существует»: `grep -A1 "glossary.md" knowledge/README.md | grep -qi "существует"`
    - `cd services/inventory && go build ./...` exits 0 (весь доменный код компилируется — SVC-01 слои валидны)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>README индексирует glossary как существующий; доменный код подтверждённо разложен по канон-слоям domain/usecase; build/vet зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| glossary forward-term → канон | glossary — единственное разрешённое место для forward-термина (Connection); не должен протечь как phantom-правило в другие доки |

> Документационная фаза без исполняемой поверхности. Угрозы — корректность канона (phantom-rule leak) и согласованность имён событий с кодом.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-20 | Tampering | glossary forward-term (Connection) | mitigate | Connection помечен как forward-term Phase 9; glossary — явное исключение из no-phantom (authoring.md/boundaries.md); не вводится как активное правило |
| T-06-21 | Repudiation | имена событий в glossary vs код | mitigate | Имена сверяются с фактическими типами из 06-02/06-03 SUMMARY — единый ubiquitous language, нет расхождения домен↔glossary↔(Phase 8 protobuf) |
</threat_model>

<verification>
- `knowledge/glossary.md` существует, содержит границу/термины/decommission≠delete/имена событий
- `knowledge/README.md` индексирует glossary «существует»
- `cd services/inventory && go build ./... && go vet ./...` exits 0 (SVC-01 слои)
</verification>

<success_criteria>
- glossary.md фиксирует границу «факт существования vs динамическое состояние», термины, идентичность, decommission≠delete, имена событий (DOC-07)
- Имена событий сверены с реализованным кодом (единый ubiquitous language, D-13)
- README индексирует glossary как существующий
- Доменный код разложен по канон-слоям; build/vet зелёные (SVC-01)
</success_criteria>

<output>
Create `.planning/phases/06-inventory/06-06-SUMMARY.md` when done
</output>
