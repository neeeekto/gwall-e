---
phase: 06-inventory
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - services/inventory/internal/domain/hardware.go
  - services/inventory/internal/domain/host.go
  - services/inventory/internal/domain/hardware_test.go
  - services/inventory/internal/domain/host_test.go
autonomous: true
requirements: [INV-02, INV-03, INV-04, INV-05, INV-06, INV-07, INV-09, HW-01, HW-02, HW-03, HW-04, HW-05, HW-06, LOC-03, EVT-01]
must_haves:
  truths:
    - "HostHardware — единый immutable VO со структурированными под-компонентами RAM/CPU/Drive/NIC/PSU/StorageController/GPU/Chassis/Motherboard + IPMIMac; внешние ID = raw string (HW-01…06/D-07)"
    - "Изменение immutable VO через слайс-ссылку невозможно — defensive copy в конструкторе и геттерах (Pitfall 2)"
    - "NewHost требует непустой ProjectID (INV-02), генерирует HostID (INV-03), несёт RackID+позицию (LOC-03), эмитит ровно один HostRegistered (EVT-01)"
    - "lifecycleState ∈ {shadow, registered, decommissioned} — ровно три; deleted НЕ член enum (D-08/D-09)"
    - "Переходы: shadow→registered, shadow→decommissioned, registered→decommissioned; decommissioned терминально (нет воскрешения); Delete() из любого состояния (D-10)"
    - "Каждая операция (Reassign/Relocate/ChangeHardware/Decommission/Delete) эмитит ровно одно семантическое событие, не HostUpdated-дамп (D-13/EVT-01/Pitfall 5)"
  artifacts:
    - path: "services/inventory/internal/domain/hardware.go"
      provides: "immutable HostHardware VO + под-VO"
      contains: "type HostHardware struct"
    - path: "services/inventory/internal/domain/host.go"
      provides: "Host-агрегат: фабрика + lifecycle SM + операции"
      contains: "func NewHost"
  key_links:
    - from: "services/inventory/internal/domain/host.go"
      to: "aggregateBase.record"
      via: "каждая операция вызывает record(семантическое событие)"
      pattern: "h\\.record\\("
    - from: "services/inventory/internal/domain/host.go"
      to: "HostHardware VO"
      via: "ChangeHardware заменяет VO целиком → один HostHardwareChanged"
      pattern: "ChangeHardware"
---

<objective>
Реализовать центральный агрегат фазы — Host: единый immutable VO `HostHardware` со всеми структурированными компонентами железа (D-07/HW-01…06), фабрику `NewHost` с инвариантами идентичности/привязки (INV-02/03), lifecycle state-machine `shadow→registered→decommissioned` + отдельный hard-`Delete()` (D-08/D-09/D-10), и операции-переходы, каждая из которых эмитит ровно одно семантическое событие (D-13/EVT-01).

Purpose: Host — носитель идентичности парка и источник большинства семантических событий backbone. Корректность lifecycle-инвариантов (терминальный decommission, deleted≠lifecycle-state) и immutability HostHardware — приёмочные точки Pitfall 2 и фундамент Phase 7/8.
Output: `internal/domain/{hardware,host}.go` + прямые unit-тесты (DescribeTable по переходам ЖЦ и по структуре/immutability железа).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory/06-CONTEXT.md
@.planning/phases/06-inventory/06-RESEARCH.md
@.planning/phases/06-inventory/06-PATTERNS.md
@.planning/phases/06-inventory/06-01-SUMMARY.md
@knowledge/patterns.md
@knowledge/style.md
@knowledge/testing.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Aggregate:** `Host` (embeds `aggregateBase`), factory `NewHost(projectID ProjectID, fqdn string, hw HostHardware, rackID RackID, position string, initial lifecycleState) (*Host, error)`.
**Host methods:** `ID() HostID`, `Reassign(newProjectID ProjectID) error`, `Relocate(rackID RackID, position string) error`, `ChangeHardware(newHW HostHardware) error`, `Decommission(reason string) error`, `Delete()`, accessors as needed.
**Lifecycle:** `lifecycleState` (unexported int enum: `stateShadow`, `stateRegistered`, `stateDecommissioned`).
**Immutable VO:** `HostHardware` + sub-VOs `Motherboard`, `RAMModule`, `CPU`, `Drive`, `NIC`, `PSU`, `StorageController`, `GPU`, `Chassis`; constructor `NewHostHardware(spec HardwareSpec) (HostHardware, error)`; spec input type `HardwareSpec`.
**Domain events:** `HostRegistered`, `HostHardwareChanged`, `HostReassigned`, `HostRelocated`, `HostDecommissioned`, `HostDeleted` (each implements `DomainEvent`).
**New files:** `internal/domain/{hardware,host,hardware_test,host_test}.go`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Immutable HostHardware VO со структурированными под-компонентами</name>
  <files>services/inventory/internal/domain/hardware.go, services/inventory/internal/domain/hardware_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-RESEARCH.md (Pattern 3 HostHardware; Pitfall 2 защитное копирование слайсов)
    - .planning/phases/06-inventory/06-PATTERNS.md (§hardware.go — greenfield VO, D-07, defensive-copy load-bearing)
    - .planning/REQUIREMENTS.md (HW-01…06 тексты)
    - knowledge/style.md (RU-комментарии / EN-идентификаторы; типизированные значения)
  </read_first>
  <behavior>
    - NewHostHardware из валидного spec собирает VO; все поля читаемы через геттеры/value; внешние ID (serial/inv/MAC/model) — raw string (HW-06)
    - NIC структурирован: model/speedGbE/macs []string — НЕ плоский MACs[] на хосте (HW-03)
    - Покрыты под-компоненты: Motherboard, RAM[], CPU[], Drive[] (slot/model/vendor/lot/serial/Inv + спеки), NIC[], PSU[] (HW-04), StorageController[]/RAID, GPU[] внутренние, Chassis отдельно от материнки (HW-05), IPMIMac, Name, Platform (HW-01)
    - Immutability: получить слайс из VO (напр. NICs()), мутировать элемент/слайс → исходный VO НЕ изменился (Pitfall 2 defensive copy)
    - NewHostHardware с пустым обязательным полём (напр. Name) → доменная ошибка (V5 input validation)
  </behavior>
  <action>
    Создай `internal/domain/hardware.go` (package domain): единый immutable `HostHardware struct` с приватными полями `name string`, `platform string`, `motherboard Motherboard`, `ipmiMAC string`, слайсы `ram []RAMModule`, `cpu []CPU`, `drives []Drive`, `nics []NIC`, `psus []PSU`, `storageCtl []StorageController`, `gpus []GPU`, поле `chassis Chassis` (D-07/HW-01…05). Под-VO как отдельные struct: `Motherboard`, `RAMModule`, `CPU`, `Drive` (slot/model/vendor/lot/serial/inv + спеки — HW-02), `NIC{ model string; speedGbE int; macs []string }` (HW-03), `PSU` (HW-04), `StorageController`, `GPU`, `Chassis` (HW-05). ВСЕ внешние идентификаторы (serial/inv/MAC/model) — raw `string` (HW-06/D-05). Конструктор `NewHostHardware(spec HardwareSpec) (HostHardware, error)` + входной тип `HardwareSpec` (публичные поля для сборки на edge); валидация простых инвариантов (напр. непустое name). CRITICAL (Pitfall 2): defensive copy слайсов в конструкторе (`append([]NIC(nil), spec.NICs...)`) И в любом слайс-геттере — иначе «immutable» VO протекает через ссылку. Геттеры возвращают копии слайсов либо агрегированные значения. Комментарии RU, идентификаторы/строки EN.
    Создай `internal/domain/hardware_test.go`: прямые unit-спеки (DescribeTable по структуре компонентов + отдельный It на immutability: получить слайс, мутировать, проверить неизменность VO; It на NIC-структуру; It на raw-string внешних ID; It на ошибку конструктора при пустом name). EN-комментарии; без моков (D-03).
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/domain/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 со спеками hardware зелёными (включая immutability-тест Pitfall 2)
    - `grep -q "type HostHardware struct" services/inventory/internal/domain/hardware.go`
    - `grep -q "type NIC struct" services/inventory/internal/domain/hardware.go` (структурированный NIC, HW-03)
    - `grep -Eq "append\(\[\]NIC\(nil\)|append\(\[\]" services/inventory/internal/domain/hardware.go` (defensive copy присутствует)
  </acceptance_criteria>
  <done>HostHardware — единый immutable VO со всеми структурированными под-компонентами; внешние ID = string; defensive-copy подтверждён тестом; ошибка валидации на пустом обязательном поле.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Host-агрегат — фабрика, lifecycle state-machine, операции-события</name>
  <files>services/inventory/internal/domain/host.go, services/inventory/internal/domain/host_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-RESEARCH.md (§Lifecycle state-machine ~519-543; Pattern 5; Pitfall 1 deleted-не-enum; Pitfall 3 version)
    - .planning/phases/06-inventory/06-PATTERNS.md (§host.go — factory-holds-invariants, state-machine+Delete форма, Pitfall 2/D-09)
    - knowledge/patterns.md (Рецепт 3 — NewX держит инварианты + record)
    - .planning/research/PITFALLS.md (Pitfall 2 decommission≠delete — приёмочная точка)
    - services/inventory/internal/domain/aggregate.go (record/PullEvents — из 06-01)
    - services/inventory/internal/domain/events.go (DomainEvent интерфейс — из 06-01)
  </read_first>
  <behavior>
    - NewHost с zero-value ProjectID → ошибка (INV-02 обязательная привязка); валидный вход → *Host с непустым HostID (INV-03), RackID+позицией (LOC-03), эмитит ровно один HostRegistered (EVT-01)
    - NewHost создаёт хост либо в shadow, либо сразу в registered (гибкий вход — D-10)
    - shadow→registered ok; shadow→decommissioned ok; registered→decommissioned ok; decommissioned→что-либо (кроме Delete) → ErrInvalidTransition/ErrAlreadyDecommissioned (терминально, D-10)
    - Decommission второй раз → ErrAlreadyDecommissioned; первый раз эмитит ровно один HostDecommissioned
    - Reassign(newProjectID) меняет ProjectID + эмитит HostReassigned (D-06 операция на Host); Relocate → HostRelocated; ChangeHardware(newHW) заменяет VO целиком + эмитит ровно один HostHardwareChanged (D-07, НЕ per-компонент)
    - Delete() из любого состояния эмитит HostDeleted с полным payload; lifecycleState НЕ получает значение «deleted» (Pitfall 1/D-09)
    - После последовательности из N операций Version()==N и len(PullEvents())==N (Pitfall 3)
  </behavior>
  <action>
    Создай `internal/domain/host.go` (package domain): `Host struct` встраивает `aggregateBase`, несёт `id HostID`, `projectID ProjectID`, `fqdn string`, `hardware HostHardware`, `rackID RackID`, `position string`, `state lifecycleState`. Enum `lifecycleState int` с `stateShadow`/`stateRegistered`/`stateDecommissioned` (iota; РОВНО три — Pitfall 1, deleted НЕ член). Фабрика `NewHost(projectID ProjectID, fqdn string, hw HostHardware, rackID RackID, position string, initial lifecycleState) (*Host, error)`: проверяет `projectID.IsZero()` → ErrInvalidTransition/доменная ошибка обязательной привязки (INV-02), генерирует `NewHostID()` (INV-03), допускает initial ∈ {shadow, registered} (D-10), вызывает `h.record(HostRegistered{...})` (EVT-01). Методы-переходы, каждый записывает РОВНО одно семантическое событие через record (D-13): `Reassign(newProjectID ProjectID) error`→HostReassigned (меняет projectID; D-06), `Relocate(rackID RackID, position string) error`→HostRelocated, `ChangeHardware(newHW HostHardware) error`→HostHardwareChanged (замена VO целиком — D-07), `Decommission(reason string) error` (guard: уже decommissioned → ErrAlreadyDecommissioned; иначе state=decommissioned + HostDecommissioned; терминально — D-10), `Delete()` (из любого состояния: record HostDeleted с полным snapshot-payload для аудита; usecase зовёт repo.Delete — НЕ state=deleted, Pitfall 1/D-09). Геттер `ID() HostID` и нужные accessors. Объяви event-struct'ы (HostRegistered/…/HostDeleted) реализующими DomainEvent (EventType()/EntityID()) — здесь или в events.go (06-01); минимально-достаточный payload на событие (D-13, не дамп). Комментарии RU, строки/идентификаторы EN.
    Создай `internal/domain/host_test.go`: прямые unit-спеки. DescribeTable по матрице переходов ЖЦ (валидные/невалидные, терминальность decommissioned). It-спеки: NewHost с zero ProjectID → ошибка; happy-path эмитит один HostRegistered; каждая операция эмитит ровно одно ожидаемое событие (проверка типа через type-assert/BeAssignableToTypeOf на PullEvents()); ChangeHardware = один HostHardwareChanged; Delete не трогает lifecycleState; Version()==число операций. EN-комментарии; без моков (D-03).
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/domain/... -count=1 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 (lifecycle DescribeTable + event-эмиссия зелёные)
    - `grep -q "func NewHost" services/inventory/internal/domain/host.go`
    - `grep -Eq "stateShadow|stateRegistered|stateDecommissioned" services/inventory/internal/domain/host.go` и НЕТ `stateDeleted` (Pitfall 1): `! grep -q "stateDeleted" services/inventory/internal/domain/host.go`
    - `grep -q "HostDecommissioned" services/inventory/internal/domain/host.go` и `grep -q "HostDeleted" services/inventory/internal/domain/host.go`
  </acceptance_criteria>
  <done>Host-агрегат: фабрика с инвариантами, 3-state lifecycle SM (терминальный decommission), отдельный hard-Delete, операции эмитят ровно одно семантическое событие; deleted не lifecycle-state; тесты Pitfall 1/3 зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| фабрика NewHost → агрегат Host | обязательная привязка к Project + генерация идентичности; невалидный вход не даёт валидный хост |
| вызывающий → HostHardware VO | переданный/возвращённый слайс не должен дать мутацию «immutable» VO |
| операция-переход → lifecycle | невалидный/терминальный переход (воскрешение) должен отклоняться |

> Чистый домен на фейках: integrity/correctness-инварианты. Enforcement FQDN-уникальности и optimistic-concurrency по version — Phase 7.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Tampering | lifecycle state-machine (host.go) | mitigate | Терминальный decommissioned: Decommission второй раз → ErrAlreadyDecommissioned; нет перехода из decommissioned (кроме Delete) — воскрешение невозможно (D-10) |
| T-06-06 | Repudiation | Delete() / state=deleted | mitigate | deleted = hard-delete + HostDeleted с полным snapshot-payload в (будущий) append-only лог; НЕ строка state=deleted в живой коллекции — аудит-след не затирается (D-09/Pitfall 2) |
| T-06-07 | Tampering | HostHardware immutable VO | mitigate | Defensive copy слайсов в конструкторе и геттерах; тест мутации подтверждает неизменность (Pitfall 2) |
| T-06-08 | Spoofing | NewHost обязательная привязка | mitigate | projectID.IsZero() guard в фабрике — хост не существует без Project (INV-02); идентичность генерится системой, не вводится (INV-03) |
</threat_model>

<verification>
- `cd services/inventory && go test ./internal/domain/... -count=1` зелёный
- `go build ./... && go vet ./internal/domain/...` exits 0
- lifecycleState имеет ровно 3 члена; нет stateDeleted
- Каждая операция Host эмитит ровно одно семантическое событие (проверено тестом)
</verification>

<success_criteria>
- HostHardware — единый immutable VO со структурированными RAM/CPU/Drive/NIC/PSU/StorageController/GPU/Chassis/Motherboard + IPMIMac; внешние ID = string; defensive-copy (HW-01…06/D-07)
- NewHost: обязательный ProjectID (INV-02), генерируемый HostID (INV-03), RackID+позиция (LOC-03), один HostRegistered (EVT-01)
- lifecycle SM: shadow/registered/decommissioned (3 члена), терминальный decommission, deleted≠lifecycle-state (INV-04/06/D-08/D-09/D-10)
- Reassign/Relocate/ChangeHardware/Decommission/Delete — по одному семантическому событию (INV-05/07/D-13/EVT-01)
</success_criteria>

<output>
Create `.planning/phases/06-inventory/06-02-SUMMARY.md` when done
</output>
