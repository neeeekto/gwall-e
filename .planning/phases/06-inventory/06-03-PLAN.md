---
phase: 06-inventory
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - services/inventory/internal/domain/project.go
  - services/inventory/internal/domain/dc.go
  - services/inventory/internal/domain/module.go
  - services/inventory/internal/domain/rack.go
  - services/inventory/internal/domain/project_test.go
  - services/inventory/internal/domain/location_test.go
autonomous: true
requirements: [INV-01, INV-03, INV-09, LOC-01, LOC-02, LOC-04, EVT-01]
must_haves:
  truths:
    - "Project-агрегат: NewProject(name, desc, owner) генерирует ProjectID (INV-03), Owner — raw string (INV-09), эмитит ProjectCreated; операции Rename/ChangeOwner/Delete по одному событию (INV-01/D-13)"
    - "DC/Module/Rack — три независимых агрегата, каждый свой корень с CRUD (LOC-01/D-04); НЕ один агрегат-дерево"
    - "Иерархия по внутреннему ID: Module несёт DCID, Rack несёт ModuleID (LOC-02/D-04/D-06)"
    - "Rack несёт топологические атрибуты (источник питания / генератор) как поля/VO (LOC-04)"
    - "Каждая CRUD-операция локаций/Project эмитит ровно одно семантическое событие (DCCreated/ModuleCreated/RackCreated/… — D-13/EVT-01)"
  artifacts:
    - path: "services/inventory/internal/domain/project.go"
      provides: "Project-агрегат + операции/события"
      contains: "func NewProject"
    - path: "services/inventory/internal/domain/dc.go"
      provides: "DC-агрегат + CRUD-методы"
      contains: "func NewDC"
    - path: "services/inventory/internal/domain/module.go"
      provides: "Module-агрегат (несёт DCID)"
      contains: "DCID"
    - path: "services/inventory/internal/domain/rack.go"
      provides: "Rack-агрегат (несёт ModuleID + топология-атрибуты)"
      contains: "ModuleID"
  key_links:
    - from: "services/inventory/internal/domain/module.go"
      to: "DCID (id.go)"
      via: "Module ссылается на DC по внутреннему ID"
      pattern: "DCID"
    - from: "services/inventory/internal/domain/rack.go"
      to: "ModuleID (id.go)"
      via: "Rack ссылается на Module по внутреннему ID"
      pattern: "ModuleID"
---

<objective>
Реализовать Project-агрегат (идентичность проекта, Owner как непрозрачный string — INV-01/09) и три независимых локационных агрегата DC/Module/Rack с иерархией по внутреннему ID (LOC-01…04/D-04/D-06). Каждый агрегат встраивает aggregateBase и эмитит ровно одно семантическое событие на CRUD-операцию (D-13/EVT-01).

Purpose: Project — обязательный родитель Host (привязка INV-02); локации — первоклассные сущности (не дерево-агрегат), что ложится на Kafka key=ID каждой сущности (Phase 8) и на «дёшево добавляемые агрегаты». Иерархия по ID (не вложенные объекты) держит агрегаты маленькими по канону DDD.
Output: `internal/domain/{project,dc,module,rack}.go` + прямые unit-тесты.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory/06-CONTEXT.md
@.planning/phases/06-inventory/06-RESEARCH.md
@.planning/phases/06-inventory/06-PATTERNS.md
@.planning/phases/06-inventory/06-01-SUMMARY.md
@knowledge/patterns.md
@knowledge/style.md
@knowledge/testing.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Aggregates:** `Project` (embeds `aggregateBase`), `DC`, `Module`, `Rack` (each embeds `aggregateBase`).
**Factories:** `NewProject(name, description, owner string) (*Project, error)`, `NewDC(name string, ...) (*DC, error)`, `NewModule(dcID DCID, name string, ...) (*Module, error)`, `NewRack(moduleID ModuleID, name string, power topology attrs...) (*Rack, error)`.
**Project methods:** `ID() ProjectID`, `Rename(name string) error`, `ChangeOwner(owner string) error`, `Delete()` (set per D-13/A7 — Claude's Discretion).
**Location methods:** `Update(...)`/`Delete()` per aggregate (CRUD — LOC-01).
**Domain events:** `ProjectCreated`, `ProjectRenamed`, `ProjectOwnerChanged`, `ProjectDeleted`; `DCCreated`/`DCUpdated`/`DCDeleted`; `ModuleCreated`/`ModuleUpdated`/`ModuleDeleted`; `RackCreated`/`RackUpdated`/`RackDeleted` (each implements `DomainEvent`).
**New files:** `internal/domain/{project,dc,module,rack,project_test,location_test}.go`.
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Project-агрегат + операции/события</name>
  <files>services/inventory/internal/domain/project.go, services/inventory/internal/domain/project_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-PATTERNS.md (§project.go — mirror host.go form: factory+record+PullEvents via aggregateBase)
    - knowledge/patterns.md (Рецепт 3 — NewX держит инварианты)
    - .planning/REQUIREMENTS.md (INV-01 Project fields; INV-09 Owner raw string)
    - services/inventory/internal/domain/aggregate.go (record/PullEvents — из 06-01)
    - services/inventory/internal/domain/id.go (ProjectID — из 06-01)
  </read_first>
  <behavior>
    - NewProject(name, desc, owner) генерирует ProjectID (INV-03), хранит Owner как raw string без резолва (INV-09), эмитит ровно один ProjectCreated (EVT-01)
    - NewProject с пустым обязательным полём (напр. name) → доменная ошибка (V5)
    - Rename(newName) меняет name + эмитит ProjectRenamed; ChangeOwner(newOwner) → ProjectOwnerChanged; Delete() → ProjectDeleted (по одному событию каждый — D-13)
    - После N операций Version()==N, len(PullEvents())==N
  </behavior>
  <action>
    Создай `internal/domain/project.go` (package domain): `Project struct` встраивает `aggregateBase`, несёт `id ProjectID`, `name string`, `description string`, `owner string` (Owner — непрозрачный внешний string-ID группы, INV-09/D-05; резолв вне scope). Фабрика `NewProject(name, description, owner string) (*Project, error)`: валидирует обязательные поля (непустое name; решение по обязательности owner/description — executor в рамках INV-01), генерирует `NewProjectID()`, `record(ProjectCreated{...})`. Операции (набор — Claude's Discretion A7, в рамках D-13 одно-событие-на-операцию): `Rename(name string) error`→ProjectRenamed, `ChangeOwner(owner string) error`→ProjectOwnerChanged, `Delete()`→ProjectDeleted (инвариант delete-only-if-empty — в usecase через query-порт HostsInProject, 06-05; агрегатный Delete лишь эмитит событие, аналогично Host.Delete). Геттер `ID() ProjectID`. Объяви event-struct'ы реализующими DomainEvent (минимально-достаточный payload — D-13). Комментарии RU, идентификаторы/строки EN.
    Создай `internal/domain/project_test.go`: прямые unit-спеки под behavior; EN-комментарии; без моков (D-03).
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/domain/... -count=1 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 (Project-спеки зелёные)
    - `grep -q "func NewProject" services/inventory/internal/domain/project.go`
    - `grep -q "owner string" services/inventory/internal/domain/project.go` (Owner raw string, INV-09)
    - `grep -q "ProjectCreated" services/inventory/internal/domain/project.go`
  </acceptance_criteria>
  <done>Project-агрегат: фабрика с инвариантами, Owner raw string, операции эмитят по одному семантическому событию; тесты зелёные.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Локационные агрегаты DC/Module/Rack (иерархия по ID)</name>
  <files>services/inventory/internal/domain/dc.go, services/inventory/internal/domain/module.go, services/inventory/internal/domain/rack.go, services/inventory/internal/domain/location_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-PATTERNS.md (§project.go/dc.go/module.go/rack.go — 3 независимых агрегата, иерархия по ID, Rack топология-атрибуты, NOT дерево)
    - .planning/REQUIREMENTS.md (LOC-01…04 тексты)
    - .planning/phases/06-inventory/06-CONTEXT.md (D-04 три независимых агрегата; D-06 ссылки по внутреннему ID)
    - services/inventory/internal/domain/id.go (DCID/ModuleID/RackID — из 06-01)
    - services/inventory/internal/domain/aggregate.go (record/PullEvents — из 06-01)
  </read_first>
  <behavior>
    - NewDC генерирует DCID, эмитит DCCreated; NewModule(dcID DCID, …) несёт DCID, генерирует ModuleID, эмитит ModuleCreated; NewRack(moduleID ModuleID, …) несёт ModuleID, генерирует RackID, эмитит RackCreated (LOC-01/02, INV-03)
    - Module хранит ссылку на DC по DCID (не вложенный DC-объект); Rack — на Module по ModuleID (D-06)
    - Rack несёт топологические атрибуты (источник питания / генератор) как поле/VO (LOC-04)
    - Update/Delete на каждом агрегате эмитят ровно одно семантическое событие (DCUpdated/RackDeleted/… — D-13)
    - NewModule с zero DCID → доменная ошибка; NewRack с zero ModuleID → доменная ошибка (иерархическая привязка)
  </behavior>
  <action>
    Создай три файла, каждый — независимый агрегат (D-04, НЕ один Location-tree), встраивающий aggregateBase:
    `internal/domain/dc.go`: `DC struct{ aggregateBase; id DCID; name string; ... }`; `NewDC(name string, ...) (*DC, error)` → DCID + record(DCCreated); методы `Rename`/`Update`/`Delete` по одному событию (DCUpdated/DCDeleted).
    `internal/domain/module.go`: `Module struct{ aggregateBase; id ModuleID; dcID DCID; name string; ... }`; `NewModule(dcID DCID, name string, ...) (*Module, error)` — guard dcID.IsZero() → ошибка (привязка к DC, LOC-02/D-06); record(ModuleCreated); методы Update/Delete (ModuleUpdated/ModuleDeleted).
    `internal/domain/rack.go`: `Rack struct{ aggregateBase; id RackID; moduleID ModuleID; name string; power <топология-атрибуты> }`; топологические атрибуты (источник питания / дизель-генератор) — поле или маленький VO (LOC-04); `NewRack(moduleID ModuleID, name string, ...) (*Rack, error)` — guard moduleID.IsZero(); record(RackCreated); методы Update/Delete (RackUpdated/RackDeleted). Host→Rack-привязка (RackID+позиция) живёт на Host (06-02), не здесь.
    Все ссылки между агрегатами — только по внутреннему ID (D-06, не вложенные объекты, не back-references). Объяви event-struct'ы реализующими DomainEvent. Комментарии RU, идентификаторы/строки EN.
    Создай `internal/domain/location_test.go`: прямые unit-спеки на три агрегата (DescribeTable по CRUD + событиям; guard-тесты на zero parent-ID; иерархия по ID). EN-комментарии; без моков (D-03).
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/domain/... -count=1 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/domain/...` exits 0 (локационные спеки зелёные)
    - `grep -q "func NewDC" services/inventory/internal/domain/dc.go` && `grep -q "func NewModule" services/inventory/internal/domain/module.go` && `grep -q "func NewRack" services/inventory/internal/domain/rack.go`
    - `grep -q "dcID DCID" services/inventory/internal/domain/module.go` (иерархия по ID, LOC-02/D-06)
    - `grep -q "moduleID ModuleID" services/inventory/internal/domain/rack.go`
  </acceptance_criteria>
  <done>DC/Module/Rack — три независимых агрегата с CRUD-операциями и событиями; иерархия по внутреннему ID; Rack несёт топология-атрибуты; guard на zero parent-ID; тесты зелёные.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| фабрика агрегата → агрегат | генерация идентичности + иерархическая привязка по ID; невалидный parent-ID не даёт валидный агрегат |
| Owner string → Project | непрозрачный внешний ID; домен не резолвит и не доверяет его как identity |

> Чистый домен на фейках: integrity/correctness-инварианты. Delete-only-if-empty enforcement — usecase-уровень (06-05) через query-порт.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Tampering | иерархическая привязка Module/Rack | mitigate | NewModule/NewRack guard на zero parent-ID (DCID/ModuleID) — нет «висячей» локации без родителя (LOC-02/D-06) |
| T-06-10 | Spoofing | Project.owner | accept | Owner — непрозрачный внешний string-ID без резолва (INV-09); валидация принадлежности группы — будущий Access-домен (вне scope); храним как факт, не доверяем как identity |
| T-06-11 | Repudiation | CRUD-события локаций/Project | mitigate | Каждая операция эмитит семантическое событие через record — изменение атрибутируемо в backbone (EVT-01/D-13) |
</threat_model>

<verification>
- `cd services/inventory && go test ./internal/domain/... -count=1` зелёный
- `go build ./... && go vet ./internal/domain/...` exits 0
- Module несёт DCID, Rack несёт ModuleID (иерархия по ID)
- Каждая CRUD-операция эмитит ровно одно семантическое событие
</verification>

<success_criteria>
- Project: NewProject + ProjectID + Owner raw string + операции по одному событию (INV-01/03/09/D-13)
- DC/Module/Rack — три независимых агрегата с CRUD (LOC-01/D-04)
- Иерархия по внутреннему ID: Module.DCID, Rack.ModuleID (LOC-02/D-06)
- Rack несёт топологические атрибуты (LOC-04)
- Каждая операция эмитит ровно одно семантическое событие (EVT-01)
</success_criteria>

<output>
Create `.planning/phases/06-inventory/06-03-SUMMARY.md` when done
</output>
