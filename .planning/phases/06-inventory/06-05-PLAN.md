---
phase: 06-inventory
plan: 05
type: execute
wave: 4
depends_on: ["06-01", "06-03", "06-04"]
files_modified:
  - services/inventory/internal/usecase/create_project.go
  - services/inventory/internal/usecase/delete_project.go
  - services/inventory/internal/usecase/update_project.go
  - services/inventory/internal/usecase/crud_dc.go
  - services/inventory/internal/usecase/crud_module.go
  - services/inventory/internal/usecase/crud_rack.go
  - services/inventory/internal/usecase/project_usecases_test.go
  - services/inventory/internal/usecase/location_usecases_test.go
autonomous: true
requirements: [INV-01, INV-10, LOC-01, LOC-02, EVT-01, EVT-02, SVC-01]
must_haves:
  truths:
    - "CreateProject оркеструет через uow.Do{repo.Save → enrich → outbox.Append}; присваивает ProjectID; эмитит один ProjectCreated (INV-01/EVT-01)"
    - "DeleteProject проверяет delete-only-if-empty через query-порт HostsInProject → ErrProjectNotEmpty при n>0 (не сырой ошибкой); иначе proj.Delete + repo.Delete (INV-10)"
    - "CRUD-usecases DC/Module/Rack в той же uow.Do-форме; каждая операция эмитит ровно одно семантическое событие (LOC-01/EVT-01)"
    - "форма Execute идентична Phase 7 — фейки свапаются на Mongo без изменения usecase (D-02)"
  artifacts:
    - path: "services/inventory/internal/usecase/create_project.go"
      provides: "CreateProjectUseCase.Execute"
      contains: "uow.Do"
    - path: "services/inventory/internal/usecase/delete_project.go"
      provides: "DeleteProjectUseCase.Execute (delete-if-empty)"
      contains: "ErrProjectNotEmpty"
    - path: "services/inventory/internal/usecase/crud_dc.go"
      provides: "DC CRUD usecases"
      contains: "uow.Do"
  key_links:
    - from: "services/inventory/internal/usecase/delete_project.go"
      to: "domain.HostsInProject"
      via: "delete-only-if-empty проверка внутри uow.Do"
      pattern: "HostsInProject"
    - from: "services/inventory/internal/usecase/create_project.go"
      to: "outbox.Append"
      via: "enrich(proj.PullEvents(), actor, clock, idgen) внутри fn"
      pattern: "outbox\\.Append"
---

<objective>
Реализовать write-usecases над Project (Create/Update/Delete — с инвариантом delete-only-if-empty через query-порт, INV-10) и CRUD-usecases над локациями DC/Module/Rack (LOC-01), все в канонической форме `uow.Do(fn){…→outbox.Append}` (D-01/D-02), переиспользуя envelope-helper и фейки из 06-04.

Purpose: завершает usecase-вертикаль — оператор может через usecase завести/изменить/удалить Project и локации (SC1/SC4 верифицируемы). delete-only-if-empty демонстрирует второй случай инварианта-требующего-чтения через query-порт (как FQDN-конфликт), фиксируя форму под Phase 7.
Output: `internal/usecase/{create_project,delete_project,update_project,crud_dc,crud_module,crud_rack}.go` + спеки (mockery-моки repo/hostCount + фейки uow/outbox/clock/idgen).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-inventory/06-CONTEXT.md
@.planning/phases/06-inventory/06-RESEARCH.md
@.planning/phases/06-inventory/06-PATTERNS.md
@.planning/phases/06-inventory/06-01-SUMMARY.md
@.planning/phases/06-inventory/06-03-SUMMARY.md
@.planning/phases/06-inventory/06-04-SUMMARY.md
@knowledge/patterns.md
@knowledge/testing.md
</context>

<artifacts_produced>
## Artifacts this phase produces (this plan)

**Project usecases:** `CreateProjectUseCase`, `UpdateProjectUseCase` (rename/change-owner — set per discretion), `DeleteProjectUseCase` — each `struct` + `Execute(ctx, Input, actor Actor) (Output, error)`.
**Location usecases:** `CreateDCUseCase`/`UpdateDCUseCase`/`DeleteDCUseCase` (crud_dc.go); same for Module (crud_module.go) and Rack (crud_rack.go).
**New files:** `internal/usecase/{create_project,delete_project,update_project,crud_dc,crud_module,crud_rack}.go`, `internal/usecase/{project_usecases_test,location_usecases_test}.go`.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Project usecases (Create/Update/Delete-if-empty)</name>
  <files>services/inventory/internal/usecase/create_project.go, services/inventory/internal/usecase/update_project.go, services/inventory/internal/usecase/delete_project.go, services/inventory/internal/usecase/project_usecases_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-RESEARCH.md (§Project delete-only-if-empty ~546-561; Pattern 5 форма)
    - .planning/phases/06-inventory/06-PATTERNS.md (§delete_project.go — delete-if-empty через query-порт)
    - knowledge/patterns.md (Рецепт 1 Execute)
    - .planning/REQUIREMENTS.md (INV-01 Project; INV-10 удалить только пустым)
    - services/inventory/internal/domain/project.go (Project + операции — из 06-03)
    - services/inventory/internal/domain/ports.go (ProjectRepository/HostsInProject — из 06-01)
    - services/inventory/internal/usecase/envelope.go (enrich + Actor — из 06-04)
    - services/inventory/internal/usecase/fakes/fakes.go (фейки — из 06-04)
  </read_first>
  <action>
    Создай три usecase-файла (package usecase), форма `uow.Do(fn){…→outbox.Append}` (outbox внутри fn — D-02/Pitfall 8), переиспользуя `enrich` и фейки из 06-04:
    `create_project.go`: `CreateProjectUseCase struct{ projects domain.ProjectRepository; outbox; uow; clock; idgen }` + `Execute(ctx, in RegisterProjectInput{Name, Description, Owner}, actor) (out{ID ProjectID}, error)` — `proj, err := domain.NewProject(in.Name, in.Description, in.Owner)` (INV-01); `uow.Do`: `projects.Save(ctx, proj)` → `outbox.Append(ctx, enrich(proj.PullEvents(), actor, clock, idgen))`; вернуть ProjectID (INV-03 — присвоен системой).
    `update_project.go`: `UpdateProjectUseCase` — Load → `proj.Rename`/`proj.ChangeOwner` (набор операций — Claude's Discretion A7, по одному событию D-13) → Save → enrich → outbox.Append.
    `delete_project.go`: `DeleteProjectUseCase.Execute(ctx, id ProjectID, actor)` — `uow.Do`: `n, err := hostCount.HostsInProject(ctx, id)` (query-порт); `if n > 0 → return domain.ErrProjectNotEmpty{ProjectID: id, HostCount: n}` (типизированный, не сырой — INV-10); иначе `proj, _ := projects.Load(ctx, id)`; `proj.Delete()`; `projects.Delete(ctx, id)` (физическое удаление); `outbox.Append(enrich(proj.PullEvents(), …))`. Поле порта `hostCount domain.HostsInProject`. Ошибки `%w`. RU-комментарии, EN-идентификаторы/строки.
    Создай `internal/usecase/project_usecases_test.go`: спеки (mockery-моки ProjectRepository + HostsInProject + фейки uow/outbox/clock/idgen). Сценарии: Create присваивает ID + один ProjectCreated в FakeOutbox.Appended (EVT-01) с actor (EVT-02); Delete при HostsInProject>0 → ErrProjectNotEmpty (errors.As), projects.Delete НЕ вызван; Delete при 0 → proj.Delete event + projects.Delete expectation. EN-комментарии.
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/usecase/... -count=1 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./internal/usecase/...` exits 0 (Project-usecase спеки зелёные)
    - `grep -q "func NewProject\|domain.NewProject" services/inventory/internal/usecase/create_project.go`
    - `grep -q "HostsInProject" services/inventory/internal/usecase/delete_project.go` и `grep -q "ErrProjectNotEmpty" services/inventory/internal/usecase/delete_project.go`
    - `grep -q "uow.Do" services/inventory/internal/usecase/create_project.go`
  </acceptance_criteria>
  <done>CreateProject присваивает ID + эмитит ProjectCreated; DeleteProject = delete-only-if-empty через query-порт (типизированный конфликт) + физическое удаление; форма идентична Phase 7; тесты зелёные.</done>
</task>

<task type="auto">
  <name>Task 2: Location CRUD usecases (DC/Module/Rack)</name>
  <files>services/inventory/internal/usecase/crud_dc.go, services/inventory/internal/usecase/crud_module.go, services/inventory/internal/usecase/crud_rack.go, services/inventory/internal/usecase/location_usecases_test.go</files>
  <read_first>
    - .planning/phases/06-inventory/06-PATTERNS.md (§loc CRUD — Рецепт 1 форма, mirror create_project)
    - knowledge/patterns.md (Рецепт 1 Execute)
    - .planning/REQUIREMENTS.md (LOC-01 CRUD; LOC-02 иерархия)
    - services/inventory/internal/domain/dc.go (DC — из 06-03)
    - services/inventory/internal/domain/module.go (Module — из 06-03)
    - services/inventory/internal/domain/rack.go (Rack — из 06-03)
    - services/inventory/internal/domain/ports.go (DCRepository/ModuleRepository/RackRepository — из 06-01)
    - services/inventory/internal/usecase/create_project.go (форма — из Task 1)
  </read_first>
  <action>
    Создай три файла (package usecase), каждый — Create/Update/Delete usecase'ы своего локационного агрегата в форме `uow.Do(fn){repo.Save|Delete → enrich → outbox.Append}` (LOC-01 CRUD):
    `crud_dc.go`: `CreateDCUseCase` (NewDC → Save → DCCreated), `UpdateDCUseCase` (Load → Update → Save → DCUpdated), `DeleteDCUseCase` (Load → Delete event → repo.Delete → DCDeleted). DTO Input/Output на каждый.
    `crud_module.go`: аналогично для Module; Create принимает `DCID` (иерархия LOC-02/D-06) — guard на zero DCID живёт в домене (06-03), usecase лишь прокидывает.
    `crud_rack.go`: аналогично для Rack; Create принимает `ModuleID` + топология-атрибуты (LOC-04). outbox.Append внутри fn (D-02/Pitfall 8). Ошибки `%w`. RU-комментарии, EN-идентификаторы/строки.
    Создай `internal/usecase/location_usecases_test.go`: спеки (mockery-моки DCRepository/ModuleRepository/RackRepository + фейки uow/outbox/clock/idgen). Ассерты: каждая CRUD-операция кладёт ровно один ожидаемый семантический envelope в FakeOutbox.Appended (EVT-01); Create Module/Rack прокидывает parent-ID; Delete вызывает repo.Delete. EN-комментарии.
  </action>
  <verify>
    <automated>cd services/inventory && go test ./internal/usecase/... -count=1 2>&1 | tail -25 && go test ./... 2>&1 | tail -10 && go vet ./... 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `go test ./...` из services/inventory exits 0 (полный inventory unit-suite зелёный)
    - `grep -q "uow.Do" services/inventory/internal/usecase/crud_dc.go` && `grep -q "uow.Do" services/inventory/internal/usecase/crud_module.go` && `grep -q "uow.Do" services/inventory/internal/usecase/crud_rack.go`
    - `grep -q "DCID" services/inventory/internal/usecase/crud_module.go` (иерархия прокинута)
    - `go vet ./...` exits 0
  </acceptance_criteria>
  <done>DC/Module/Rack CRUD-usecases в канонической форме; каждая операция эмитит одно семантическое событие; parent-ID прокинут; полный inventory unit-suite зелёный.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usecase→HostsInProject query-порт | delete-only-if-empty пересекает границу чтения; результат — доменный конфликт, не сырая ошибка |
| usecase→outbox | CRUD-события в той же tx-форме, что repo.Save/Delete |

> Чистый домен на фейках. Реальный atomicity + partial-index defense — Phase 7. Форма зафиксирована.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-17 | Information Disclosure | DeleteProject not-empty check | mitigate | HostsInProject query-порт → типизированный ErrProjectNotEmpty{ProjectID,HostCount}; сырая DB-ошибка не протекает (INV-10/D-11) |
| T-06-18 | Tampering | DeleteProject/DeleteDC физическое удаление | mitigate | proj.Delete event + repo.Delete (hard); нет soft-delete-флага — обход истории невозможен (D-09) |
| T-06-19 | Repudiation | CRUD-события локаций/Project | mitigate | enrich навешивает actor на каждое CRUD-событие — изменение топологии атрибутируемо (EVT-02/D-15) |
</threat_model>

<verification>
- `cd services/inventory && go test ./... -count=1` зелёный (полный inventory unit-suite)
- `go build ./... && go vet ./...` exits 0
- DeleteProject = delete-only-if-empty через query-порт; CRUD-операции — по одному envelope
- outbox.Append внутри uow.Do во всех usecase
</verification>

<success_criteria>
- CreateProject присваивает ProjectID + один ProjectCreated (INV-01/03/EVT-01)
- DeleteProject = delete-only-if-empty через HostsInProject query-порт → ErrProjectNotEmpty (INV-10)
- DC/Module/Rack CRUD-usecases в форме uow.Do{…→outbox.Append} (LOC-01)
- Каждая операция эмитит ровно одно семантическое событие с actor (EVT-01/EVT-02)
- Полный inventory unit-suite зелёный (SVC-01 слои компилируются)
</success_criteria>

<output>
Create `.planning/phases/06-inventory/06-05-SUMMARY.md` when done
</output>
