---
phase: 02-foundation-docs
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - knowledge/git.md
  - knowledge/README.md
autonomous: true
requirements: [DOC-06]
must_haves:
  truths:
    - "Агент знает рабочие ветки (dev/main) и remote origin (github.com/neeeekto/gwall-e)"
    - "Агент пишет коммиты в Conventional Commits и знает, когда коммитить"
    - "Агент знает нормы PR проекта"
    - "Агент видит краткий блок GSD-практик (docs(NN): scope, Co-Authored-By, .planning, gsd-pr-branch) как GSD-тулинг, не как общий стандарт"
    - "knowledge/README.md больше не помечает git.md как «запланировано» и ссылается на него"
  artifacts:
    - path: "knowledge/git.md"
      provides: "Git-конвенции проекта: ветки, Conventional Commits, PR, когда коммитить + краткий GSD-блок"
      contains: "Conventional Commits"
      min_lines: 25
    - path: "knowledge/README.md"
      provides: "Индекс со ссылкой на git.md (без статуса «запланировано»)"
      contains: "git.md"
  key_links:
    - from: "knowledge/README.md"
      to: "knowledge/git.md"
      via: "markdown-ссылка в таблице индекса"
      pattern: "\\[git\\.md\\]\\(git\\.md\\)"
---

<objective>
Наполнить `knowledge/git.md` (DOC-06): git-конвенции проекта — ветки, Conventional Commits,
нормы PR, «когда коммитить», плюс краткий блок GSD-практик (per D-05). Снять статус
«запланировано» и добавить ссылку на git.md в индекс `knowledge/README.md`.

Purpose: команда/ИИ имеют единые git-правила; GSD-специфика упомянута кратко со ссылкой, без
дублирования полного GSD git-workflow (живёт в GSD-доках).
Output: `knowledge/git.md`, обновлённая строка индекса git.md в `knowledge/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-docs/02-CONTEXT.md
@knowledge/authoring.md
@knowledge/README.md
</context>

<artifacts_this_phase_produces>
Этот план создаёт/изменяет:
- `knowledge/git.md` (новый файл)
- `knowledge/README.md` (изменение: индекс-строка git.md — снять «запланировано», проставить ссылку и статус «существует»; обновить маркер в «Порядок чтения»)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Создать knowledge/git.md — git-конвенции + краткий GSD-блок</name>
  <files>knowledge/git.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт MUST/SHOULD/WON'T, парность «запрет → do», pointer-over-copy, no-phantom, размер ~150–200 строк
    - knowledge/README.md — формат/язык (русское содержимое, тех-термины английские)
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — D-05 (общие конвенции + краткое упоминание GSD; полный GSD git-workflow — WON'T дублировать); раздел `<specifics>` (реальные конвенции из истории: ветки dev/main, Co-Authored-By, phase-scope docs(NN):)
    - Реальный репо: `git remote -v` → origin = https://github.com/neeeekto/gwall-e.git; ветки `dev`/`main` (origin/HEAD → main); история коммитов (Conventional Commits с phase-scope: `docs(01):`, `docs(02):`, `docs(phase-1):`, `fix(01):`)
  </read_first>
  <action>
    Создай `knowledge/git.md` на русском по стандарту authoring.md (каждое правило с тегом
    MUST/SHOULD/WON'T; каждый запрет — с предписанной альтернативой «do»). Содержимое:
    - Ветки: `dev` — рабочая интеграционная, `main` — основная (origin/HEAD → main). Remote
      `origin` → `github.com/neeeekto/gwall-e`. Сформулируй как MUST/SHOULD рабочий поток ветвления
      на основе реального состояния (две ветки dev/main).
    - Conventional Commits: **MUST** — формат `type(scope): subject` (типы feat/fix/docs/refactor/...).
      Привести реальный пример из истории как образец. Запрет «свободных» сообщений — WON'T, вместо
      этого Conventional Commits.
    - Нормы PR: что попадает в PR, как описывать; **SHOULD** держать PR сфокусированным.
    - «Когда коммитить»: правило о логически завершённых атомарных изменениях.
    - Краткий блок «GSD-практики» (per D-05) — пометить как GSD-тулинг (НЕ общий стандарт):
      коммиты в `.planning/` для планов/состояния; phase-scoped scope вида `docs(NN):`
      (NN — номер фазы), trailer `Co-Authored-By:`; фильтрация `.planning` из PR через `gsd-pr-branch`.
      Дать со ссылкой/пометкой «детали — в GSD-доках/командах». Полный GSD git-workflow дублировать
      — **WON'T**, потому что он живёт в GSD-доках и разойдётся с копией; вместо копии — краткое
      упоминание + указатель.
    - Pointer-over-copy: не копировать GSD-документацию целиком; не выдумывать несуществующие
      процессы (no-phantom) — описывать только реально применяемые в репо конвенции.
    Размер: ~< 120 строк. Без длинных фенсед-листингов; примеры коммитов — короткий инлайн.
  </action>
  <verify>
    <automated>test -f knowledge/git.md && grep -q 'Conventional Commits' knowledge/git.md && grep -qi 'dev' knowledge/git.md && grep -qi 'main' knowledge/git.md && grep -q 'neeeekto/gwall-e' knowledge/git.md && grep -qi 'GSD' knowledge/git.md && grep -q 'Co-Authored-By' knowledge/git.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/git.md` существует
    - содержит `Conventional Commits`
    - называет ветки `dev` и `main`
    - содержит remote `neeeekto/gwall-e`
    - содержит краткий GSD-блок: упоминает `GSD`, `Co-Authored-By` и `gsd-pr-branch` (grep каждого находит)
    - содержит правило «когда коммитить» (grep `коммит` находит контекст)
    - каждое нормативное правило несёт тег `**MUST**`/`**SHOULD**`/`**WON'T**` (grep находит ≥1)
    - НЕ дублирует полный GSD git-workflow — файл остаётся ~< 150 строк (`test $(wc -l < knowledge/git.md) -lt 150`)
  </acceptance_criteria>
  <done>Файл создан: общие git-конвенции (ветки dev/main, origin neeeekto/gwall-e, Conventional Commits, PR, когда коммитить) + краткий GSD-блок со ссылкой, без дублирования полного GSD-workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Обновить knowledge/README.md — снять «запланировано» и проставить ссылку на git.md</name>
  <files>knowledge/README.md</files>
  <read_first>
    - knowledge/README.md — текущий индекс (строка git.md помечена «запланировано (Phase 2)»); блок «Порядок чтения»; правило «ссылка появляется вместе с файлом»
    - knowledge/authoring.md — ссылки только на существующие файлы
    - knowledge/git.md (создан в Task 1) — целевой файл ссылки
  </read_first>
  <action>
    Отредактируй `knowledge/README.md`: в строке таблицы «Индекс» для `git.md` преврати имя файла
    в рабочую markdown-ссылку `[git.md](git.md)` и смени статус с «запланировано (Phase 2)» на
    «существует». В блоке «Порядок чтения» у пункта про git.md убрать маркер «*(Phase 2)*».
    Не трогать строку boundaries.md (её обновит план 02-03) — оставить «запланировано (Phase 2)»
    без ссылки. Строки structure.md/build.md уже обновлены планом 02-01 — не ломать их.
  </action>
  <verify>
    <automated>grep -Eq '\[git\.md\]\(git\.md\)' knowledge/README.md && ! grep -q 'git.md.*запланировано' knowledge/README.md && grep -q 'boundaries.md.*запланировано' knowledge/README.md && for f in $(grep -oE '\]\([a-z-]+\.md\)' knowledge/README.md | sed -E 's/\]\(|\)//g'); do test -f knowledge/$f || exit 1; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/README.md` содержит рабочую ссылку `[git.md](git.md)`
    - строка git.md больше НЕ содержит «запланировано»
    - строка boundaries.md остаётся «запланировано» без ссылки
    - все relative-ссылки в README ведут на существующие файлы (цикл проверки проходит)
  </acceptance_criteria>
  <done>Индекс обновлён: git.md — со ссылкой и статусом «существует»; boundaries.md остаётся запланированным; битых ссылок нет.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Фаза производит только markdown-документацию в `knowledge/`. Исполняемого кода, обработки
ввода и сетевой поверхности не вводится — новых trust boundaries нет.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | knowledge/git.md контент | mitigate | НЕ документировать секреты/токены/внутренние URL в git-доке; только публичные конвенции и публичный remote |
| T-02-02 | — (no executable surface) | вся фаза | accept | Фаза не вводит исполняемой поверхности/ввода → high-severity угроз нет |
</threat_model>

<verification>
- `test -f knowledge/git.md`
- git.md отражает реальные конвенции репо (ветки dev/main, origin neeeekto/gwall-e, Conventional Commits с phase-scope) + краткий GSD-блок со ссылкой
- README.md ссылается на git.md, статус «запланировано» снят
- Битых relative-ссылок в README нет
</verification>

<success_criteria>
- DOC-06 закрыт: `knowledge/git.md` фиксирует git-конвенции (ветки, Conventional Commits, нормы PR, когда коммитить) + краткий GSD-блок (D-05)
- Полный GSD git-workflow НЕ продублирован (WON'T per D-05)
- Индекс `knowledge/README.md` синхронизирован (ссылка + статус)
- Все нормативные правила тегированы MUST/SHOULD/WON'T; запреты парны с «do»
</success_criteria>

<output>
Create `.planning/phases/02-foundation-docs/02-02-SUMMARY.md` when done
</output>
