---
phase: 02-foundation-docs
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - knowledge/boundaries.md
  - knowledge/structure.md
  - knowledge/README.md
autonomous: true
requirements: [DOC-08]
must_haves:
  truths:
    - "Агент знает: WIP-сервисы вне go.work не чинить/не расширять"
    - "Агент знает: стале README/Makefile/docker-compose.yml не авторитетны"
    - "Агент знает: не документировать несуществующие фичи (phantom rules)"
    - "Агент знает: черновики/спайки/планы в .planning/ не канон; не запускать авто-регенерацию тяжёлых секций CLAUDE.md"
    - "Агент видит карту владения фактами: один факт = один канон-док, остальные ссылаются"
    - "boundaries.md ↔ structure.md взаимно ссылаются по WIP-статусу inventory"
    - "knowledge/README.md больше не помечает boundaries.md как «запланировано» и ссылается на него"
  artifacts:
    - path: "knowledge/boundaries.md"
      provides: "Правила «do-not» на уровне возможностей + примеры + карта владения фактами"
      contains: "phantom"
      min_lines: 30
    - path: "knowledge/README.md"
      provides: "Индекс со ссылкой на boundaries.md (без статуса «запланировано»)"
      contains: "boundaries.md"
  key_links:
    - from: "knowledge/boundaries.md"
      to: "knowledge/structure.md"
      via: "относительная ссылка по WIP-статусу inventory"
      pattern: "structure\\.md"
    - from: "knowledge/structure.md"
      to: "knowledge/boundaries.md"
      via: "относительная ссылка (правило не трогать WIP-леса)"
      pattern: "boundaries\\.md"
    - from: "knowledge/boundaries.md"
      to: "knowledge/build.md"
      via: "карта владения фактами (команды — канон в build.md)"
      pattern: "build\\.md"
---

<objective>
Наполнить `knowledge/boundaries.md` (DOC-08): правила «do-not» на уровне возможностей + примеры
(per D-06), перенести два посева Phase 1 (per D-07), добавить явную карту владения фактами
(per D-08). Замкнуть взаимные ссылки boundaries.md ↔ structure.md по WIP-статусу inventory.
Снять статус «запланировано» и добавить ссылку на boundaries.md в индекс `knowledge/README.md`.

Purpose: ИИ/команда имеют границы «что не трогать» и единую карту владения фактами (один факт =
один канон), без хрупких path-карт и phantom-правил (Pitfall 3/4/8).
Output: `knowledge/boundaries.md`, обратная ссылка в `knowledge/structure.md`, обновлённая строка
индекса boundaries.md в `knowledge/README.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation-docs/02-CONTEXT.md
@.planning/phases/01-knowledge-base-layout/01-CONTEXT.md
@knowledge/authoring.md
@knowledge/README.md
@.planning/research/PITFALLS.md
</context>

<artifacts_this_phase_produces>
Этот план создаёт/изменяет:
- `knowledge/boundaries.md` (новый файл, включает карту владения фактами per D-08)
- `knowledge/structure.md` (изменение: проставить рабочую обратную ссылку на boundaries.md по WIP-статусу inventory — замкнуть пару D-03/D-08)
- `knowledge/README.md` (изменение: индекс-строка boundaries.md — снять «запланировано», проставить ссылку и статус «существует»; обновить маркер в «Порядок чтения»)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Создать knowledge/boundaries.md — правила «do-not» + посевы Phase 1 + карта владения фактами</name>
  <files>knowledge/boundaries.md</files>
  <read_first>
    - knowledge/authoring.md — стандарт MUST/SHOULD/WON'T, парность «запрет → do», pointer-over-copy, no-phantom, без хрупких path-карт
    - knowledge/README.md — блок «Что где живёт» (knowledge/ = канон, .planning/ = процесс); формат/язык
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — D-06 (do-not на уровне возможностей + примеры, без path-карт; inventory/gateway/outgate как ПРИМЕРЫ), D-07 (два посева Phase 1: (а) что НЕ кладём в knowledge/; (б) риск ре-раздувания CLAUDE.md через generate-claude-md), D-08 (карта владения фактами: язык кода → style.md (Phase 3); WIP inventory → structure.md; команды → build.md; один факт = один канон)
    - .planning/phases/01-knowledge-base-layout/01-CONTEXT.md — посевы Phase 1 (раскладка, источник истины AGENTS.md, риск регенерации CLAUDE.md)
    - .planning/research/PITFALLS.md — Pitfall 4 (scaffolding-as-design), Pitfall 8 (phantom rules), Pitfall 3 (brittle path maps)
    - knowledge/structure.md (создан в плане 02-01) — WIP-статус inventory (канон там; boundaries ссылается)
    - knowledge/build.md (создан в плане 02-01) — команды (канон там)
  </read_first>
  <action>
    Создай `knowledge/boundaries.md` на русском по стандарту authoring.md (каждый запрет — WON'T
    с предписанной альтернативой «do», парно). Уровень — возможности + примеры, БЕЗ хрупких
    path-карт (per D-06). Содержимое:
    - Правило WIP-лесов: WIP-сервисы вне `go.work` **WON'T** чинить/расширять/достраивать
      (nil-deps, TODO, пустые internal-каталоги), потому что они намеренно пересобираются;
      вместо этого **MUST** оставить как есть и работать в активных модулях. `inventory`
      (а также `gateway`/`outgate` — README+go.mod-заглушки) — как ПРИМЕРЫ, не жёсткий список путей.
      Сослаться на `structure.md` за актуальным WIP-статусом inventory (относительная ссылка —
      владение фактом WIP у structure.md).
    - Правило стале-файлов: корневые/сервисные `README`/`Makefile`/`docker-compose.yml`, помеченные
      устаревшими, **WON'T** считать авторитетом и **WON'T** повторять их содержимое в базе; вместо
      этого **MUST** опираться на `knowledge/`-канон (structure.md/build.md).
    - Правило phantom-фич: документировать поведение несуществующих подсистем/фич **WON'T** (per
      authoring no-phantom); вместо этого база описывает только реально существующее, а будущее —
      в `.planning/`/«запланировано».
    - Посев Phase 1 (а): что НЕ кладём в `knowledge/` — черновики/спайки/планы (`.planning/`) НЕ
      канон. Сформулировать как правило (расширяет блок «Что где живёт» README).
    - Посев Phase 1 (б): риск ре-раздувания корневого `CLAUDE.md` через `generate-claude-md` —
      **WON'T** запускать авто-генерацию тяжёлых секций / ре-раздувать CLAUDE.md; вместо этого
      **MUST** держать тонкий гибрид (указатель на AGENTS.md/knowledge). Зафиксировать как правило.
    - Карта владения фактами (D-08) — короткая таблица «какой факт где канон»: язык кода/комментариев
      → `style.md` (Phase 3, помечен «запланировано» — НЕ ссылка, упоминание); WIP-статус `inventory`
      → `structure.md` (ссылка); команды сборки/тестов → `build.md` (ссылка); git-конвенции →
      `git.md` (ссылка). Принцип: один факт = один канон, остальные доки ссылаются, не копируют
      (pointer-over-copy). Ссылки в таблице — только на уже существующие файлы (style.md ещё нет —
      указать со статусом «запланировано», без ссылки, per authoring).
    Размер: ~< 140 строк; если превышает 150 — дробить и связывать ссылками (authoring).
    Без длинных фенсед path-листингов.
  </action>
  <verify>
    <automated>test -f knowledge/boundaries.md && grep -qi 'phantom' knowledge/boundaries.md && grep -q 'go.work' knowledge/boundaries.md && grep -q 'docker-compose\|Makefile' knowledge/boundaries.md && grep -q 'generate-claude-md\|CLAUDE.md' knowledge/boundaries.md && grep -q '.planning' knowledge/boundaries.md && grep -q 'structure.md' knowledge/boundaries.md && grep -q 'build.md' knowledge/boundaries.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/boundaries.md` существует
    - содержит правило phantom (grep `phantom` находит)
    - содержит правило WIP-лесов вне `go.work` (содержит `go.work` и `WIP`)
    - содержит правило стале-файлов (содержит `Makefile` или `docker-compose`)
    - содержит посев (б) про CLAUDE.md/generate-claude-md (grep `generate-claude-md` или `CLAUDE.md` находит)
    - содержит посев (а) про `.planning` не-канон (grep `.planning` находит)
    - карта владения фактами ссылается на `structure.md` и `build.md` (grep каждого находит)
    - упоминает `inventory` как ПРИМЕР (grep `inventory` находит)
    - каждый запрет парен с «do»: grep тегов `\*\*WON'T\*\*` находит ≥1 и `\*\*MUST\*\*` находит ≥1
    - файл ~< 150 строк (`test $(wc -l < knowledge/boundaries.md) -lt 160`)
  </acceptance_criteria>
  <done>Файл создан: do-not правила (WIP-леса, стале-файлы, phantom) на уровне возможностей+примеры; оба посева Phase 1; карта владения фактами со ссылками на structure.md/build.md/git.md и упоминанием style.md как запланированного.</done>
</task>

<task type="auto">
  <name>Task 2: Замкнуть обратную ссылку в knowledge/structure.md на boundaries.md</name>
  <files>knowledge/structure.md</files>
  <read_first>
    - knowledge/structure.md — раздел про inventory WIP (упоминание boundaries.md, вставленное планом 02-01)
    - knowledge/boundaries.md (создан в Task 1 этого плана) — целевой файл ссылки
    - .planning/phases/02-foundation-docs/02-CONTEXT.md — D-03/D-08 (boundaries.md ↔ structure.md взаимные ссылки по WIP-статусу inventory)
  </read_first>
  <action>
    Отредактируй `knowledge/structure.md`: в разделе про WIP-статус `inventory` убедись, что
    упоминание `boundaries.md` оформлено как рабочая относительная markdown-ссылка
    `[boundaries.md](boundaries.md)` (правило «не чинить WIP-леса»). Если план 02-01 оставил
    голое упоминание без ссылки — преврати его в рабочую ссылку теперь, когда boundaries.md
    существует. Не переписывать остальной контент structure.md.
  </action>
  <verify>
    <automated>grep -Eq '\[boundaries\.md\]\(boundaries\.md\)' knowledge/structure.md && test -f knowledge/boundaries.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/structure.md` содержит рабочую ссылку `[boundaries.md](boundaries.md)`
    - целевой файл `knowledge/boundaries.md` существует (ссылка не битая)
    - пара boundaries.md ↔ structure.md замкнута (boundaries.md ссылается на structure.md из Task 1, structure.md — на boundaries.md)
  </acceptance_criteria>
  <done>structure.md содержит рабочую обратную ссылку на boundaries.md; взаимная пара по WIP-статусу inventory замкнута, битых ссылок нет.</done>
</task>

<task type="auto">
  <name>Task 3: Обновить knowledge/README.md — снять «запланировано» и проставить ссылку на boundaries.md; финальная проверка ссылок</name>
  <files>knowledge/README.md</files>
  <read_first>
    - knowledge/README.md — текущий индекс (строка boundaries.md помечена «запланировано (Phase 2)»); блок «Порядок чтения»
    - knowledge/authoring.md — ссылки только на существующие файлы
    - knowledge/boundaries.md (создан в Task 1) — целевой файл ссылки
  </read_first>
  <action>
    Отредактируй `knowledge/README.md`: в строке таблицы «Индекс» для `boundaries.md` преврати имя
    файла в рабочую markdown-ссылку `[boundaries.md](boundaries.md)` и смени статус с
    «запланировано (Phase 2)» на «существует». В блоке «Порядок чтения» у пункта про boundaries.md
    убрать маркер «*(Phase 2)*». Не трогать строки style.md/testing.md/architecture.md/patterns.md
    (Phase 3 — остаются «запланировано»). После правки выполни финальную проверку: все relative-ссылки
    во ВСЕХ четырёх новых доках Phase 2 (structure.md, build.md, git.md, boundaries.md) и в README.md
    ведут на существующие файлы.
  </action>
  <verify>
    <automated>grep -Eq '\[boundaries\.md\]\(boundaries\.md\)' knowledge/README.md && ! grep -q 'boundaries.md.*запланировано' knowledge/README.md && grep -q 'style.md.*запланировано' knowledge/README.md && for src in knowledge/README.md knowledge/structure.md knowledge/build.md knowledge/git.md knowledge/boundaries.md; do for f in $(grep -oE '\]\([a-z][a-z-]*\.md\)' "$src" | sed -E 's/\]\(|\)//g'); do test -f knowledge/$f || { echo "BROKEN $src -> $f"; exit 1; }; done; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `knowledge/README.md` содержит рабочую ссылку `[boundaries.md](boundaries.md)`
    - строка boundaries.md больше НЕ содержит «запланировано»
    - строки Phase 3 (style.md и др.) остаются «запланировано» без ссылок (grep `style.md.*запланировано` находит)
    - финальная проверка: нет битых relative-ссылок ни в README.md, ни в structure.md/build.md/git.md/boundaries.md (цикл проверки проходит, печатает OK)
  </acceptance_criteria>
  <done>Индекс обновлён: boundaries.md — со ссылкой и статусом «существует»; Phase 3 доки остаются запланированными; все relative-ссылки во всех четырёх новых доках и README валидны (битых нет).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

Фаза производит только markdown-документацию в `knowledge/`. Исполняемого кода, обработки
ввода и сетевой поверхности не вводится — новых trust boundaries нет.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | knowledge/boundaries.md контент | mitigate | НЕ документировать секреты/внутренние хостнеймы/креды; границы и карта владения — только публичные факты репо |
| T-02-02 | — (no executable surface) | вся фаза | accept | Фаза не вводит исполняемой поверхности/ввода → high-severity угроз нет |
</threat_model>

<verification>
- `test -f knowledge/boundaries.md`
- boundaries.md содержит do-not правила (WIP-леса, стале-файлы, phantom), оба посева Phase 1, карту владения фактами
- structure.md ↔ boundaries.md взаимно ссылаются по WIP-статусу inventory
- README.md ссылается на boundaries.md, статус «запланировано» снят
- Нет ни одной битой relative-ссылки во всех четырёх новых доках Phase 2 и README.md
</verification>

<success_criteria>
- DOC-08 закрыт: `knowledge/boundaries.md` содержит правила «do-not» (WIP-леса; стале README/Makefile/docker-compose не авторитетны; phantom rules) на уровне возможностей+примеры (D-06)
- Перенесены оба посева Phase 1 (D-07): .planning/ не канон; риск ре-раздувания CLAUDE.md
- Присутствует карта владения фактами (D-08): один факт = один канон, остальные ссылаются
- Пара boundaries.md ↔ structure.md замкнута (D-03/D-08)
- Все нормативные правила тегированы MUST/SHOULD/WON'T; запреты парны с «do»; битых ссылок нет
</success_criteria>

<output>
Create `.planning/phases/02-foundation-docs/02-03-SUMMARY.md` when done
</output>
